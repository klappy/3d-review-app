Queue contextual feedback with **observability as the first constraint**. Planning/design only; no version assigned. Keep current shipments and roadmap PR127, provenance PR120 and reusable-pattern issue129 separate.

Use the existing feedback capability/storage/support pipeline and issue116 attribution work. Do not add a second backend or tool. The proposed frontend provides page-level feedback plus an optional element picker; agents/MCP submit through the same existing capability using the same stable component identifier and semantics. Picker labels must come from a maintained component registry, not scraped user text or DOM selectors containing user data.

Minimum context: safe route template (not full URL), stable component ID if selected, surface, experienced UI artifact version, relevant API response version if actually observed, observation/report time and description; optional annotation. Distinguish trusted submission identity from client-reported experienced identity, including stale tabs and unknown versions. Do not label nearby health or submission version as the experienced version. HTTP/MCP and frontend parity use the same field contract, privacy bounds and uncertainty semantics. Issue116/PR120 owns foundational provenance; this is its separately scoped contextual UI/agent follow-up.

Privacy by allowlist: no answers, input values, tokens, query strings, query IDs, session IDs, raw selectors, private object IDs or automatic DOM capture. Preview the exact context/attachments before sending. Screenshots are explicit, optional and removable/redactable before transmission; annotation is optional and bounded. No automatic screenshot, replay recording or collection from another app. Store/read attachments only under the existing authorized custody model after separately reviewing its support; lack of attachment support does not justify a parallel upload backend.

Evidence chain: preserve each occurrence → linked issue → decisions/hypothesis → fix attempts → exact release and environment → same-scenario verification → original-user outcome. Deduplicate repeated retrieval, not distinct occurrences. Preserve not reproduced, unknown provenance, pending delivery, suspected failed remediation, confirmed persistence/regression, technical resolution and user confirmation as separate states. Reopening/new tickets cannot reset the attempt ledger. Private raw feedback stays authenticated; public summaries are sanitized.

Measurement before implementation:
- Freeze a named cohort by received-time window, surface and scenario; report total eligible occurrences, exclusions with reasons, unique occurrences and dedup rule. Never silently drop incomplete reports.
- Evidence completeness: occurrences with every required applicable field / all eligible occurrences, with per-field present/unknown/unavailable/invalid counts. Optional screenshot/annotation absence is not incompleteness; declared unknown version remains unknown, not successful attribution.
- Report→actionable latency: trusted receipt timestamp to first recorded triage decision meeting a declared minimum (expected/observed behavior, scope, evidence/unknowns and owned next action). Do not rewrite the original timestamp.
- Report→reproducible-evidence latency: receipt to first linked actual reproduction using the declared scenario/surface/version and evidence class. Keep unable-to-reproduce and still-open cases visible as censored/unresolved; do not count classification alone as reproduction.
- Show sample size, observation window, completed count, unresolved count and units; median/p95 only with an adequate declared sample method. No baseline delta until comparable cohorts exist. Satisfaction/confusion/frustration and efficacy remain NOT MEASURED without direct supported observations.

Bounded acceptance plan: (1) normal page-level entry; (2) optional stable element selection/cancel; (3) identical agent/MCP component report; (4) exact preview, optional redact/remove screenshot; (5) privacy rejection; (6) stale UI versus newer API identity; (7) missing/denied evidence; (8) duplicate retrieval versus distinct recurrence and released failed attempt. Fixed eight scenario families; expand transparently if role/surface variants create more cases. Publish pass/attempt/fail/untested by surface without treating family counts as independent user outcomes.

The live roadmap (issue123) observes delivery. This slice observes user experience and evidence quality, linking delivery receipts without conflating the two. A future reuse view (issue129) can project its adoption evidence; it must not claim replication or gold-standard outcomes from this plan. Authority: kitchen feedback-release-loop recipe/templates/notes, existing privacy/auth contracts and normal release gates. Next step after active shipments: refine the exact shared field/UI contract and smallest independently releasable slice; no product changes, monitor installation or live submissions authorized by this planning ticket alone.


Source observation: issue116 describes missing experienced-release attribution; PR120 explicitly implements only server-derived submission identity and bounded optional client-reported evidence. It does not claim automatic loaded UI/API capture. This design preserves that boundary and adds no evidence retrospectively. Current implementation baseline and field-level source pins must be re-observed when this queue item is fired.

## Source inbox and disposition — creator clarification

The feedback database is a **private harvested source inbox**, alongside authorized transcripts, PDFs and repositories. It is not an opaque final backlog. Source ingestion preserves custody, source type/locator, observation time and dedup evidence; it does not automatically publish content or convert a report into a committed feature. This plan grants no new connector access, harvesting job or permission to read unrelated material.

Every eligible report receives an owned disposition: awaiting triage, needs clarification, linked existing concern, distinct candidate, deferred, declined with reason, or resolved with evidence. Preserve occurrences even when concerns are grouped. Make a sanitized source→issue→decision→attempt→release→outcome chain reviewable without exposing the underlying private source; authorized internal provenance retains the precise locator. Reports with missing evidence remain explicitly unknown or not reproduced, never silently discarded. Deferred/declined is a decision, not a successful outcome.

Measure **untriaged count**, **oldest untriaged age**, and **time-to-actionable disposition** against the same fixed received-time cohort, eligibility rules and dedup definitions in issue130. Report observation timestamp and unresolved count; oldest age is measured from original trusted receipt time, not last retrieval or reclassification. Keep source publication lag and unknown source event time distinct. A shrinking inbox count does not establish quality if records were merely grouped, hidden or closed.
