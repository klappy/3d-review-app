# Scoped B gate evidence

Actual Oddkit preflight06:48:03.205Z FOUND, with contract-governed handoff and independent agent-validation constraints. Actual challenge06:48:14.033Z CHALLENGED, block_until_addressed:false, tensions:[]; not a PASS. Generic prompts are disposed below, not silently treated as semantic validation.

Driver-seat outcome: an authorized staff retry must not fabricate a result, leak another grant's report or disclose mixed data. Existing source report adapter/query experiments informed the design; accepted strict A attestation and current schema were inspected. We reuse canonical/input proof and standard conditional SQL, not a new named architecture. Separate SELECTs across awaited JavaScript would lose the chosen observation boundary; source labels/prefixes cannot establish eligibility; broad OR IGNORE hides constraint failures;0005adapter is unused and not imported.

Working confidence is supported by independent34assessment/425row SQLite capture-to-A probes and full plan review, not actual D1 runtime proof. Disconfirmers are actual D1 bind/semantics differences, failed malformed or mixed input refusals, exceeded resource limits, wrong stored identity or race outcomes. Stop and amend on those. Costs are an acknowledged3–4active author hours plus independent review and remaining C/UI/rollout; no08:00 promise. Local ingredient changes can be abandoned without remote effects. Remote DDL/release is a separate reviewed boundary; no provider mutation is authorized here.

Root current-source selection, exact persistence/readback, final narrow review and explicit FIRE remain distinct from this preflight/challenge. Candidate review must be an independent agent, not green lint or author's own tests.
