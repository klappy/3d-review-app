# September 17 B1 amendment to existing report-entry ticket

What this is: Build the local complete-capture and strict attestation adapter ingredient only.
Why now: This reviewed dependency can progress without waiting for public rendering or remote CPU entitlement evidence.
Your move: Root lands the exact reviewed packet, closes the final readback gate and fires the acknowledged author.

Class: entrée. Risk: STANDARD, isolated synthetic fixtures and reversible local code; no real participant data access, remote state or public routes. Station: subagent. Owner: /root/queue_resolution/a8_inert_author. Coordinator: /root/release_reconciliation. Promise: actual ACK30active minutes from actual START, checkpoint30; first iteration, not finish promise. Overall B estimate3–4active hours remains unchanged. Depends: accepted A3523f20, accepted B plan54e05455 and narrow B1 review, root persistence/FIRE.

Base exactly3523f20ae5807a0f1d4a047352b62c3aa6d2dd82, parent maina57ba930ced3c23616982c7dab291949b75a3b8a. Isolated /tmp/3d-materialization-author, local report/materialization-boundary-20260917. Root retains integration; no shared push before disposition. All parent inputs/toolchain preserved.

Exactly three editable app paths: src/report-capture.ts; test/synthetic-report-boundary.test.ts; docs/synthetic-report-boundary.md. No store, materialization-test, migration, A, Auth, UI, routes, registry, package, seed or provider changes. Auth0008 reservation5710224924 remains held for B2, not permission to create/apply the table in B1.

Use exact accepted B PLAN54e05455 and B1-CAPTURE.sql8e3f51e2f5cb7350c2a7824e5a78c91c64fb17a1966d79b9fe3f064c711598c6. This replaces only target report lookup with `?2 IS NULL AND a.id=?3`, retains five bindings and all authority/integrity/packing predicates, and appends the capture SELECT. rid=NULL, minimum='member'; never create synthetic_report merely to satisfy unused SQL. Map exact raw strings into accepted A without parsing away duplicate evidence. Fixtures use committed synthetic inputs only; no remote queries.

Actual account standard usage_model is observed; paid CPU entitlement is not. Narrow independent disposition explicitly defers effective-runtime limit acceptance for isolated B1 authoring only. Record local sizes/timing and actual runtime; integrated release still requires CPU/memory/platform comparison, security review and later renderer/contract/UI/rollout gates.

## Done means for this bounded iteration
1. Reviewer can inspect exact base/diff and observe only three declared paths.
2. Author can prepare and bind the five-parameter statement on local D1 without a report table and report actual success or exact failure.
3. Reviewer can run measured gold coverage and observe assessments/34, tuples/425 and maximum packed/template bytes, without hidden truncation.
4. Reviewer can pass real packed captures through A and observe eligibility; malformed/raw-duplicate, wrong-grant, archived or mixed inputs refuse under applicable boundary tests.
5. Integrator can read saved commit and exported adapter/types, observe no public routes or storage writes, and reuse exact captures later.
6. Coordinator can inspect actual30-minute checkpoint, failures and remaining estimate without mistaking this iteration for whole B/report delivery.

## Reference fidelity and prior art

Reuse accepted A utility3523, its independent f1a926b3 review, actual app schema a57, existing d605 report/query experiments and independently inspected34/425 capture probes. Earlier house inventory and report source receipts remain attributed in parent DELTA/18-B design; no new discovery is claimed. Old report-snapshot adapter depends on0005 and does not provide this complete strict eligibility boundary, so it is not imported. Same-query capture and exact source attestation are preserved; table lookup is explicitly removed for this narrower ingredient.

## Failure Modes — What Breaks When Capture Silently Changes Membership

Missing/filtered/orphan rows; raw JSON evidence erased; unsafe grant/archive boundary; accidental report-table dependency; local-D1 binding failure; resource breach or stale base; extra path/real data/provider access; ingredient mistaken for report delivery.

## Required Response When Detected

Refuse incomplete/invalid captures and retain tests; preserve raw strings and use strict A; stop on boundary failures; use reviewed table-free target only; report exact failing bind/statement; return measured AMEND rather than relaxing limits; stop scope violation and notify coordinator; label local ingredient only. Root must separately authorize continuation, integration or provider action.
