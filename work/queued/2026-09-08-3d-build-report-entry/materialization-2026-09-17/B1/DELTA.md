# B1 driver-seat delta

Future integrator needs a complete authorized capture, not another approximate source label. B1 removes the unused report-table lookup so it actually runs against current a57 schema, while retaining every membership/grant/integrity predicate. It measures actual local-D1/strict-A behavior before the shared store writer proceeds. Account CPU entitlement remains unknown; only local authoring is released, not platform acceptance. Rejected an empty report table, replay0005, ordinary parsing of raw inputs and whole old engine import. Reversibility forward/backward low: local unwired ingredient, no remote data effects.

| Six-B step | Verdict | Evidence |
|---|---|---|
| Borrow | applied | Current a57 schema, accepted A3523 and independently tested complete SQL capture. |
| Bend | applied | Table-free aid-only target for isolated B1, same five bindings. |
| Break | observed | Common report-id target references absent table; old0005adapter cannot be silently imported. |
| Beget | delegated | Actual a8 author ACK30-minute iteration, independent tester reviews design and candidate separately. |
| Bide | rejected | Waiting for account billing proof does not improve local capture authoring; release proof remains required. |
| Build | minimal | Three paths only, strict adapter and relevant tests/docs, no new parser/query engine or public face. |

Parent GATE-RECEIPTS carries actual preflight06:48:03 and challenge06:48:14, generic questions disposition and disconfirmers. Confidence is bounded by actual34/425 SQLite-to-A probes; local D1 execution remains candidate work. This is no universal consistency or timing claim. Costs are actual author budget and distinct later integration/review, not a deadline promise.
