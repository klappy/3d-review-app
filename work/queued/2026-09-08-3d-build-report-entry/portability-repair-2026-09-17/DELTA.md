# Driver-seat delta — reporting portability repair

Planning seat release_recovery_coordinator read the original plan, observed failure/metadata cargo, exact pinned generator/test, product AGENTS and release procedure, then applied the live driver-seat prompt to this revision before challenge.

The system is one recovery chain: pinned offline source reader -> existing exact artifact/oracle tests -> required protected PR checks -> Git build -> independently observed deployment -> separately fired Auth acceptance. Each return identifies its actual stage; lower-level green never advances a higher-stage claim.

Changed: explicit single author and distinct reviewer with actual availability/budgets, so FIRE does not borrow a departed author's promise. Changed: replace unworkable module spy with a disposable missing-sqlite module and actual failing control; enforce PYTHONPATH restoration and original-reader RED. Changed: done-means binds exact artifact identity and runtime gap separately, preventing local success from releasing Auth early. Changed: written two-path boundary and no-new-skips disposition preserve tests when matching the failure.

Considered and rejected: provider Python installation/config adjustment expands remote authority; handwritten SQL decoder adds source-fidelity risk; Miniflare lifecycle is unnecessary for nine local rows while built-in capability passes; spawning implementation before root FIRE violates the handoff. These decisions amend planning documents only; no code was changed.
