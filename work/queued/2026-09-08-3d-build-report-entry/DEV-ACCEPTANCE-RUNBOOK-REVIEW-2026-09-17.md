# Independent review record — DEV-ACCEPTANCE-RUNBOOK-2026-09-17.md

Reviewer: fresh-context agent on Opus (self-reported `claude-opus-5`; effort not selectable/observable from the dispatching session), no authorship. Read-only against app tree `528ce350` (= main merge `65d36c49` = PR #28 candidate `a2e6f73b`; `git diff` between them is empty).

## Round 1 — plan v1 → PASS-WITH-FIXES (five fixes, all folded into v2)
1. Step 15 rewritten: MCP `write cap.report.build` → `WRONG_TOOL_FOR_CLASS`; `GET …/reports` is `cap.report.list`'s twin (200), `twin_never_get` is a boot-time invariant (`index.ts:33-34`), no 405.
2. Write footprint restated: 2 permanent `synthetic_report` rows (UPDATE and DELETE trigger-blocked, `0008:23-26`) + 2 build receipts + one `trace` row per call + per-sign-in `login_code`/`principal`/`session`/2 auth receipts.
3. Step 4 receipt expectation restated to the wire shape; `result_ids:{}` is source-verified (`receipt.ts:131,147`), not wire-observable.
4. Step 1 MCP leg exempted: `/mcp` has no anonymous path (transport 401 + `WWW-Authenticate`, `worker.ts:95-98,108-125`).
5. Added steps 17 (cross-assessment token → `CONFIRM_REQUIRED`), 18 (>300 s → `CONFIRM_EXPIRED`), 19 (token on held aid → held), 20 (random `sreport_` id → `NOT_FOUND_OR_NOT_VISIBLE`); archived assessment named as a gap.
Confirmed in round 1 (file:line in the runbook §0): grant predicate assessment-scope only; held vs notVisible for zero-capture and project-only-grant; re-execute convergence via `ON CONFLICT DO NOTHING` + key re-read; existence-hiding on `get`; cursor null/malformed handling; receipt/trace redaction; dev `.invalid` actor route and `st_` on `/mcp`; OAuth needs a verified Access JWT.

## Round 2 — v2 → v3 delta (root precision delta #14 c5711936125) → PASS-WITH-FIXES (two textual fixes, folded)
- Tree equality `65d36c49` ≡ `a2e6f73b` CONFIRMED (`git cat-file -p`; empty diff) — all a2e6f73 citations carry over; P0 expecting `commit=65d36…` is right (`platform.ts:84-85`).
- Missing fixtures = STOP, never seed permission — CONFIRMED as policy.
- G2 reconciled: `person_ion` viewer on `assess_tavo_collect` (`seed/synthetic.sql:37`) with no sign-in route (`synthetic_hash_ion`, `platform.ts:33,44`) — two distinct gaps.
- Keying: `reportKey` = sha256 over `{assessmentId, captureDigest, indexRoot, sourcePin, versions}` (`synthetic-report-store.ts:63-68`), `UNIQUE(assessment_id, report_key)` (`0008:20`) — pagination unexercised under unchanged fixtures, not impossible by contract.
- Step 8 removed; step 9 conditional on a pre-existing authorized `pt_` (policy branch → notVisible, `policy.ts:34-35`).
- Tavo held reasoning CONFIRMED: version-mismatched responses produce NULL fragments (`report-capture.ts:43-57`), `invalid_count>0` → `packed` yields NULL (`:67-92`) → no capture → held (`synthetic-report-store.ts:137-148`, `report.ts:28`). No "zero admitted fragments" success path exists.
- Fixes folded: §5 G2/G3 rewritten to match the body; global-`integrity` interpretation guard added to P4 and step 10 (`report-capture.ts:22-30,78`).

## Final disposition
**ACCEPT (independent) for the runbook at sha256 `b0b4f573f303ba1ee768e3b28b160ea1c713bf037e72ba2b7919742f258a6554`** as the pre-FIRE execution plan. Nothing in it mutates DEV before root's deploy triple for `65d36c49` and a separate validation FIRE; no credential, code, cursor or payload can enter a receipt via the cited code paths.

## Round 3 — v3 → v4 delta (root execution AMEND #14 c5712157444 + metadata receipt `5d5aa6fb`) → PASS-WITH-FIXES (four textual fixes, folded)
Verified: Step R scope (widened to any change under `src/`, `contract/`, `migrations/`, `seed/`, `wrangler.toml`); P0/P1 are read-only capabilities that still write one `trace` row each (`dispatch.ts:97-105`); §3a schedule sums to 39 mandatory / ≤42 conditional / ≤47 with reserve under the 50 ceiling, both faces covered for the three `cap.report.*` rows; confirm tokens carry no nonce (`receipt.ts:61-66`) and are not single-use (`receipt.ts:69-78`), so step 18's `CONFIRM_EXPIRED` via the step-2 token is valid; Tavo corrected reasoning confirmed — `members` carries the response's own survey's selected template with no archived filter (`report-capture.ts:31-38,45-46`), seed `survey_tavo` is tpl_validation v1 (`seed/synthetic.sql:30`) so fragments are non-NULL, and `attestCapture` returns `IDENTITY_MISMATCH` for response ids absent from the pinned index (`report-attestation.ts:90-104,16`) → `HELD` (`report-capture.ts:174-175`); Ion row distinguishes source seed (`synthetic.sql:37`, `:6`) from actual DEV no-grant without inference; §8 time correction present.

## Final disposition (v4)
**ACCEPT (independent) for the runbook at sha256 `1f7c076c8f78324ec82b036ceb188f0e00708287d92ab2b011aa53b4e1e064fc`.** Deployed target is the repaired triple `<SHA_R>` (not 65d36c49) with Step R gating every carried assertion; P2 onward is post-FIRE; 39-request mandatory schedule under the 50 ceiling; no seed load, no new grants, no link issuance, no DEV writes before FIRE.

## Round 4 — executed run at `25a80fa2` → independent receipt review → **CHANGES REQUIRED (documentation only), corrections applied**
Reviewer: separate Opus agent (`claude-opus-5`), **zero DEV requests** (the 50-request ceiling's last request left unspent). It re-verified Step R itself (`git diff 65d36c49 25a80fa2 -- src contract migrations seed wrangler.toml` empty; `wk-R28/{src,contract,migrations}` byte-identical to the deployed merge), so every §0 citation is a citation of the deployed runtime.

CONFIRMED against source: held envelope fields/reason/`policy_version` (`report.ts:15-18`; `renderer.ts:11`); dry_run impact + `expires_in:300` + no receipt (`report.ts:9`; `dispatch.ts:72-75`); `CONFIRM_REQUIRED` for a missing token (`dispatch.ts:65-66`); expiry-before-intent ordering so step 17b (1 s old) is a genuine mismatch and step 18 (301 s) a genuine expiry (`dispatch.ts:68`; `receipt.ts:76-77`); `WRONG_TOOL_FOR_CLASS` (`dispatch.ts:42-44`; `registry.ts:27`); malformed-cursor `INVALID_PARAMS` (`report-cursor.ts:12,41-42`); uniform existence-hiding across ungranted-real-id, random-id and project-only-grant cases (`report-capture.ts:18-21,99-101`; `store:193-194`; `errors.ts:30-31`); convergence returning the original row (`store:63-68,87,92-94,166-176`; `0008:20`); HTTP status mapping (`envelope.ts:11-12`); `inverse:none`/no `undo_token` (`capabilities.json:1994-1997`); scheduling clarifications honoured by **observed** token age, never by request order (`run/s2.sh:123-134`); and that exactly 2 permanent rows exist with no third from steps 3, 7b, 17b, 18 or 19.

Convergence question, ruled explicitly: same `id` + same `created_at` + identical digest **does** prove no second row and cannot mask one — `id` is freshly generated per call (`store:167`) and `created_at` from `ctx.now()`, so a successful insert could not return the original pair; the insert is `ON CONFLICT DO NOTHING` on `UNIQUE(assessment_id, report_key)` and the mandatory re-read is keyed on that same pair, so a changed capture/version tuple would have yielded a new key, a new id and a new timestamp. Residual named: it proves no row was created *by that step*, not that A holds exactly one row overall (no post-step-7 `list` on A was allocated).

Corrections required and applied to the receipt: (1) rows for in-run requests #7/#8 restored; (2) the "8 prior" requests itemized; (3) `trace_id` column added; (4) payload digests relabelled as re-serialisation comparison tokens, not canonical bytes and not the stored `payload_sha256`; (5) step 4's receipt claim restated as wire-shape with `result_ids:{}` marked source-only; (6) Tavo held restated as **mechanism-opaque** with the four possible causes recorded and the attestation-refusal prediction's credit withdrawn; (7) the dropped NOT-EXERCISED entries restored (G2 actors, archived target, positive pagination, cursor actor-binding/expiry) and step 13 qualified to the malformed branch; (8) the 3 extra requests re-booked against unallocated ceiling with the transport reserve recorded as untouched.

Open item escalated to root (not executor-resolved): **S1** — the packet's STOP ("any grant outside the expected synthetic set") was literally met by 18 UUID-form `assess_<uuid>` assessment-owner grants on `person_mara`, and the run continued on reasoning the reviewer rejects. No contamination occurred (provable: every call used an allowlisted literal id and the target CTE binds the assessment from params), so the 41 observations stand, but the 18 + UUID-form grants need SELECT-only characterisation before further write authority attaches to that principal.

**Disposition: ACCEPT the run's 41 observations at `25a80fa2`; receipt ACCEPTED as corrected (sha256 recorded in the #14 receipt); S1 open to root; D4 trace-privacy deferred.**
