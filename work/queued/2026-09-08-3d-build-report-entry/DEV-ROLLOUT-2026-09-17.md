# Reporting Git → DEV sequence — proposed, no migration or rollout FIRE

Coordinator: release_reconciliation. Root owns final source freeze, protected merges, provider actions and acceptance disposition. This bounded plan reuses the existing report-entry/18-B and controlled A8 additive-migration method; it creates no new environment, deploy path or proxy-measurement project.

## Observed inputs and limits

- Published draft appPR28 head68f795cb0e4470d5bf8785b7488fd31e748f8273/tree15c5d8b63c1f030e9e16369d163a1582f59a1fac, accepted C2b plus exact main299f825 docs changes. Bugbot105126094066 SUCCESS08:15:07Z. Later sibling integration is not included by that check.
- Paired 18-B docs123ce96a774eb9d474ce53caff98d880f688d453 published on existing cookbookPR12. Independent semantic acceptanceba20b91e, not wholePR12 acceptance; normal checks requested.
- Fresh Git main299f8255526e13b17215ea1af44e3600d4b35dc2, production895339fb5a289b357148e7050447125791e6a411. Current main ruleset23571595 active: strict required Cursor Bugbot app1210556, PR required, resolved conversations, no bypass/force/deletion; zero account approval count does not replace independent review. Literal SUCCESS remains the stronger manual gate.
- Fresh read-only provider triggerb82be56e maps main→3d-review-dev, build `npm ci && npm run typecheck && npm test`, deploy `npx wrangler deploy --config wrangler.toml`. There is no migration command. Active DEV version485ae432-1d2e-497b-8cde-363a9ecd74b6 at100%, deploymentba06f906-85fc-4ef4-919a-62f32b872457. Provider readback adjacent; mapping/source association to299f825 is also in root's existing deployment receipt, not re-established by this deployment-list call alone.
- Actual DEV DB binding is5d4cc260-a7b1-47cc-b03d-ed4f60d324c3 in accountb03e6ea242724c05eb97eb732cceb21d. Read-only schema query returned existing oauth_code_redemption/shared_response_claim and no synthetic_report, its named index/triggers or d1_migrations. HTTP200, changed_db:false, rows_written:0. This proves queried objects absent/present now, not all historical migration execution.
- Exact migration0008 SHA2562ad9a48901204c4f1a162b99bbe3ba998d9c317c8ded72a6c6e859e87419365a. It creates one new table, one named index and two immutability triggers; SQLite also creates its key/unique indexes. No0005, seeds, initial migrations or ledger fabrication. It changes no existing row/table definition. Final candidate must retain identical bytes or receive new review.
- Root source config and0006/0007 are unchanged between299f825 and68f795cb. Deployed-base source has no synthetic_report references/report handlers, so adding an empty independent table does not require new runtime first. This is source-level compatibility, not a claim every later populated-table recovery is already tested.
- Actual settings show usage_model:standard and compatibility2026-09-01; limits/observability are absent. This is not paid entitlement/effective CPU or peak-memory proof. Root's prior subscription read was unauthorized; this plan does not repeat it. No transport limit is inferred from JSON size.

## Proposed sequence and exact authority boundaries

1. **Freeze a reviewable integration, not every sibling indefinitely.** Root chooses the current report candidate plus any actually required merged sibling security/config changes. Metadata/recovery/feedback/FAQ do not become reporting dependencies solely by being open PRs. Inspect actual shared-file overlaps (receipt, registry/schema, policy, handler registry), preserve all accepted sibling hunks, run proportional combined checks, get independent exact-delta acceptance and literal exact-head Bugbot SUCCESS with terminal checks/resolved findings. Record app head/tree,0008 hash, binding/config hash and paired18-B commit. Existing root whole-goal decisions remain; this is a prospective DEV validation release, not production or full-app acceptance.

2. **Separately authorize additive schema execution.** Existing Auth reservation14c5710224924 gives0008 to the report coordinator and excludes0005; obtain fresh no-competing-schema-writer readback for the execution window. Root names an actual available executor, its own budget/checkpoint and exact provider write FIRE after independent review of this sequence. No executor is invented by this plan. Applying0008 can precede final UI readiness if the independently accepted SQL/config remain exact, current runtime compatibility is verified and root explicitly records that narrowed source gate. Do not silently reuse the older0006/0007 FIRE.

3. **Preflight the named DEV database and preserve data.** Before the schema window and again before the merge window, root/executor inventory the actual DEV build/deploy queue, drain or explicitly reconcile older jobs under their owners, and record that no stale job can change the active source during the controlled step. Do not cancel blindly. Re-read active source/version/binding after drain; an unexpected deployment or new queue entry stops the affected window and requires fresh compatibility/custody readback. Executor reads active source/version and live DB binding again, confirms schema objects absent, captures table/index/FK definitions for assessment/principal and existing collection/auth tables, exact current counts and foreign_key_check, and a current Time Travel bookmark. Coordinate a short schema/write-quiescence window with active DEV test/staff owners; their ACK is not a claim public ingress is technically blocked. Any observed writes invalidate a strict unchanged-count claim and require reconciliation before completion. Wrong binding, drift, existing mismatched objects, FK failures or concurrent schema writer stops execution. Do not dump answers, tokens or participant data.

4. **Apply exact0008 once through the existing provider query endpoint.** After named FIRE, POST `/accounts/b03e6ea242724c05eb97eb732cceb21d/d1/database/5d4cc260-a7b1-47cc-b03d-ed4f60d324c3/query` with body.sql equal to the hash-checked file bytes. No `migrations apply`, additional BEGIN/COMMIT, IF-NOT-EXISTS rewrite, seed or trigger-bypass. The exposed API supports semicolon-separated SQL batch, but this plan does not assume all-or-nothing remote-file behavior. Require HTTP/top-level/every-result success. Immediately read exact table constraints/FKs/columns, named index, unique key coverage and both trigger bodies, empty new-table count, unchanged existing counts under the recorded window and unchanged/clear FK check. Save actual result/readback and source/hash receipts. Any error or transport uncertainty stops runtime promotion; inspect exact partial schema, do not blind retry/drop/replay. Root commissions only missing forward DDL after fresh independent review if needed. A bookmark is not authorization to restore or lose later data.

5. **Deploy only through protected main Git hooks.** Once schema and candidate gates close, root gives a candidate-specific prospective DEV disposition and normally merges the exact reviewed head into current main with expected-head guard. Record merge parents/tree. Existing trigger builds/tests/deploys; no manual wrangler, API upload/version switch or trigger-command edit. Observe resulting merge SHA→push-event build→successful deployment→active Worker version/source. Refresh queues so older pending builds cannot later replace the candidate; do not cancel blindly. Verify production branch/trigger/version unchanged as a separate actual readback. A merge or health response alone is not a deployment receipt.

6. **Validate the actual deployed report path before accepting it.** Named Auth/Auditor owner obtains legitimate actual staff and delegated OAuth credentials through existing mechanisms. On retained source-approved synthetic assessments: preview causes no report/receipt; execute explicit confirmation builds one immutable report; same-capture retry converges; HTTP and genuinely delegated OAuth MCP get/list agree semantically; viewer/support exact grants and non-granted/participant refusal hold; held current inputs disclose no hidden fields. Choose bounded known fixtures and record their changes/receipts. The immutable report rows are retained; no cleanup DELETE/trigger bypass is authorized. Do not recreate synthetic source identities or mutate shared seed answers to manufacture a worst case without separately reviewed data custody.

7. **Make the resource decision from actual runtime evidence.** Root establishes the applicable Worker CPU/memory/transport semantics and available observation mechanism, then records actual candidate outcomes/metrics with cold/warm and report/page workload labels. Successful requests alone do not prove CPU/peak-memory bounds. Use the known resource cases to bound what the evidence covers: normal writer with expanded capture produced1,532,919 serialized bytes locally; separately padded read-admitted payload2,571,891; malformed six-row page13,125,577. The latter two came from disposable fixture manipulation, not ordinary production writes. Do not bypass immutability or modify real/shared data in DEV merely to run them. If exact worst-case/runtime proof needs another fixture or instrumentation, root chooses a bounded reviewed way to obtain it or a reviewed conservative admission/projection disposition; retain the unproved gate meanwhile. No report-specific limiter exists; root must expressly disposition that security/capacity risk, not silently infer safe throughput from the existing MCP ceiling. No generic local proxy harness is fired here.

8. **Close acceptance and recover through Git if needed.** Root combines actual runtime/API/OAuth receipts with Design's actual report UI/browser scope (still unassigned/not implemented by C2b), paired cookbook/source/version record and any remaining critical findings. A validated API slice may be reported as such; it is not full reporting journey or full-app acceptance. Publish a frozen release manifest only at its actual accepted scope. Production is excluded from this order.

## Recovery and compatibility

Prefer keeping a secure accepted shared-aware runtime serving while a reviewed forward fix moves through protected main. Preserve0008 and all report/collection rows. Never revert to pre-shared-link10f5 or erase later Auth/feedback/security fixes. The old299f825 report handlers are absent/reserved and cannot disclose synthetic_report; it is a source reference for a reviewed report-disabled recovery, not permission to reset main. If a report-specific defect requires disabling these operations, prepare an ordinary reviewed Git candidate preserving current collection/Auth/security/config, route the three operations back to non-disclosing refusal, and deploy through the same hooks. Exact diff/checks and behavior proof are required; no such candidate is prebuilt or claimed ready here.

After reports exist, their FKs/immutable triggers and retention remain. Do not drop the table or disable triggers to make legacy deletes succeed. Existing assessment deletion is restricted to empty surveys/responses; any new lifecycle interaction observed must be reviewed rather than bypassed. A secure current runtime can remain during forward repair; no prebuilt second version or new isolation architecture is automatically required. An active authorization/corrupting-write incident still needs separately proven containment under root authority, following the existing Git-only recovery procedure; this plan invents no tested kill switch.

## Actual remaining dependencies

Independent review of this rollout plan; final integration pin/paired docs check; fresh Auth schema custody/quiescence; actual executor ACK and root0008 FIRE; fresh preflight/bookmark; exact Git merge/build/version receipts; actual delegated OAuth report journey; applicable runtime evidence and explicit no-limiter disposition; Design report UI scope/acceptance. No generic extra human permission is required by this plan. Root owns precise disposition of these real gates. Planning and read-only provider observations above do not authorize execution.
