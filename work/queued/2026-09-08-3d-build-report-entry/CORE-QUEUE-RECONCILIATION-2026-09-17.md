# Core queue reconciliation — original Meal A + Sept-8 build tickets + open app PRs

Owner: Auth seat (cos door), session `claude-opus-5`. START 2026-09-17T11:04:09Z (observed). **Bounded read-only owner assessment.** No ticket moved, closed, reopened or edited; no rail file rewritten; no PR touched; no new plan, no product work, no development fire. No content reads, no new provider queries (S1 is closed at #14 c5713221603). App-request budget untouched: 49/50, 2 rows, 1 unspent.

Read: ticket headers + done-means lines only, CHECKLIST-RUN verdict lines, open PR heads/bases, and `git` positioning of branches. Not the encyclopedia.

Evidence anchors: app main **`6f20dba56634958c3b31dde09a3ded80954c126c`**; DEV last verified at `25a80fa2` (acceptance receipt, kitchen `dfe55af7`); kitchen main at read time `ba6b707b`.

## Legend

`DONE+PROOF` = shipped and independently evidenced, proof cited · `ACTIVE(owner)` = in flight, owner named · `BLOCKED(reason)` = cannot proceed without a named input · `SUPERSEDED-BY(item)` = exact successor named · `CAPTAIN-DEFERRED(ruling)` = deferred by a cited ruling · `NOT-VERIFIABLE-HERE(owner)` = acceptance is another seat's to assert. **Code present ≠ accepted** is applied throughout.

## Meal A — A0…A8

| Item | What it was | Status | Proof / owner / residue |
|---|---|---|---|
| **A0** audit-share | Astra answers the 12 §per-lane questions for Lane A: which 04 rows cannot be generated from 04 alone | **NOT-VERIFIABLE-HERE (Astra/root)** | Planning dish; `CHECKLIST-RUN.md` present with header gate ✅. Acceptance is root's to assert, not mine. No claim of done. |
| **A1** contract-freeze | Freeze `contract-v0.1` bundle — `capabilities.json`, `openapi.yaml`, `contract-manifest.json` generated from 04, at tracked paths | **DONE+PROOF (artifact side); ACTIVE(Astra) for the cookbook freeze record** | All three tracked paths exist on app main (`contract/{capabilities.json,openapi.yaml,contract-manifest.json}`), 84 rows, `contract-v0.1` served live (verified on DEV: `contract: contract-v0.1`, 83 rows served vs 84 in file — see residue R1). The ticket's own escape hatch ("artifacts only in a seat's workspace → not frozen; land on PR #18") is **still open**: cookbook PR **#18 is open/DRAFT**. So the app-side bundle is frozen at tracked paths; the cookbook freeze record is not landed. |
| **A2** schema | D1 schema for the fresh app (identity, curated links, versioned instruments, scope/grant/invitation/access_code/receipt/trace) | **DONE+PROOF** | `migrations/0001–0004, 0006, 0007, 0008` on main; `0007_shared_link_context` and `0008_synthetic_report` both executed and verified on DEV (0008 at 08:52:09Z, root receipt; 0007 exercised live in the shared-link acceptance). Note `0005` is reserved by an unmerged branch and applied nowhere — recorded, not a defect. |
| **A3** auth | Sign-in per OF-7 — magic link as written vs Cloudflare email-code with shared identity and separate browser sessions; **both prepared, neither cooked** at ticket time | **DONE+PROOF (shipped), with live security residuals R2/R3** | Email-code route live (`.invalid`-gated, fail-closed off `ENVIRONMENT="dev"`); Cloudflare Access route live; OAuth 2.1/PKCE live (PR #15/#20 chain merged, Bugbot SUCCESS). Verified live: 401 + `WWW-Authenticate` on `/mcp`, `st_` bearer on both faces, `NOT_AUTHENTICATED` envelope on HTTP (acceptance steps 1–3, 9–10). **Residuals preserved below — do NOT read A3 as closed.** |
| **A4** policy | Server-side grant check per (scope, role); no inheritance; unauthorised = 404 that hides existence | **DONE+PROOF** | `src/policy.ts` live; existence-hiding verified live and uniformly across three distinct causes — project-only grant, ungranted signed-in actor on a *real* report id, and a random id all returned identical `NOT_FOUND_OR_NOT_VISIBLE` (acceptance steps 20–21, 24–26, 38). Assessment-scope-only gating confirmed (report capture `target` CTE). |
| **A5** handlers | One handler per 04 row; R/W/E/D enforced by effect; receipts; undo only where `inverse:` declared; E+D two-step; suppression | **DONE+PROOF for the v2.0-bcs surface; PARTIAL BY DESIGN** | 84 contract rows: **79 `v2.0-bcs` / 5 `v2.1-oct`**; statuses **76 target / 3 implemented-synthetic-only / 5 RESERVED_NOT_BUILT**. Two-step danger, receipts, `inverse:none`/no-undo-token, and typed suppression all verified live (acceptance steps 11–14, 30, 35). The 5 `RESERVED_NOT_BUILT` rows are the declared v2.1-oct shape-only rows, not a gap. The 3 `implemented-synthetic-only` rows are the report trio — see R4. |
| **A6** mcp-wrapper | Four MCP tools (docs/read/write/danger) over the same handlers, official SDK, stateless | **DONE+PROOF** | Both faces exercised on every report capability with identical envelopes modulo transport; `WRONG_TOOL_FOR_CLASS` enforced on a class/tool mismatch (acceptance steps 12, 16, 19, 21, 25, 32–33, 35). Anonymous `/mcp` correctly has no envelope path (transport 401). |
| **A7** telemetry-xray | Analytics Engine events, a span log per request, and `cap.ops.trace` returning a request's trace | **ACTIVE(Auth) — one gate UNPROVEN on DEV (R5)** | `cap.ops.trace` present and live: owner reading their own trace returned HTTP 200 (acceptance step 28). **But the span content was never captured** (harness `jq` fault), so redaction, absence of answers/payloads/codes/cursors in persisted spans, and the foreign-trace negative are **unproven on the deployed runtime** — local suite only. Needs 2 requests; deferred under the preserved ceiling. |
| **A8** deploy | Live URL on `*.klappy.workers.dev` (OF-2), deployed by merge-to-main via Workers Builds, no custom deploy scripts | **DONE+PROOF for DEV; ACTIVE(root/Otto) for topological release completion (R6)** | Merge-to-main → Workers Builds verified end to end twice today: build `35f43c77` → `commit 25a80fa2` served live, and the earlier `abc78270` failure (Python `_sqlite3`) was repaired via PR #37 rather than any custom deploy script. Release identity (version/build/commit/build_uuid/release_source) live on `/v2/health`. **Production topology and the A8 freeze are not closed** — app PR **#22 (topology doc) is open/DRAFT**, and `v0.1.0` tag + cookbook C2 remain root's. |

## Sept-8 build tickets

All three were ordered under the captain's 2026-09-08 "Let's do it all" authorization and each still carries its original header **`Status: HOLD — proposed implementation order; not ready to cook until repository binding and listed decision gates are satisfied`**. That HOLD has never been lifted in the ticket text. None is stale-closed; none should be silently moved.

| Item | Status | Proof / owner / note |
|---|---|---|
| **scope-contract** — bind scope and data contract | **SUPERSEDED-BY(A1 + cookbook `plan/2026-09-16-parity-build-preplan` §04 matrix)** — ticket HOLD never lifted | The scope/data contract was actually bound through the 04 capability matrix and the frozen `contract-v0.1` bundle (A1), not through this ticket. Successor is exact and live; the Sept-8 ticket text remains HOLD and unedited. Open contract work now runs as cookbook PR **#36** (`api/shared-link-contract`, API pen — Auth's rows 21/79/83 ACCEPTed, two reconciliations owed) and PR **#34** (04-ACCEPTANCE rows 34–50). |
| **report-entry** — implement coverage and report entry | **ACTIVE(root + Design), substantially DONE+PROOF on the API side** | Its six original done-means are reproducibility/forgery/collision/perf/negative-control gates on the 425-tuple artifact — those are the attestation work, delivered and independently reviewed (PR #28 → merged; DEV-executed 0008; two immutable reports built and verified live). **This rail folder is now the evidence home** for the whole reporting slice: `DEV-ACCEPTANCE-RECEIPT-25a80fa2.md`, `DEV-ACCEPTANCE-RUNBOOK-2026-09-17.md` (+ review), `DEV-METADATA-PREFLIGHT-2026-09-17.md`, `S1-GRANT-CHARACTERIZATION-2026-09-17.md`, `API-DESIGN-HANDOFF-2026-09-17.md`, `portability-repair-2026-09-17`. Remaining: UI/report path under Design PR **#41**/**#43** (independent review), and R4/R5 residuals. |
| **response-recovery** — implement response collection and recovery | **DONE+PROOF (collection + recovery), ticket HOLD never lifted** | Shared-link collection, per-open independent respondents, draft restore, duplicate/idempotency, closed/revoked copy and receipt-after-closure were all proven in real Chromium and accepted on DEV (project docs `2026-09-17-3d-dev-acceptance.md`, `-final-proof-dafefe1.md`, `-browser-proof-76a8539.md`; root accepted the first increment). The rail ticket was never updated to reflect it — **stale ticket text, real delivery**. Flagging, not moving. |

## Open app PRs — auth / mail / seed

**Structural finding:** `phase-0/walking-skeleton` is frozen at **`10f5f444`** (last commit 2026-09-16 20:50Z). App main is **108 commits ahead of it**, and **0 commits of phase-0 are in main** — the entire phase-0 stack was bypassed by the parity rebuild, not merged. Four open PRs still target that dead base, and their heads **predate shared-link entirely** (no `0007_shared_link_context.sql`, no `src/handlers/shared-link.ts`). This is real work stranded on a stale base. **Code present ≠ accepted. Do not silently close or retarget — each needs an explicit owner ruling.**

| PR | What | Status | Note |
|---|---|---|---|
| **#16** `fable/mail-sender` → phase-0 | OF-3 Resend sender adapter wired into `cap.grant.invite`; honest `delivered:false` until configured | **BLOCKED(mail provider not configured by captain) + stranded base** | **MAIL PROVIDER BLOCKER PRESERVED.** Sends only when `ENVIRONMENT === "production"` **and** `RESEND_API_KEY` + `MAIL_FROM` exist; otherwise honest `delivered:false`. This is a captain-configuration blocker, not a code gap. Head `6150483e` predates shared-link; base is dead. Needs: captain's provider decision, then a retarget ruling. |
| **#14** `fable/seed-coverage` → phase-0 | Source-backed Audio / Video-Sign / Community-Pastor synthetic seed coverage | **BLOCKED(stale base) — source-fidelity item, preserve** | Head `436f9456`, mergeable against the dead base only. **Source fidelity / forms coverage is an original data item, not superseded by the parity seed** — the 34-persona `synthetic-responses.sql` covers different ground. Needs an owner ruling on retarget vs supersede; I assert neither. |
| **#17** `fable/phase-a-sweep` → phase-0 | Phase-A refusal sweep and partial acceptance coverage | **BLOCKED(stale base) — persona/auth refusal coverage, preserve** | Head `cc88f369`. Original refusal-sweep coverage; overlaps but is not identical to the live acceptance suite. Same retarget ruling needed. |
| **#22** `audit/a8-main-dev-inert` → phase-0 | Document main DEV and protected production topology | **ACTIVE(Otto/root) — A8 topological completion (R6)** | DRAFT, head `78d00915`, on the dead base. Directly carries the **topological release completion** item. Should be retargeted or reissued against main by its owner, not by me. |
| **#35** `docs/journeys-j11-shared-link` → main | J11 fixture corrected to shipped shared-link semantics | **DONE+PROOF, awaiting root merge** | Head `cf177578`, Bugbot SUCCESS `105131388374`, independent review APPROVE-WITH-NITS → folded. Mine; root's to merge. |
| **#38** `cursor/fix-b2b3-runner-repair-3d17` → main | B2/B3 runner repair so false-greens fail | **ACTIVE(Auditor)** | Auditor's plan (`5712024889`) and project doc `2026-09-17-3d-b2b3-runner-repair-plan.md`. Not mine. |
| **#41 / #42 / #43** | Glass / public entry / report path; design screens; ReportCard three-lens | **ACTIVE(Design)** | PR #41 `3e9e38a` under independent review per root; #43 builds atop it. Not mine to disposition — **not stopped by this reconciliation.** |

Cookbook open PRs (15) are Astra's/Design's/API pen's queue and are listed for the inventory, not dispositioned here: **#12, #18, #21, #23, #24, #25, #28, #30, #31, #32, #34, #35, #36, #37, #42**. Auth's own touchpoints in that set are **#36** (rows ACCEPTed, two reconciliations owed to the API pen) and **#31** (18-D/18-I paired revision, ACCEPT chain complete).

## Residuals explicitly preserved (none of these is closed or deferred by this reconciliation)

| # | Residual | Owner |
|---|---|---|
| **R1** | Contract count skew: 84 rows in `contract/capabilities.json` on main vs **83** served by `/v2/capabilities.json` on the deployed runtime. Recorded, unexplained, not chased — could be a generated-vs-tracked drift or an expected filter. Worth one look by the contract pen. | API/contract pen |
| **R2** | `GET /v2/auth/access` mints a first-class `user`/`support` session for **any** Cloudflare-Access-verified email — **no `.invalid` test, no environment gate** — and Access is configured for dev. A real-world identity can hold a session on the shared synthetic DEV database. | Auth + captain (policy) |
| **R3** | `POST /v2/ops/seed/synthetic` is a non-capability, un-receipted write route executing the seed for **any** signed-in principal — no `provisioned` check, no owner check, outside parity. | Auth + root |
| **R4** | Report limitations: the trio is `implemented-synthetic-only`; real-data reporting remains held; for the 4 UUID-form assessments now holding 1–2 responses the report closure is **single-control** (attestation index membership). Cursor actor-binding, expiry and positive pagination unexercised on DEV; viewer/member/support/participant/OAuth actors and archived targets unexercised. | Auth (evidence), root (policy) |
| **R5** | Trace-span redaction **unproven on the deployed runtime** (A7) — plus the foreign-trace negative never run. Local suite only. Needs 2 requests. | Auth |
| **R6** | Topological release completion: production topology doc (#22) open on a dead base; `v0.1.0` tag + cookbook C2 outstanding; A8 freeze still in force. | root / Otto |
| **R7** | Mail provider not configured (OF-3) — `delivered:false` is honest, not a defect. Original OF-3 item stays open. | captain |
| **R8** | Source fidelity / forms coverage (#14) and persona refusal sweep (#17) stranded on the dead phase-0 base; neither superseded by the parity rebuild. | root (retarget ruling) |
| **R9** | S1 provenance: the 18 UUID-form assessments' operator identity is **not established**; assessment free text (`name`, `purpose`, `notes_*`) is readable by a signed-in mara; project/workspace UUID-form grant presence established (≥27 / ≥8) but unenumerated; DEV is not quiescent (40→39 project grants implies a concurrent delete). | root (decisions listed at #14 c5713221603) |

## Next phase — explicitly the captain's ruling, not a blanket defer

**Recommendations, rollups and reconstruction are next-phase only** per the captain's ruling. That deferral is scoped to those three and does **not** defer any original auth or data item: A3's residuals R2/R3, A2/A7 data-and-telemetry items, OF-3 mail (R7), source fidelity (R8) and report limitations (R4) all remain **open original items in this queue**, not next-phase work.

## What this assessment is not

It moves nothing. Every "stale ticket text, real delivery" and every "stranded on a dead base" line above is a flag for the owning seat, not an action taken. CoS root reconciles all meals; Design, persona and runner work continue uninterrupted.
