# Independent K3b1 plan review — 2026-09-22

**ACCEPT planning specification at kitchen064de71821d6a2f95c2721c496fd1bc172f9adec, TICKET1.0.1 and FIRE-CHECK-RUN. NOT implementation-ready, not SOURCE FIRE.**

The22 exact paths are bounded to original159 reconciliation and four regression/harness files. Shared assess.js/feedback custody begins only after independently accepted K3a exacttree handoff and actual Fable dish ACK/promise. No competing writer or internal cooking. Existing three159 commits remain ancestry, current161/K3a functional and active metadata identity survive, historical0.15.0 candidate is not promoted by implication. One final design release remains separate.

Real feedback controller/API semantics remain authority. Explicit allowed context, loaded-client release versus server-authored provenance, null historical provenance, no account/input/token capture, support authorization and identical HTTP/MCP validation are specified. Late dispatched writes may commit; only stale UI/receipt/reopen/resend is suppressed. Success, rejection and uncertainty are distinct with no blind retry. Native dialog focus must stay inside in both directions; BODY fails without being misreported as background activation. Actual browser focus/retention and opener return are required, preserving unsaved nodes/listeners/account/changelog.

The test plan is executable at the accepted handoff and contains meaningful independent disconfirmers; unknown current scripts/interfaces are explicit handoff gates rather than invented commands. Interpret node --test integration as DOM/controller evidence only: desktop/phone native focus/render proof requires the separately stated actual browser harness. Existing local fixture is scaffolding, not evidence for future integrated bytes. Asset exclusions outside22paths require a bounded amendment if inspection establishes a missing entry; no blanket extra path permission.

Actual independent Oddkit planning challenge2026-09-22T14:43:45.352Z: CHALLENGED, knowledge_base, block_until_addressed false, tensions empty. Generic questions retained: confidence, shared ground, dependency, multiple cases, retraction, strong-claim evidence. Responses: medium confidence in plan sufficiency, no source completion claim; proposal and existing159 both retain real feedback API/controller; depends on acceptedK3a lifecycle and current manifest, failure invalidates this integration ordering and requires amendment; no universal principle inferred from one BODYfocus observation; retract acceptance if exact handoff or new native/privacy evidence contradicts specified contracts. References are immutable159 lineage, accepted fixturefbc0547 and current ticket; future outcome not observed. Tool challenge is not PASS and independent acceptance is separate.

Remaining gates exactly as FIRE-CHECK: acceptedK3a source/tree/custody, Fable ACK/promise, fresh ALLERGY/source checks and coordinator FIRE. Browser/reference assets and full K3a are presently pending; this review cannot activate K3b1. Return implementation through Fable and independent exacthead review; no product edits or new scope introduced by reviewer.
