# Independent reconciliation-plan review — 2026-09-21

Verdict: AMEND before implementation readiness. Reviewed RECONCILIATION-PLAN-2026-09-21.md at a45c68d, authored by orphan_audit; reviewer ui_audit is independent. This is bounded planning review, not source acceptance, claim transfer, FIRE or release permission.

## Accepted corrections

The proposal preserves all three existing candidate commits and requires actual custody before merge reconciliation. Historical intake is correctly distinguished from actual implementation ACK. The complete roadmap inventory/blob comparison protects accepted #156, and cookbook99 pin/source-chain reconciliation protects prior release history. Late response proof correctly tests stale UI suppression after dispatch without pretending to cancel a possibly committed write. Synthetic normal-shell coverage requires actual controllers, sensitive-payload rejection and deny-by-default writes; privacy scope does not expand.

## Required amendments

1. Resolve the normal-shell fixture preparation dependency before calling this implementation-ready. The proposal explicitly says the actual entrypoint/start/run commands remain unverified after a timeout. Name a bounded prerequisite that produces the actual local harness path, executable commands, synthetic route contract and independently observed deny guard, then supply that receipt before FIRE for the browser-dependent reconciliation. Alternatively include a separately authorized, bounded fixture-preparation step with an explicit stop before dependent product work. Do not move discovery silently into final acceptance or invent successful commands. Actual component tests are not a substitute.

2. Record shared-path sequencing with the accepted design batch K3 controller owner, not only #149. Name the exclusive ui/assess/assess.js custody interval, current implementation owner ACK, preserved #159 starting head, and the exact reconciled handoff head that K3 must consume. #149 and #161 remain outside this implementation. Do not assume their old intake names or a proposed K3 seat are ACKs. Coordinator must explicitly disposition independent recovery release versus subsequent single design-batch integration; this review does not require combining them or forbid the previously independent recovery lane.

3. Make the file boundary machine-checkable before custody: expand shorthand paths (feedback-modal.js, feedback.js, openapi.yaml) to exact repository-relative paths and enumerate the release/cookbook records/index entries from the current delta. Retain a narrow explicitly recorded allowance for generated pin artifacts if the recipe requires them; “confirm actual diff at fire” must not silently admit a new release surface. This is scope precision, not broader source archaeology.

## Driver-seat challenge

A future reconciler must be able to start the claimed normal shell without discovering an unavailable fixture only after changing release metadata. A future K3 worker must know which feedback version and source owner it inherits. Those are the remaining operational gaps; the privacy and async semantics are now explicit enough to preserve. Reject broad rewrites, force-push cleanup and component-only proof as shortcuts.

After these bounded amendments, narrow independent review is sufficient. Actual owner/promise, fresh refs/claims, applicable checklist/lens/challenge/6B/FIRE and later exact-head source/browser/check receipts remain owed. No product edits, merge, deployment, live submission or invented owner acknowledgment occurred in this review.
