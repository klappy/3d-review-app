# K3b1 readiness checks — Fable local (read-only; no source edits)
Observed 15:11:43Z start. Inputs: K3a head 185ede97 (verified by Auggie), 159 tip 332b8629 (tag `k159` in my isolated clone), K3b1 TICKET 1.0.1 @ 064de718, plan review @ 3b269b4b. Dry-merge evidence already recorded in the claim; not repeated.

## ALLERGY / privacy / provenance — inspected at exact sources
- Client context (159 `ui/assess/feedback-modal.js` @ 332b8629): `pageContext(route)` emits only `{page: <enum of 9 or 'unknown'>, component:'app_feedback'}`; no ids, hashes, URLs, input values. Experience = `{occurred_at, surface:'web', host:'browser', client_release:{…generated}, context}` — matches the server allowlist.
- Payload (159 `ui/assess/feedback.js` delta, 1 line): body = form fields only (`helpful`, `note`, five scores, `sentiment_journey`) + `require_authenticated:true` + `experience` when `ctx.feedbackExperience` exists. No account email, no `state.principal`, no token: `ctx` is spread into `bound` but nothing from `ctx.state` is serialised. K3a's `ctxFor` adds `pageModel` and guarded `api/apiFull/go/note` — the guarded `api` is what the modal's `bound.api` will call, so stale sends after route/identity change are refused before dispatch (consistent with 159's own `isCurrent` serial gate; both apply).
- Server (159 `src/handlers/feedback-provenance.ts`): `feedbackExperience` allowlist gains `context` with strict enum checks, 1536-byte cap retained; projection rejects unknown keys and non-`client_reported` source; contract/capabilities.json + openapi.yaml mirror the same schema; docs/feedback-provenance.md documents issue 130 slice. Server-authored provenance unchanged.
- Baseline at K3a head 185ede97 (current-main feedback, before any merge): `npx vitest run test/feedback-provenance.test.ts test/version-stamp.test.ts test/ops-feedback-privacy-redact.test.ts test/ops-feedback-persist-http.test.ts test/ops-feedback-persist-mcp.test.ts` → **5 files, 76/76 pass** (miniflare available in the sandbox; no network).
- Account/identity: K3a `#who` is the only node carrying the email; `resetIdentity()` clears it; 159 adds `feedbackModal?.reset()` there — compatible.

## Executable-harness / inherited-exclusion inventory (findings for Auggie)
1. **GENUINE GAP — `ui/.assetsignore` lacks `assess/feedback-modal.test.mjs`** at both 159 (332b8629, file unchanged from merge-base) and K3a head. After the merge the 159 test module would be served as a public asset (wrangler `[assets] directory="./ui"`). `ui/.assetsignore` is outside the 22 K3b1 paths (it is inside my K3a 13). Per ticket: bounded amendment required — one line `assess/feedback-modal.test.mjs`. Requesting that amendment now; the wrangler 404 proof will cover it.
2. `ui/client-release.js` is generated by 159's `scripts/stamp-version.mjs` change and gitignored (159 adds it to `.gitignore`); it is NOT generated at the K3a head (`npm run stamp` there writes only version.generated.ts + changelog.json). Consequences: (a) `assess.js` will import `/client-release.js` — jsdom/vm harnesses must inject `clientRelease` (my integration test strips imports and supplies globals; add it); (b) my esbuild harness resolves `/client-release.js` from `ui/` — the stamp must run before the bundle (already true via `pretest`); (c) it must be SERVED (not in `.assetsignore`) — correct as is; loaded bytes must equal the generated identity (ticket check) — provable via wrangler byte-equal like the other runtime modules.
3. `ui/server.mjs` (159) maps `/client-release.js` and `/assess/feedback-modal.js` for the temporary dev server — in scope, no conflict with K3a.
4. K3a harness inheritance: `test/kit-root.integration.test.mjs` boots via the controller's own guard (`kit || #app`); 159's boot block wraps `boot()` in `if (document.getElementById('app'))` and adds the click intercept + hashchange reset — reconciliation must keep `kit ||` and attach the intercept so it works for the `#feedback` link inside the account menu (the link is inside `#account-menu`; the delegated `document` click listener still sees it; the menu's own click handler does not stop propagation). Opener return: 159 focuses `opener` on close — the opener will be the menu link, which is `hidden` once the menu closes → focus would fail silently; plan: pass the menu toggle as opener when the link is inside the closed menu (owned assess.js seam).
5. Existing 159 `test/version-stamp.test.ts` conflicts with the K3a-era test (dry merge) — reconcile to current stamp semantics + client-release output; counts are results, not targets.
6. Vitest + miniflare run locally; `node --test` suites run; wrangler `--local` runs; Chrome file:// harness + `frames-probe.html` (openmenu/measure hooks) reusable for the dialog focus probe (needs a `?openfeedback=1` hook in the probe build — evidence tooling, not product).

## Exact 22-path worktree plan (executes only at SOURCE FIRE)
1. `git worktree add /sessions/…/w/k3b1 -b design-batch/fable-k3b1-20260922 <accepted K3a head>` (new branch; original 159/cloud refs untouched; check absence on remote first).
2. `git merge --no-ff 332b8629` (history-preserving; all three 159 commits become ancestry). Resolve the 6 known conflicts: package.json / package-lock.json / releases.json / release-manifest.json → keep K3a bytes (no 0.15.0 promotion), keep `release/cookbook/0.15.0.md` as incoming historical cargo; `test/version-stamp.test.ts` → current stamp semantics + client-release assertion; `ui/assess/assess.js` → K3a lifecycle + 159 hooks (imports, `feedbackModal?.reset()` in resetIdentity, intercept, hashchange reset, `kit || #app` boot).
3. Verify diff vs the K3a head touches only the 22 paths (+ the amended `.assetsignore` line if granted); anything else → stop and report.
4. Tests in ticket order: `node --test ui/assess/feedback-modal.test.mjs ui/assess/feedback.test.mjs ui/assess/identity-reset.test.mjs ui/kit/shell.test.mjs ui/kit/coordinator.test.mjs`; `npx vitest run test/feedback-provenance.test.ts test/version-stamp.test.ts test/ops-feedback-*.test.ts`; `node --test test/kit-root.integration.test.mjs` (+ seven feedback outcomes); full `node --test`; `npm run typecheck`; wrangler `--local` 404/200 byte-equal incl. `client-release.js`.
5. Browser: rebuild the file:// harness at the candidate head (transform unchanged), probe hooks for open-dialog Tab/Shift+Tab containment, opener return, outside click, Escape at 1440×900 and 390×844; declare a stable bundle for the reviewer's PNGs.
6. Receipts to this dish; PR from the new branch (draft, no merge).

## Genuine blockers
- `.assetsignore` amendment (finding 1) — needed before the candidate can pass asset exclusion.
- SOURCE FIRE + accepted K3a handoff (in progress).
None else. Checklist: K3a gate closure open pending reviewer; K3b1 implementation blocked on FIRE.
