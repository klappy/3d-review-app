# K3b1 driver-seat delta — 2026-09-22
Planning only. Applies to TICKET 1.0.1 at 1af49eddf756ba61801329d11ab2bbe3df4e97db.
Imagined use: while editing a project, I report a problem, inspect optional experience details, dismiss, and continue the same unfinished work. A delayed response must not appear under another account.

Revisions produced by this lens:
- Use the existing native dialog outside the actual app root; retain the exact editable node, delegated listeners and account/version hosts, not merely the same text.
- Preserve same-route draft; reset on route/identity replacement. A dispatched send can have committed, so stale-UI suppression is not cancellation.
- Keep automatic context minimal and enum-only; account email and underlying form markers are negative payload assertions.
- Reuse original159 history and current canonical metadata rather than stamping an unaccepted design release.
- Carry the existing strict focus oracle forward: BODY or background focus while open fails. Prior actual BODY observation does not establish background activation.
- Scope this dish to feedback real writes; enumerate remaining K3b work instead of claiming whole M2 delivery.

Rejected alternatives: route replacement loses work; an isolated component fixture cannot prove mounted app coexistence; automatic DOM/screenshot capture expands private data; automatic resend can duplicate a committed submission; merging old package/pin over current K3a loses accepted identity history.
Costs: a normal-root browser harness and shared-path sequential handoff are required. No implementation claim follows this planning exercise.
