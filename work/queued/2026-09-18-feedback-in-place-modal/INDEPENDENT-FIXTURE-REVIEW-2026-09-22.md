# Independent PR159 fixture prerequisite review — 2026-09-22

ACCEPT the bounded runnable synthetic normal-shell fixture prerequisite at immutable332b862903c2d3961681649d2d450fd7fe669e8a. This is not PR159 implementation acceptance, reconciliation FIRE or live/provider proof.

Reviewed server.mjs, browser-check.cjs and route-contract.json from /tmp/3d-review-159-normal-shell. Independently copied the browser script/contract to /tmp/3d-review-159-independent and ran:
node /tmp/3d-review-159-independent/browser-check.cjs --origin http://127.0.0.1:8899 --expected-head 332b862903c2d3961681649d2d450fd7fe669e8a

Chrome153.0.8010.53 completed both desktop1440×900 and phone390×844. Actual root Projects controller/editable input survives modal Back/Escape; draft and return focus pass. Pending synthetic feedback then Workspaces navigation suppresses stale modal/receipt, with no claim that dispatched writes are undone.503 retains uncertain draft with no automatic retry during the bounded observation. Minimal submitted context excludes unsaved work. Two synthetic writes per viewport, zero page errors. Deliberate unknown local and nonlocal POST probes abort on both viewports.

Safety: fresh browser contexts with no real cookies; request routing installed before navigation. Exact local origin/method/path allowlist, enumerated local static GETs, feedback intercepted to in-memory synthetic responses. Server also refuses unknown/non-GET routes. Read-only source scan found no active worker/service-worker/WS path in this shell. Acceptance applies to the pinned source and stated loopback command, not arbitrary origins or future unreviewed route additions.

Independently compared all340 tracked source files to Git332b862 after execution: zero mismatches. Author outputs were preserved; independent results and screenshots are in the separate directory.

## Retained product finding

Strict Tab containment remains FALSE on both viewports: BODY appears once in the focus sequence; no background application control observed. The harness accurately exposes this and does not count it passed. It remains a modal keyboard acceptance/fix obligation during159 reconciliation. No claim of universal focus trapping, signed-out/demo coverage, identity replacement coverage or production proof.

This fixture is a valid prerequisite for the next bounded159 reconciliation, which must inherit actual accepted161 main handoff before shared-source ownership begins; K3 waits for accepted159 handoff. Source refresh and rerun are required after that integration. Independent review does not authorize editing protected product source.
