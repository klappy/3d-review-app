# EVIDENCE — Fable local K3a outstanding-evidence receipt (observed 14:39:12Z)
Owner: Fable local Cowork delegate. Branch `design-batch/fable-k3a-20260922`. Draft PR #171 (no merge/deploy).

## Revision
- Head `ab501ef02f9c77c69c82e0f9eecad7331f5c6a00` (remote readback equal) = 46b23d10 + review amend fix: `roleLabel()` own-property lookup on a frozen null-prototype map; `roleLabel('constructor')`/`('__proto__')`/toString/hasOwnProperty/valueOf/prototype/CONSTRUCTOR → ''; hostile role strings in known data yield no shell/tree label. Regressions fail at 46b23d10 (1 fail/7 pass) and pass at ab501ef0. F1/F2 untouched. `node --test` **431/431**, `tsc` clean, thirteen paths only.

## Wrangler asset exclusion proof (previously omitted) — DONE
`npx wrangler dev --local` 4.133.0 (locked dependency, sandbox 127.0.0.1:8791, no network) at head ab501ef0: `kit/app-adapter.test.mjs`, `kit/shell.test.mjs`, `kit/coordinator.test.mjs`, `assess/scope.test.mjs`, `assess/identity-reset.test.mjs` → **404**; `kit/app-adapter.js`, `core.js`, `tree.js`, `tokens.css`, `kit.css`, `components.css`, `assess/assess.js`, `assess/scope.js`, `changelog.js` → **200 byte-equal** to source (sha256 prefixes recorded); `/` → 200 byte-equal `ui/index.html`; `/index.html` → 307 to `/` (asset canonicalisation, pre-existing). File: approved folder `evidence/k3a-wrangler-asset-proof-ab501ef.txt`.

## Exact-head desktop AND phone evidence + durable manifest
- Durable source manifest: `evidence/k3a-harness/manifest.json` rebuilt at ab501ef0 (26 modules → sha256 + git blob; output sha256 16e69ba5f2bc896f…); transform `build-harness.mjs` alongside; harness `index.html`.
- Real-browser DOM journey (Chrome 153, file://, synthetic transport) captured verbatim at 46b23d10: `evidence/k3a-browser-journey-46b23d10.json` — routes #workspaces → #workspace/w1 → #project/p1 → #assessment/a1 → #project/p1, then search "Spring" (tree filtered to one row; #who unchanged; two read cards retained; host ids intact). Header count 1 on every route; host holds exactly `shell-links`,`version`,`account`; Owner/Viewer labels only where loaded; legacy context panel present only on the assessment route. The role-label code path between 46b23d10 and ab501ef0 differs only for inherited-key inputs, none of which occur in the synthetic data, so the capture is representative of ab501ef0; re-capture on request.
- Screenshots (computer-use, read-only) observed at exact heads for desk 1440×900 and phone 390×844 via `frames.html?identity=…&view=desk|phone&hash=…`: owner #workspaces/#workspace/w1/#project/p1/#assessment/a1 (desk), owner #workspaces/#project/p1/#workspace/w1 (phone), viewer #project/p2 (desk), viewer #workspaces (phone), member #project/p1 (desk). **Capability gap:** the screenshot tool returns images to me but cannot write image files, so no PNGs exist in the folder; every capture is reproducible from the URLs above. Chrome was closed during the last measurement pass; not reopened.

## Duplicate workspace-owner cards — pointer for reviewer ruling
- Observed: owner at `#workspace/w1` shows the grouped project "River Valley" as a kit read card AND again as a legacy `entity-card-wrap` card under "Manage grouped projects" (phone capture `frames.html?identity=owner&view=phone&hash=%23workspace/w1`; desktop capture earlier at 7dca20bd; DOM capture `readCards:["River Valley"]`, `actionCards:["Project River Valley Your role: owner Remove from workspace"]`).
- Source: `ui/assess/scope.js` workspace.render — read grid at line ~232 (`kitGrid(ctx, r.items, r.empty)`), retained management cards at line ~233 (`actionRegion(... 'Manage grouped projects' + ctx.cards.cardGrid(cards, ''))`), `cards` built at line ~226 from the same `model.projects` to host the existing `[data-remove]` control (handler unchanged at bind).
- Options for ruling (no change made): (a) keep as explicit intermediate duplication until K3b; (b) render the per-card Remove control inside the kit read card's action slot and drop the second grid (touches only scope.js presentation, keeps selector `[data-remove]`); (c) list only names + Remove buttons (no cards) in the action region.

## Header density — Chris rejection ACK, measurements, proposal (no changes made)
- Exact measurement (real page, 856×931 viewport, #project/p1): header.top = **246px = 26% of viewport**; rows: brand/crumbs (y50–91), `.me` pill (y107, 32px), host row 1 shell-links + Version button (y150–193), host row 2 account email + Sign out + Use another account (y203–251). Content h1 begins at y=340 (37% down); first card at y=514.
- From scaled captures (estimates ±10px): desk 1440×900 header ≈ 125–135px (14–15%) in 2 rows (brand+crumbs; pill+links+version+account); phone 390×844 header ≈ 250px (~30%) in 6–7 stacked rows, then the tree block ≈ 150px, so the work area (project title) starts ≈ 470px down (~55%) before any content.
- Stacked/duplicate rows: role appears twice (crumb badge and `.me` pill); "Signed in" pill is redundant with the account email; links, version and account each occupy their own wrapped row; on phone the tree is a third band above content.
- Proposed compact hierarchy (for Auggie's amended scope): ONE 56px desktop / 48px phone bar: brand · crumbs with the Owner/Member/Viewer badge · right: a single account button (verified email truncated, 44px target, `aria-haspopup=menu`) whose disclosure menu holds the full email (`#who`), Sign out, Use another account, Version 0.14.5 (opens changelog), App feedback, Roadmap — same node ids and listeners, moved into the menu container; drop the `.me` pill (role lives in the crumb badge; identity in the account button). Desktop tree unchanged beneath. Phone: tree collapsed into a "Context ▾" disclosure under the bar (current row + caret), work area first. Identity reset still clears `#who`. Touch targets ≥44px; menu keyboard-operable (reuse level-menu key handling). Expected header: ≤56px desktop (6%), ≤48px phone (6%).

## Fresh Oddkit checks at this revision (exact input/result)
- `oddkit_challenge` (mode execution) on the bounded acceptance claim at 46b23d10: **CHALLENGED, block_until_addressed=true**, governance knowledge_base; missing: confidence level, disconfirmer, retraction condition, scope. Answered here: confidence = working belief backed by the tests/proof above, NOT established for real auth/DEV/prod; disconfirmers = the recorded ones (F1/F2/F3/inherited-key regressions, wrangler 404s, DOM journey); retraction = any reviewer-run case where a stale completion paints/navigates, a label appears without loaded authority, or a test module is served; scope = synthetic normal-root read slice only.
- `oddkit_validate` on the completion claim: **NEEDS_ARTIFACTS** — gaps: visual proof (screenshot/recording) files, change summary, revision tracking. Disposition: change summary = PR #171 body + commits; revision = ab501ef0; **visual-proof files remain a real gap** (capability, see above) and are NOT claimed satisfied.
- `oddkit_gate` planning→execution PASS 4/4 at f52fce62 is a mode-entry check, not completion validation; it does not carry to ab501ef0 as acceptance.

## Blockers / requests to Auggie
1. Visual proof as files: needs a screenshot-capable route (Chris/Auggie capture of the frames URLs, or a sanctioned headless browser in scope).
2. Header density: awaiting amended bounded scope + gates before touching chrome/index.html/core.js beyond current custody.
3. Duplicate-card ruling (a/b/c above).
