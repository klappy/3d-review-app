# K2 build order — isolated accepted presentation scope

User explicitly directs the UI worker to implement accepted design-system gaps while the separate account-fix review continues. Coordinator assigns ui_audit exclusive K2 authored scope: ui/kit/views-public.js, ui/kit/views-coordinator.js, ui/kit/views-admin.js, ui/kit/coordinator.test.mjs and exactly its exclusion line in ui/.assetsignore. Frozen design comparison is c653482135a18e6ca33cccc230b44855595f9dc2. Accepted ticket3bb1e8ac and independent plan ACCEPT7ff9f586 remain design authority; no new design or broader files.

Independent orphan_audit explicitly ACCEPTED a build-only dependency relaxation: freeze full K1 commit1b0ae83a9c79b17f2154317d29c035422e769fce in a separate staging-base ref and isolated K2 branch/worktree. Record the exact base tree. Existing K1 bytes are inherited, not newly authored or accepted by this order. No write to PR162, its cloud branch, K1 tree/core,163 account files, root controllers or canonical environment. K1 IME defect remains AMEND; uncertain cloud custody is not taken over.

K2 authored diff against frozen staging base must be exactly the five paths above. Final integration is still blocked until independently accepted K1 lineage is available; incorporate that lineage, inspect interface differences and rerun affected role/interaction/assets checks before K2 acceptance/integration. No standalone partial-reskin release; one coherent kit-first batch and preserved controller/capability obligations remain.

SOURCE FIRE is authorized after the assigned worker records the current well-formed ticket/checklist/preflight/challenge/6B/lens/FIRE gates with this explicit dependency supersession and independent staging acceptance. Do not repeat broad planning or ask another routine permission; if a real gate fails, stop before source edits and name it. This is conditional source authorization, not a claim the worker has started. Actual implementation-custody ACK received; first source checkpoint25 active minutes from actual FIRE/START. Revised owner estimate60–100 local active minutes supersedes earlier35–60 estimate for now-confirmed frozen-base/browser scope; it is not a silently reset promise or release ETA.

Exact existing ticket obligations TXT042/TXT060/TXT070 apply to retained warnings/privacy/action consequences where these views expose them. Worker maps actual covered GAP/DD IDs from existing accepted crosswalk rather than inventing coverage. Changed-state/completion/blocker/decision reports only. Independent exact-head source/browser review and actual check/release gates remain. Other worker handles163; no competing source edits.
