# Fable design audit intake — single coherent batch

Status: anticipated input; report not received, no external access/ACK inferred. This is an intake contract and coordination amendment, not an empty implementation ticket.

Chris reports Fable on another machine/account is auditing the design system and explicitly requires one coherent batch. This supersedes any proposal to split the incoming design correction into independently drifting UI releases. Existing accepted release160/156/159 work continues through its own gates; no rework or interruption follows merely from this intake.

## Where Fable's report belongs

Product knowledge: klappy/3d-review-cookbook, `planning/2026-09-21-design-system-batch/GAP-ANALYSIS.md`, submitted as one reviewable PR with supporting evidence in that folder. It is a proposed new path until report delivery, not a claimed existing artifact. Link the PR from this existing survey-feedback meal. Do not send the detailed audit as many app-feedback messages; that inbox preserves user occurrences and does not replace governed design source.

Report contract:
- Exact app commit/environment/build actually inspected; cookbook/design-system source commit. If current work changed after inspection, retain original baseline and identify delta instead of silently rebasing conclusions.
- Stable gapIDs; affected user journey/surface/component/token; actual vs intended behavior with source and screenshot/evidence pointer.
- Severity/priority with rationale, acceptance test, dependencies and explicit unknowns.
- Cross-map existing app issues/PRs and our audit-2026-09-21/UI-MATRIX.md; mark duplicate/superseded/new findings without losing provenance.
- No secrets, private user content or unapproved personal voice. Reports are proposals until reviewed; observed evidence and inference remain distinct.

## Batch integration contract

After receipt, one planning dish freezes a coherent baseline and full gap matrix, challenges/reviews the complete plan, and specifies bounded worker partitions. One integration owner owns shared files, design tokens, component contracts and final merge composition. Workers use disjoint paths or serialized handoffs, not independently diverging design decisions. All partitions integrate into one coherent release batch with independent whole-batch visual/journey/regression validation plus affected contracts/privacy/access checks. Partial worker completion is not batch acceptance. Existing meaningful user choices, limits, provenance and accessibility remain.

A missing Fable report or exact source baseline blocks final batch design/build FIRE, not current source-mapping research or existing160/156/159 delivery. No placeholder build tickets may become claimable. Once input exists, concrete fully specified tickets and actual owner promises/gates land in the existing kitchen rail; product specification remains in cookbook.

Actual planning delegate ui_audit ACKed this steering: its former separate UI-release proposal is replaced by one coherent batch with partitions; final copy/tokens/components provisional pending Fable input. CoS root remains router. No external message, Fable assignment, code edit or deployment was performed by this amendment.
