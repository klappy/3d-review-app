# CHECKPOINT 1 — K5a code issue → preview → export (Fable K5 worker, under Auggie)
Observed (oddkit_time): FIRE read 16:22:52Z; branch created + remote readback 16:23Z (**actual START**); checkpoint commit pushed 16:30Z; this receipt 16:31Z (8 min after START). Not merged; not deployed; PR not yet opened (opens at increment 2 after the ignore line and browser proof are dispositioned).

## Base and branch
- FIRE: kitchen c1b227ce7f910cc9510182b0fc8820da88f73c95. Base **57fa453ca20754d1315d6bfbc373151c42bc0d7e** verified = efb53588 + one `ui/.assetsignore` line (participant-presentation exclusion); absence check for `design-batch/k5-code-export*` passed before creation.
- Branch `design-batch/k5-code-export-20260922`; head **10cd7e0c7f7c42c6239813121ec36233c50b8507** (one commit on 57fa). Remote ref readback = 10cd7e0c.
- `git diff --stat 57fa..10cd7e0c`: 5 files, +432/−19 — exactly the five active paths. `ui/.assetsignore` untouched (staff serial custody).

## What changed (five paths)
1. `ui/legacy/index.html` — survey code-batch region only: kit panel (`glass panel`), contextual row with phase badge, compact `.row` action grouping, "Action review" section, once-only note. Same control ids. No new stylesheet links: every class used is already served to the legacy page by `design-system/tokens.css`/`components.css` (checked), so no unrelated legacy restyle.
2. `ui/kit/legacy-adapter.js` (new) — presentation-only `paintCodeBatch(root, model)` + `codeBatchMarkup()` + `validCount()`. No state/transport/storage; never touches `disabled` or replaces control nodes; output painted by `textContent` only.
3. `ui/app.js` — code-only guarded chain: `codeRun()` captures an immutable snapshot (session, assessment, survey, batch generation; handlers add method/target/ids/token) before dispatch and re-checks it before completion/failure notice, state/output paint, and control restoration (each held control returns to its ORIGINAL disabled state; release-codes/issue-link-confirm/build-report derive from state; never blanket false; disconnected nodes skipped). Duplicate pending invocation refused. `api()` gains an optional `evidence` predicate consulted before the diagnostic row append — code operations pass their currentness check; every non-code caller passes nothing and keeps the unconditional append (auth, parsing, redaction unchanged). `clearCodeBatch()` bumps the generation on every existing invalidation path (identity reset, project/assessment/survey change, entity clear). Execute clears the token before the awaited request; a failed/uncertain execute paints a "preview again" warning only if still current; response shape is validated (missing ids/token/codes → explicit failure, never synthetic success). No retry, no revoke/undo/email/PDF.
4. `ui/kit/legacy-adapter.test.mjs` (new) — 6 tests incl. byte-for-byte page/markup parity and "painting never touches disabled/node identity".
5. `test/legacy-code-export.integration.test.mjs` (new) — REAL `ui/legacy/index.html` + REAL `ui/app.js` (imports stripped, real modules injected) in jsdom; synthetic fail-closed fetch installed BEFORE load; only the exact code endpoints plus the reads needed to select a survey are simulated; unknown/non-local → failed closed and logged.

## Observed test evidence at 10cd7e0c (worker, sandbox clone)
- Integration: **24/24** — boot + region; issue→preview exact target `POST /v2/assessments/asm_synthetic_1/surveys/svy_synthetic_1/codes` body `{count:3}` with bearer, dry-run body `{params:{ids},mode:'dry_run'}`, values hidden after preview, review title "Confirm action", redacted evidence rows with no token/values; count 0/101/2.5 refused with no request, 1 and 100 dispatch; original-disabled-state restoration; duplicate-invoke refusal; **18/18 held {issue,preview,execute} × replace {survey,assessment,identity} × {success,reject}**: no stale ids/token/values/notice/error/evidence row, no control disabled at dispatch enabled by the stale return, no secret in DOM, no automatic retry; still-current held issue adopted with truthful notice and retained evidence.
- Disconfirmer proof: the same file against BASE `app.js` (57fa) → **4 pass / 20 fail** (all 18 stale cases + duplicate-invoke + hidden-values), i.e. the matrix catches the existing outer run()/api() behaviour, not only inner assignments.
- Adapter: 6/6. Ordered non-code suites (`ui/diagnostic-path`, `ui/visibility`, `ui/participant-dom`, `ui/participant-resume`, `ui/public-choices`) + the two new: **58/58**. `npm run typecheck` (pretypecheck stamp) exit 0.
- Wrangler local asset proof (`npx wrangler dev --local`): `/legacy/` 200 with the kit region; `/app.js` and `/kit/legacy-adapter.js` 200 byte-equal to source; existing exclusions `/kit/app-adapter.test.mjs`, `/kit/participant-presentation.test.mjs` → 404. `/kit/legacy-adapter.test.mjs` → **200 today** because its exclusion line is the staff-serialized `.assetsignore` cargo not yet integrated; after that line it must read 404 (re-proved at integration).

## Gaps (exact, not simulated)
- **Browser screenshots (1440×900 / 390×844 over real legacy modules): not produced.** No Chromium/Playwright in the sandbox; the in-app browser cannot reach a server started in the sandbox; shared Chrome custody not seized per order. Layout is asserted structurally (jsdom) only. Options for Auggie: independent reviewer runs the harness locally; or grant an isolated browser route; I will not fabricate PNGs.
- Frozen reference c653482 not reachable through any named repo (cookbook 404; app/kitchen 422). Kit idiom mapped to the app's accepted K2 adaptation (`ui/kit/views-coordinator.js`: glass panel, `.row`, "Action review", `note[role=status]`). Independent reference-fidelity check remains required.
- `.assetsignore` line `kit/legacy-adapter.test.mjs`: **requested via coordinator** — not edited.

## Next increment (forecast)
Increment 2 (≤30 min after any handoff): stage/role/refusal cases (401/refused/501/rate-limit/timeout/expired confirmation as truthful failures, fresh-preview requirement after uncertain execute), keyboard/focus/disabled-state cases, generated identity manifest, then open the review PR against main with exact five-path diff, test output and remaining gates. Full slice forecast unchanged: two to three further 30-minute increments, conditional on evidence.
