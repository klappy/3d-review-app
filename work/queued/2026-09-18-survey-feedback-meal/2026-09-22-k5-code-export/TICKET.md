# K5a legacy code-batch/export presentation — proposed bounded build order
2026-09-22. PROVISIONAL: ready for independent plan review and external owner read/estimate; NOT claimable execution or FIRE.

## Outcome and authority
Accepted delivery plan291d7e1 M3/P20 code batches and P24 request evidence; accepted K5 partition4218b0 explicitly owns legacy/index.html, app.js and new legacy-adapter. Deliver the existing legacy staff code issue→preview→confirmed once-only export with frozen-kit presentation, retaining authoritative handlers and evidence. This does not complete P19 code participation, invitations/P16, all legacy routes/P27 or full K5.

Planning source efb53588edb0efd0b2f2b2edb15901794a287cd2; final build base exact accepted staff/release integration chosen at FIRE. No product implementation by internal planner. Proposed cook: available external release Fable only after actual reassignment ACK; do not assume cloud completion or interrupt its release custody. Separate isolated branch proposed design-batch/k5-code-export-20260922; check existing branch before creation.

## Exact six-path proposal
1.ui/legacy/index.html — only existing survey code-batch region presentation/classes/style links.
2.ui/app.js — only code issue/preview/export/clearCodeBatch binding/presentation and narrow selection/identity generation invalidation for those operations. Existing general auth/shared participant/report/share handlers otherwise unchanged.
3.ui/kit/legacy-adapter.js — new presentation-only code-batch adapter, no transport/storage/prototype state.
4.ui/kit/legacy-adapter.test.mjs — new bounded adapter tests.
5.test/legacy-code-export.integration.test.mjs — new actual legacy-entry synthetic harness tests; fixtures inline/synthetic only.
6.ui/.assetsignore — exactly kit/legacy-adapter.test.mjs; coordinator serialized single-line custody, never concurrent with K4 exclusion.

No assess/scope/feedback/core/tree/global CSS, participant files/controller/view, backend/API/storage/grants or metadata writes. Reuse existing kit stylesheet links/classes through owned legacy HTML; no unrelated legacy restyle. Preserve K4 mountParticipantView callable contract without editing its files. If source integration truly needs another path, stop for exact amendment.

## Verified actual controller contract
At efb ui/app.js381–413 and legacy HTML survey-card:
- count integer1–100; POST /v2/assessments/:aid/surveys/:sid/codes body{count}, returned ids retained in page memory.
- POST same /codes/export body{params:{ids},mode:'dry_run'} obtains confirm_token/count/impact/expires_in.
- Explicit separate execute body{params:{ids},mode:'execute',confirm_token}; token cleared before awaited request. Display returned codes by textContent only, then existing once-only/save-now/no-email disclosure.
- Existing identity/project/assessment/survey changes clear codeIds/confirmToken/code output. Existing API controls authority and redacts request diagnostic path, capability/receipt/trace only in events.
Never make preview execute, retain/export credentials to storage, auto retry, add revoke/undo or pretend configured email delivery. Missing/invalid response is explicit failure/uncertainty, not synthetic success. Existing backend roles/stage/refusal remain authoritative; do not infer role from kit prototype.

## Frozen design mapping and preserved differences
Reference c653482135a18e6ca33cccc230b44855595f9dc2 design-system/ui_kits/3d-review/views-coordinator.js collecting survey code-batch rows and confirm/Print disclosure. Match kit panel, contextual batch row, compact action grouping, hierarchy and confirmation treatment at desktop/phone. Prototype fixed Issue10 maps to actual bounded count input; prototype Print maps to actual separate preview→confirmed reveal (do not invent PDF/export persistence). Prototype revoke/undo remains absent because no adopted live command is in this slice. These are explicit semantic preservation dispositions for independent review, not permission to silently remove actual controls. No public prose beyond existing operational strings without accepted copy authority.

## First30-minute useful increment
Owner supplies its own forecast/ACK: actual legacy staff route and selected survey → kit-presented bounded count → issued IDs → explicit preview, with code values still hidden and exact request evidence. Then same dish completes explicit confirmed export, failure/currentness and parity proof. Thirty minutes is checkpoint target, not a guaranteed full delivery or production permission. Preserve working unaffected controls at every candidate.

## Critical currentness correction
Existing run() disables broad buttons but lacks code-specific generation snapshot. Before each code action capture actual identity/session + assessment + survey + code-batch generation. Prevent duplicate pending invocation. On any relevant replacement increment/clear invalidation. After awaited success OR failure, only current snapshot may update code state/output/notification/control state. A dispatched server action may have committed: never claim cancellation/undo. Preserve independently disabled controls; do not refactor global auth/run unless a concrete source need is separately approved. Requiring a fresh preview after failed/uncertain execute remains existing semantics.

## Independent review amendment — complete code-operation effect chain
Supersedes the implementation ambiguity in the preceding currentness paragraph; responds to independent AMEND2ef34b86809d407d0c13a351b8c98d40656c390c. Same six paths.
The existing outer run() announces success/error and restores global controls AFTER the inner handler returns; existing api() appends receipt/trace evidence BEFORE the handler resumes. An inner stale-return alone is insufficient.
Use a CODE-ONLY guarded invocation wrapper and transport-evidence path/hook in owned app.js. Code issue/preview/execute must not pass through unguarded outer completion/error/finally effects. Capture immutable identity/session, assessment, survey, batch generation, method/target/ids/confirm token before dispatch. Check the same currentness snapshot before every completion/error/notification, diagnostic-event append and control restoration. Preserve each control's original disabled state and node identity; never enable new-view controls or blanket restore false. A narrow optional evidence-currentness callback in api() is permissible if all existing non-code callers retain exact behavior; equivalently use a code-only request wrapper preserving existing auth, response/error parsing and diagnostic redaction. Do not create new credentials, storage or global auth policy.
Mandatory actual-entry negative matrix: separately hold issue, preview, execute; independently replace identity, assessment and survey; release success AND reject. Assert no stale code IDs, confirmation token, code values, notification, diagnostic receipt/trace row or control-state changes. Still-current operations must retain actual redacted request evidence and truthful outcomes. Confirm no automatic retry; already-dispatched write may have committed. These cases must catch existing outer-wrapper behavior, not only inner assignments.
Driver-seat revision: secrecy and currentness cover the complete effect chain, including the operator's event log and global status, rather than only the code output panel.
Actual revised Oddkit planning challenge2026-09-22T16:11:48.286Z returned CHALLENGED, block_until_addressed=false. Bounded source-backed wrapper counterexample prompted this amendment; no tool PASS or self-acceptance claimed. Independent reviewer must reaccept before FIRE.

## Seven done-means
1.Real /legacy/ entry and existing selection/controllers perform issue→dryrun→explicitexecute, desktop1440x900/phone390x844 frozen-reference pairs.
2.Values hidden until real execute result; count1/100 valid,0/101/fraction invalid; preview token/count/impact/expiry truthful, no fake batch/count.
3.Distinct selected surveys and ids generate exact targets/bodies; no target drift across delayed request/selection/identity changes.
4.Confirmed values memory-only/text-safe and cleared on all existing invalidations; no codes/tokens/answers in logs, public screenshot or unrelated request evidence.
5.401/refused/501/rate-limit/timeout/expiredconfirmation responses remain truthful; no auto repeat; uncertainexecute requires freshpreview.
6.Keyboard/pointer/focus/disabled-state and double-invoke tests pass; unchanged legacy participant/invitation/sharedlink/report flows regression remain.
7.Exact allowed diff, authoritative role/stage checks retained, test404/runtime200, independent exact-source acceptance; not allK5 or deployed claim.

## Executable proof
Run node --test ui/kit/legacy-adapter.test.mjs test/legacy-code-export.integration.test.mjs ui/diagnostic-path.test.mjs ui/visibility.test.mjs ui/participant-dom.test.mjs ui/participant-resume.test.mjs ui/public-choices.test.mjs, plus current typecheck/stamp rules. Actual legacy HTML/app.js module graph, empty isolated context, synthetic API intercepted BEFORE navigation; unknown/nonlocal reads/writes fail closed. Simulate exact code endpoints only; no real issue/export. Test held success and rejection under selection/identity changes and meaningful failing-parent regressions. Browser actual keyboard/clicks not callback-only mocks; sanitize secret code output screenshots while still assert synthetic values internally. Record exact module/generated identity manifest; stamp after commit before bundle. Reviewer independently checks source/payload/currentness/secrecy and reference fidelity.

## Planning lens/challenge and readiness
Lens: operator must distinguish issuing identifiers from releasing credentials, and must never reveal a late prior-survey batch. Rejected merging this into K4 code participant (shared view ownership), touching current staff scope.js (cloud/K3b2 collision), prototype revoke/undo and broad legacy rewrite. This seam is source-disjoint except serialized assetsignore and shared-view contract consumption.
Actual Oddkit planning challenge16:08:13.501Z returned CHALLENGED/block_until_addressed false; no tool PASS. Its initial hypothesis said literal legacy flow lacks a kit equivalent; subsequent selective source read found collecting code-batch/Print reference. This proposal explicitly maps it above instead of inventing parity or missing-capability claims. No universal principle; wrongtarget/stalecredentialdisplay/duplicateexecute/fixture-onlyrender/refmismatch retract readiness.
Before execution: independent review of this exact scope/design mapping, external worker ACK/current claims/promise, accepted exactbase, applicable privacy/release gates and coordinator FIRE. No unsupported future activity claimed. Return cargo/head/tree/tests/refpairs and actual remaining gates via real dispatcher channel.
