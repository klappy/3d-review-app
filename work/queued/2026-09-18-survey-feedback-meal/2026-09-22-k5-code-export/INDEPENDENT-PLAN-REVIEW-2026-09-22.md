# Independent K5a plan review — AMEND
2026-09-22. Exact proposal36db3da061c0a014eab77b0856be07a6ae05e4b5, TICKET.md. Reviewer ui_audit did not author proposal. Planning only; no product changes.

Scope/reuse accepted in principle: six paths are disjoint from staff assess/scope and participant, except explicitly serialized ignore line. Actual efb53588 app.js code issue/preview/execute endpoints,1–100 validation, one-use confirm clearing and memory-only output match ticket. Frozen c653482 coordinator source contains code-batch/Print presentation; map to real preview/execute without inventing revoke/undo/email/PDF. Thirty-minute first increment is reasonably bounded as a target, not a promise. Existing owner must actually reassign/ACK after release custody; no silent takeover.

One material correction before implementation FIRE:
Ticket currentness requirement covers code state/output/notifications/control state, but the proposed boundary must explicitly encompass the existing wrappers that act OUTSIDE the handler. At inspected source, run() unconditionally emits completion/failure and re-enables nearly all document buttons after awaited task; api() unconditionally prepends request evidence after a successful response. A code handler's internal stale-return is insufficient: run() will still say complete and mutate controls, and api() may expose prior-context receipt/trace in events.

Specify a CODE-ONLY guarded invocation/transport-evidence hook inside owned app.js (or equivalent bounded implementation) that checks the same immutable snapshot before run completion/error/finally AND before appending code-operation evidence. No broad auth/run refactor required or implied. Preserve original disabled states, not blanket false. Keep code request method/target/IDs/token captured before await; old results may have committed but must not change new-context UI. Existing non-code operations retain behavior.

Add meaningful actual-entry negative tests: hold issue, preview and execute separately; change identity or survey; release success AND reject; assert no stale output, notification, diagnostic row or new-control state changes. Assert still-current receipt/evidence is retained/redacted. This closes the producing wrapper behavior, not merely inner-function assignments. It fits the same six paths.

No other plan blocker identified in this bounded review. Asset404 and immutable base/current claims remain execution gates. Source-code response validation/uncertain outcome tests and independently rendered kit comparison remain required, not proven by plan review. Actual author Oddkit challenge recorded in ticket is acknowledged as CHALLENGED, not independent acceptance. Return narrow ticket amendment for readback; readiness dispatch may proceed, source FIRE waits.
