# CLAIM — Fable PR174 isolated-corrections worker (fourth local Fable, under Auggie)
2026-09-22 (America/New_York; observed server_time 2026-09-22T16:32Z–16:37Z via oddkit_time). Status: OWNER CLAIM / READINESS ONLY / NOT FIRED. Orders read: ISOLATED-FINDING-CORRECTION-TICKET.md @ kitchen aa22aec70da74acced7b039b5bbd10926813c8b2; INDEPENDENT-ISOLATED-CORRECTION-PLAN-ACCEPT.md @ kitchen 263630f25e54e33ab9e859fe75d6a775413723cc; BUGBOT-174-907bd5-TRIAGE.md @ kitchen b01e5929828f340d0cb121a62d3d47465e97a140. Kitchen main at read time: 78ef2353.

## Authentic identity
- Surface: fresh Claude Cowork local session in the CoS project, task 07f7043c / worker 2a237323, distinct from the K3a source Fable (`app/`+`evidence/`), K4 (`worker-k4-participant/`), K3a release-metadata (`worker-release-k3a/`) and `worker-code-export/`. Model observed: claude-fable-5-1 (effort setting not observable; not claimed). Design skill: only generic `artifact-design` installed; the frozen kit remains the sole visual authority and is not edited by this dish.
- Runtime observed: isolated Linux sandbox, node v22.23.2, npm 10.9.8, git 2.34.1. Only `/Users/chrisklapp/Documents/3d-review-fable` mounted. Own subfolder created: `worker-release-corrections/` (app/, evidence/). Sibling worker trees NOT touched.
- Role: implementation cook under Auggie, not CoS chair. Source corrections Fable-only; Codex reviews independently. Root routes replies; Git is the queue; no callback-tool dependency assumed.

## Immutable inputs read (GitAuth klappy installation, contents:read, HTTP 200)
- 3d-review-app: PR174 open, not draft, head release/k3a-0.16.0-20260922 @ 907bd5d2b8056e669e7e3972db395b704612f748 → main, not merged. 907bd5d message: "release: pin 0.16.0 candidate record for K3a kit-root source (metadata only)". Proposed branch design-batch/fable-174-isolated-corrections-20260922: ABSENT (404 at 16:34Z). Created only after FIRE, after a fresh availability recheck.
- Local isolated checkout: `worker-release-corrections/app` detached at 907bd5d, `git status` clean, origin https://github.com/klappy/3d-review-app.git (no credential stored in remote URL).
- Four paths at 907bd5d: ui/assess/scope.js 357 lines / 33814 B; ui/assess/assess.js 598 / 65549 B; ui/assess/scope.test.mjs 212 / 19504 B; test/kit-root.integration.test.mjs 283 / 25404 B.
- Source inspection confirms each finding at exact lines (inspection, not browser reproduction yet):
  1. Titles — scope.js:178-179 `kitHead()` comment "The kit shell already shows the page title and eyebrow"; renders `data-read-head` with role/archived/extra only, no h1/eyebrow. Used unconditionally by workspaces/workspace/projects/project renderers (lines 190-317). ui/assess/index.html:20 is `<main id="app">` with no `#rv` — assess.js:22-23 `kitRoot = document.getElementById('rv')` → `kit = null` there, so the non-kit host gets no page heading.
  2. Demo disclosure — assess.js:570 creates `#demo-notice` and calls `app.before(banner)`; in the kit host `app` is `kit.content`, so the banner is a sibling of the content element inside the shell, outside the preserved content/header hosts; `syncShell()` → `kit.update()` repaints can discard it.
  3. Dual retry — scope.js:56-57 `bindRetry` uses `root.querySelector('[data-act="retry"]')` (first match only); project render emits two `data-act="retry"` buttons (assessments at line 308, languages at line 316) → second is inert.
- kitchen: HYGIENE.md is a superseded pointer to health-code/HYGIENE.md (R14); dish dir listing read (13 files incl. SOURCE-FIRE-FABLE-RELEASE-K3A for the earlier metadata dish — not a FIRE for this correction).
- No denial on any named read.

## Baseline runtime proof at 907bd5d (no candidate edits)
- `npm ci` locked deps OK (no manifest change). `npm run stamp` → `0.16.0+907bd5d (release_source 20d5144)`.
- `node --test ui/assess/scope.test.mjs test/kit-root.integration.test.mjs` → 39 pass / 0 fail (7.4 s). `npm run typecheck` → tsc clean. Tree still clean after (generated files ignored).
- Harness observation: kit-root.integration.test.mjs boots the REAL ui/index.html + real controller in jsdom over the synthetic fail-closed transport; the same harness can load ui/assess/index.html (non-kit host) by parameterising the HTML source — this is how non-kit title proof will be produced inside the four paths, no new fixture file needed.

## Custody ACK — exact four paths (ticket aa22aec7)
1 ui/assess/scope.js · 2 ui/assess/assess.js · 3 ui/assess/scope.test.mjs · 4 test/kit-root.integration.test.mjs. Base 907bd5d2b8056e669e7e3972db395b704612f748. Branch design-batch/fable-174-isolated-corrections-20260922 (rechecked absent before creation), review PR only.
Everything else read-only: no kit component, release metadata, auth/session, API/backend, permissions, package/config, .assetsignore, participant or legacy changes. No edit to release/k3a-0.16.0-20260922, PR174, any cloud/Autofix branch, staff K3b2 scope.js branch, K4 participant branch, or code-export worktree. No main/prod/DEV write, merge, tag, deploy, force-push, cloud cancellation or takeover. Tests extended only, never weakened. If demo correction proves to need a kit path, I stop and return the concrete path/contract for amendment.

## Gates (actual, this owner)
- oddkit_preflight 16:36:20Z: FOUND; DoD klappy://canon/definition-of-done; pitfalls: visual proof for UI, test output for logic, reference decisions. Constraint surfaced earlier via bootstrap: release-validation-gate (same-session smoke is not validation).
- oddkit_gate: invocation 1 (16:36:24Z) mis-detected as execution→completion NOT_READY 1/2 (wording "execution readiness"); invocation 2 (16:36:35Z) PASS but still detected execution→completion — not counted; invocation 3 (16:36:42Z) "start building" detected planning→execution PASS 4/4 (decisions_locked, dod_defined, irreversibility_assessed, constraints_satisfied), governance_source knowledge_base. Owner gate PASS is a prerequisite, not FIRE.

## Own promise (binds only at Auggie SOURCE FIRE)
First 30-minute increment: recheck branch absent, create it from 907bd5d; correction 3 (bind every rendered retry with one shared in-flight guard + existing swap/isCurrent currentness) plus scope.test.mjs regression that fails on parent (second button unbound) and passes on candidate; correction 1 (explicit host-aware title context, e.g. ctx flag set only by the kit controller, default = render heading) plus non-kit and kit tests for all four pages; run the two bounded test files + typecheck; push; return head SHA + counts here. Second increment: correction 2 (demo disclosure kept in a controller-owned stable host or re-inserted through the existing lifecycle without recreating content/inputs) with the parent-failing repaint test in kit-root.integration.test.mjs (route, tree search, expansion, model update; one visible disclosure; sample links; no lost field values; write refusal unchanged) and stale-retry currentness proof. Third–fourth increments: browser proof (root + non-kit entries, desktop 1440×900 and phone 390×844, local synthetic session, fail-closed transport, screenshots + module hashes + console capture in worker-release-corrections/evidence/), review PR opened, cloud PR174/associated branches refreshed and compared, handoff report (head/base/tree, four-path diff, finding→fix→test map, commands/counts, parent failures). Forecast total: four 30-minute increments (~2 h) inside the 4–6 h rollout target; retracts on head drift, four-path insufficiency, or unique cloud delta — reported, not skipped.

## Blockers / capability gaps (exact, not simulated)
- Auggie callback channel not observed; return is this session + this Git receipt. No recipient ACK invented.
- GitAuth token currently contents:read only; branch push needs contents:write and PR opening needs pull_requests:write on 3d-review-app — requested only at FIRE, scoped to that repo.
- Mounted worktree refuses some unlink operations (git pack/maintenance lock warnings); fetch, checkout, npm ci, stamp, tests and typecheck all succeeded. Fallback if a commit fails on the mount: sandbox-local clone with evidence mirrored into worker-release-corrections/evidence/.
- Browser proof will use the isolated in-app browser or loopback harness only; shared Chrome not seized; no Cursor dashboard route.
- Cloud Autofix 106825481088 state not observable to me beyond PR174 head unchanged at 907bd5d (16:34Z); no inference of abandonment.
