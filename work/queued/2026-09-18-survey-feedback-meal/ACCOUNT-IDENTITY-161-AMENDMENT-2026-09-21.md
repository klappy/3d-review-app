# Issue161 account identity and switch — incident scope amendment

Status: bounded planning, NOT FIRED; supersedes generic-greeting-only remedy. Existing intake: app issue161 and this survey-feedback meal ROADMAP-CURRENT. ui_audit actual source-investigation ACK; implementation ownership not assigned. No product edit, live logout, account switch or data write performed.

## Settled requested outcome
Show the actual authenticated email in account UI and provide clear Sign out / Use another account. An alias or generic greeting is not a substitute. Optional persisted alias/profile, signup redesign and data/grant recovery are excluded. Preserve restricted incident identities outside Git.

## Observed source and incident boundary
App main76fe13823dda23c9c046d2b44cdce94fc0842602: src/access.ts verifyAccessJwt validates signature/issuer/audience/expiry and lowercases verified email. src/index.ts /v2/auth/access hashes that email and selects principal by email_hash; Access subject is not the principal lookup key. src/auth.ts mintSession persists an independent12-hour app session; extra via/sub are not stored. resolvePrincipal prefers Authorization bearer over session cookie and resolves the stored principal without checking current Access identity. ui/assess/assess.js holds facilitatorToken in tab sessionStorage and consumes credential hash on return. Thus matching current provider email alone is not a durable proof of app identity.

Actual browser incident: both environments showed opaque identifiers, and authenticated same-session API responses returned zero grants/projects/workspaces. Separate investigator subsequently verified each app principal's stored email hash matches the observed provider email. This is incident-specific matching evidence, not permission to assume future sessions match. No actual email, IDs, hashes or screenshots enter this record.

ui/assess/scope.js hero renders principal.id and ui/assess/assess.js boot renders kind + id. authMe in src/handlers/platform.ts supplies no email/name; principal schema stores email_hash only. A Principal.email optional TypeScript field is not populated proof. Synthetic actual-function reproduction at /tmp/3d-161-evidence/reproduce.mjs confirmed both exposed IDs; this is VM/render evidence, not real sign-in journey acceptance.

## Exact logout contract and limitation
Existing DELETE /v2/auth/session invokes authLogout: it revokes the credential actually presented; OAuth callers separately revoke their client grants. HTTP transport clears session cookie. Existing entry signout then removes facilitatorToken, resets identity and paints signed-out even when its write helper returned undefined after failure: correction must wait for confirmed result and preserve uncertainty.

After confirmed app logout and client reset, browser navigation to SAME-ORIGIN /cdn-cgi/access/logout is the documented Access logout action. Official reference: https://developers.cloudflare.com/cloudflare-one/access-controls/access-settings/session-management/#log-out-as-a-user . It clears that host's Access cookie and revokes the Access session across applications; provider tokens can remain accepted20–30seconds. This is not a per-app-only operation and does not itself revoke independent app or connector sessions. Return to /v2/auth/access afterward for email-code sign-in. Do not invent an undocumented redirect parameter or promise instantaneous provider revocation. Product copy must make the cross-application Access effect clear at the switch action. No agent should execute this against the incident session while investigation continues.

## Minimal implementation shape and unresolved contract
Two UI identity exposures must become one consistent account presentation; retain current identity-generation invalidation. Show email only from a server-verified binding to the currently presented app credential. Do not display get-identity email as the app email without that binding; do not infer email from principal ID/hash, copy it into URL/localStorage/logs/feedback, or use it for grants. Missing binding must honestly say account email unavailable with deliberate reauthentication action, not fabricate identity.

The existing app contract cannot currently return that verified email. Required bounded design decision before build: establish an authenticated, no-store account-display response whose provider email is cryptographically verified AND matched to the presented app principal email_hash, or bind verified email to newly minted app sessions with an explicitly reviewed privacy/retention contract. Prefer no new persistent PII; determine exact Access-protected read route/forwarding contract and configuration needs before choosing. Do not silently introduce a new endpoint, database column, cookie payload, or broader auth.me/MCP disclosure. This document does not claim that decision is implemented or implementation-ready.

## Custody, acceptance, and gates
K3 owns root/controller integration: ui/assess/assess.js and scope.js are shared protected paths. Coordinator must name an actual implementation owner and ACK, sequence159 accepted-head handoff, isolate149 navigation, freeze starting head and exact backend/test/config write set after account-display contract review. No parallel root edits. Any urgency-driven independent release requires explicit coordinator disposition, preserving one coherent design batch afterward.

Acceptance: actual email corresponds to active app principal under differing Access/app identities, absent/expired provider session, bearer-versus-cookie mismatch and rapid identity replacement; no prior email remains after invalidation. Treat email as bounded text, not HTML. Successful logout revokes presented credential and clears matching browser state; failed/uncertain logout cannot claim success or trigger automatic account switch. Switch reaches a different-email sign-in choice in authorized synthetic/test account browser proof; no live incident logout used as test. Connector and other app sessions are not silently claimed revoked. Root desktop/phone account UI, existing role/grant behavior and159 state remain unchanged except scoped identity controls. Independent source/privacy/browser review and normal release gates apply.

Conditional effort: source-only generic-ID removal was small, but requested verified email and full switch require this explicit auth contract first. Implementation estimate withheld until that boundary is accepted; do not reuse pure-view K2 estimate. Next bounded step is independent review of proposed identity-binding alternatives and exact file/config scope, not broad profile planning.

## Superseding concrete implementation ticket — read mode on existing protected path

This section resolves the earlier identity-binding alternative for independent review. Proposal, NOT FIRE. Class: entrée. Risk: ALLERGY because authenticated email is PII (live CHECKLIST row8/R12), even without persistence. Station: subagent. Owner: ui_audit planning only; implementation ACK pending independent acceptance. Promise: source/planning checkpoint delivered; build promise not yet bound. Depends: independent plan review, exclusive K3/159 handoff, fresh refs, current ALLERGY gates and incident-specific release disposition. No data recovery or grant writes. Current source baseline76fe138; coordinator now explicitly sequences161 FIRST at current main76fe138, before159 andK3; neither has an active overlapping product writer. Later159/K3 consume the exact accepted161 tree. First build owner is not assigned or ACKed by this plan.

### Chosen mechanism
Use existing Access-protected GET /v2/auth/access with exact query view=account. The route already receives Cf-Access-Jwt-Assertion and uses verifyAccessJwt with configured issuer/audience; therefore no new protected path/configuration is proposed. At the very start inspect presence of view: account enters read-only branch; every other/duplicate view value fails400 with no auth side effects. Ordinary no-view sign-in behavior stays unchanged.

Read branch: validate existing provider assertion; resolve current app principal using unchanged resolvePrincipal precedence; permit only current user/support first-party app session, not anonymous/participant/OAuth. Read that principal's email_hash and compare SHA256 of verifier-normalized email. Return200 JSON {email: verifiedEmail} ONLY on match. Reject mismatch/missing/expired/invalid state with generic401/403 JSON and no email/provider metadata. Never insert principal, mint session, invoke consent, redirect, set cookies or emit receipt/log with email. All responses Cache-Control: private, no-store; no CORS expansion. Length/type guard treats empty/non-string/over254-codepoint email as unavailable; render full accepted value as text, never truncate to a different apparent account.

Client makes exact same-origin read with current app authorization mechanism, credentials:same-origin, redirect:error, cache:no-store; no automatic navigation on failure. Do not route via helper that drops redirect:error. Identity-generation check guards completion; reset clears email synchronously and failures clear any prior displayed email. A provider redirect/error is account identity unavailable, not a successful app logout. Show matched email in shell account area on all root routes and remove the duplicate opaque-ID welcome label. No client hashing, provider identity proxy or persistence.

### Actual controls and logout
Observed Projects/Workspaces UI has no Sign out or account switch control. Add accessible always-available account controls in existing root shell, retaining route/body work. Labels: Account, Sign out, Use another account. Account unavailable state: “Account email unavailable.” No alias fallback or fake email.

Sign out invokes existing DELETE /v2/auth/session exactly once. Only confirmed signed_out:true clears facilitatorToken and identity state and returns to signed-out root. Failure/uncertain outcome retains honest visible state; no automatic retry or SSO navigation. Avoid current scope helper's unconditional clear-after-failure. Reuse one root logout function via ctx so home action and shell controls agree. Preserve connector logout behavior server-side; do not change authLogout or claim all sessions terminated.

Use another account makes cross-application effect reviewable before effect: “This also signs you out of other apps protected by this Cloudflare Access account.” Confirm action explicitly labeled “Sign out and use another account”; cancel makes no request. On confirmed app logout clear local state, then same-origin /cdn-cgi/access/logout. Do not add undocumented return parameters, auto-clear other sites or execute on incident session. Provider docs specify up to20–30seconds revocation; do not claim instantaneous global completion. Supply a clear return-to-sign-in instruction before navigation; actual authorized test-account browser run must demonstrate selecting a different email before acceptance. Existing provider page may require user navigation back to /v2/auth/access; this is a known UX limitation to observe, not invented seamless redirect behavior.

### Exact proposed source custody
1. src/index.ts — guarded account read branch only, before sign-in side effects.
2. test/access.test.ts — preserve verifier tests; add actual Hono route tests for account read/ordinary sign-in boundary using synthetic signed claims and fake D1.
3. ui/index.html — account container/control mount in existing header only; minimal wrapping styles if required for long emails/mobile, no visual overhaul.
4. ui/assess/assess.js — generation-bound account read/render and single confirmed logout/switch handler.
5. ui/assess/scope.js — remove principal-ID welcome output and delegate existing home signout to shared handler.
6. ui/assess/identity-reset.test.mjs — deferred identity/account response, logout success/failure and stale callback proof using real functions.
7. ui/assess/scope.test.mjs — welcome generic status/noID and correct shared logout delegation.

Everything else excluded: schema/migrations, auth.ts, access.ts verifier, platform auth.me/MCP, principal grants, contracts, K1/K2 modules,159 modal implementation,149 return route, release/version/config. If verifier validation or test harness needs an excluded path, report concrete bounded amendment before write. Existing tests remain excluded by current asset rules; verify before handoff.

Coordinator grants161 the FIRST exclusive interval across the seven listed paths at main76fe13823dda23c9c046d2b44cdce94fc0842602; no parallel159/K3/149 protected-path writer. Actual worker ACK and fresh ref check are still required before FIRE. This supersedes earlier159-first dependencies in this document. Later159 andK3 must consume the exact accepted161 tree and preserve its account behavior. No automatic merge into main or production; coordinator chooses urgent recovery integration versus batch sequence explicitly.

### Required tests and evidence
- node --test ui/assess/identity-reset.test.mjs ui/assess/scope.test.mjs ui/assess/entry.test.mjs
- npx vitest run test/access.test.ts test/logout-cross-face.test.ts test/mcp-oauth.test.ts test/participant-auth.test.ts
- npm run typecheck, and repository-required checks at final handoff; generated artifacts follow existing stamp recipe, not new arbitrary files.
- Backend matrix: matching signed email returns minimal email; mismatch, wrong audience, expiry, no session, participant, malformed email and duplicate/unknown view refuse without email. Assert zero D1 writes, mint/consent/cookies/redirects for every account read. Ordinary no-view sign-in still mints under existing tests; bearer/cookie mismatch honors actual precedence.
- Client matrix: same identity success, unavailable and redirect failure, stale deferred result after identity reset, logout failed/uncertain versus confirmed, double-click busy, cancel switch, and no uncontrolled provider navigation. No private values in diagnostics/storage/feedback.
- Actual root synthetic browser at1440x900/390x844: Projects and Workspaces account controls visible; full long email wraps/no overflow; keyboard operation, focus/cancel, signed-out state and identity replacement; root input/body retained while account read resolves. Deny all unrecognized/nonlocal writes. Record exact head and screenshot paths with only synthetic identities.
- Separate authorized test-account provider journey verifies different-email entry after Access logout, with cross-app effect acknowledged by test operator. Current incident session is forbidden fixture. If no such authorized account/browser is available, report this specific acceptance gap; source/mocked test is not end-to-end pass.
- Independent exact-head privacy/source/browser review and normal terminal checks precede release gates. Source/API repair alone does not restore missing data or justify closing incident.

### Driver-seat revision, challenge and effort
Future user must identify the account without knowing internal IDs and deliberately switch without a failed logout silently claiming success. Revision replaces generic label-only repair with matched transient email and reachable shell actions; separates app signout from provider-wide switch. Rejected unbound get-identity display, persisted profile/email table, alias substitute and undocumented provider redirect.

Oddkit fresh planning challenge returned knowledge_base, no tensions, block_until_addressed=false. Answered confidence/disconfirmer: source/header verification is observed; query forwarding and provider switch UX remain working assumptions to prove. Failure to preserve query/claims at Access, any account-read side effect or inability to choose another email invalidates acceptance. Reversible source patch carries no data migration; actual logout is consequential user action and remains explicit. This is a narrow case, not a universal identity design.

Owner-supported conditional estimate:45–75active minutes implementation/local tests/browser fixture after independent plan acceptance and exclusive source custody; provider test-account availability, independent review and service checks add elapsed waits. This is an estimate, not a new build promise. Current live recipes/checklist/preflight/6B/FIRE, named owner ACK/promise and scoped independent plan ACCEPT remain required. No product edits or live logout performed.

## Narrow independent AMEND resolution
Review8dfda2bd691c8098318af9f2b44f08f990408542 required two concrete corrections. They supersede any less precise wording above.

1. Account read eligibility is the selected existing FIRST_PARTY session contract, not invented Access-minted provenance. After resolvePrincipal, query session by exactly its sessionTokenHash, joining its principal; verify the row is still present/unexpired, row principal_id equals resolved principal.id, stored kind is user or support, and delegated_by is null/empty. Reject delegated_by regardless of cookie/bearer transport or supportActor exposure. Reject participant, missing/replaced/expired row and OAuth; no fallback from a selected invalid bearer to another cookie session. Undelegated stored support session may display its own matched account email; delegated support is refused. Verify provider email hash against this same joined principal row. Keep this inspection in index.ts only, no auth.ts refactor or provenance column. Tests include ordinary user and undelegated support success, delegated user cookie AND bearer refusal, delegated support cookie AND bearer refusal, bearer/cookie selection mismatch, expiry and missing row. Never claim every st_ token came from Access.

2. At logout/switch start capture identityGeneration and current in-memory credential selection. After any asynchronous response, act on client state or provider navigation ONLY if that generation and credential still match. A late success for an older identity may have revoked its server session; it must not clear a newer account, overwrite its status or navigate it to global Access logout. A late failure similarly does not contaminate current status. Do not call the dispatched old request canceled/undone. Existing resetIdentity already changes generation; preserve that invariant. Add deferred success and failure tests for both plain logout and switch with intervening identity replacement; assert current email/body/token retained, no provider navigation and no stale status. Same-identity confirmed completion may clear current state; failure/uncertainty does not.

Driver-seat delta: switching identity while logout is pending must leave the new account untouched; a delegated cookie is not silently treated as first-party merely because the resolver hides delegation. These revisions require narrow independent re-review and applicable refreshed challenge before FIRE, not broader auth work.

Coordinator source-custody supersession:161 goes FIRST, starting at current main76fe138, exclusive seven paths.159 andK3 are later consumers of accepted161 exact tree. No source merge/deploy/release permission follows from this sequence; incident-specific disposition remains required.
