# Independent K3 external fixture plan challenge — 2026-09-22

ACCEPT bounded plan shape at ticket27c4774cb4ab9f94d9957f35b3b2c257153484c6, blobd6b5de809c63d100523b03d7e9beff3f974a88ca. This is planning acceptance only; declared dependency gate is currently NOT satisfied because pinned K1ef661bc has independent AMEND4390a330. Do not FIRE until its corrected exact accepted head/tree is recorded and coordinator reorients the dependency. K273a995e is independently accepted.

The declared files are external synthetic fixture artifacts only. Actual transport/auth/storage/root activation and app source writes are excluded; immutable input archives and per-path source manifest preserve source provenance. The two-source composite must remain explicitly throwaway and never be represented as an integrated accepted tree.

Driver/usefulness challenge: the fixture makes identity/route/epoch changes, denied actions, two-item target distinctions, report eligibility and uncertain receipts selectable and observable. Callback refusal must be proven by attempted interaction with retained old controls/deferred results, not merely a model label. The prescribed deliberately unguarded variant ensures that callback-currentness tests can fail. Existing K1/K2 role display cannot manufacture permission.

Safety and sufficiency: empty context, interception before navigation, literal local GET allowlist and all-write/nonlocal rejection prevent live effects. Synthetic labels and in-memory outcomes prevent invented provider receipts. Plan correctly distinguishes external adapter-model proof from P01–P27 production/controller acceptance and preserves161→159→K3 custody. No human approval boundary is introduced by this purely synthetic scope.

Execution evidence must keep byte equality of each immutable input archive separate from the declared mixed fixture dependency map; cannot assert the mixed composite equals either whole Git tree. Browser case matrix should enumerate fixed named states/roles and executed combinations; two viewports alone cannot imply all combinations were covered. These are applications of existing acceptance wording, not new scope or new framework.

After dependency refresh, existing per-dish preflight/checklist/challenge/FIRE and actual owner ACK remain required. This review does not mark those unrun gates passed, authorize code or accept future fixture results.
