# Premerge gate inventory — read-only, proposed next order

Observed START 2026-09-17 11:44:23 UTC. Inspection only: no test executions, provider calls, workflow/branch protection/config changes or secret reads. App inspected at participant candidate `04d2fca9c95e3d57968cb639251d946634d081f5` (PR44 base fad1); remote-tracking integration/demo-combined-6 package/workflow tree also inspected. This is not a fresh PR44 check-status receipt. Historical B/G cargo below remains explicitly historical; owner ACKs/budgets are not inferred.

Captain's current acceptance requirement: exact premerge candidate correctness, applicable quality gates, persona success and performance must pass. A postmerge provider test is too late to establish that premerge condition.

| Gate | Already reusable | Concrete gap / smallest proposed closure |
|---|---|---|
| Exact candidate + review | AGENTS.md and docs/release.md require independent exact-head review, real Cursor Bugbot literal SUCCESS, all attached checks terminal/dispositioned. Release identity stamps SHA. | No tracked .github workflow in inspected tree. Existing documented ruleset requires Bugbot, not full local suite. Provider build is documented as `npm ci && npm run typecheck && npm test` after Git trigger. Do not represent these as premerge correctness automation. First reuse manual exact-SHA receipt bundle; automation/enforcement is separately authorized work. |
| Backend correctness | package.json:8–13 stamp/typecheck/test; vitest.config.ts includes `test/**/*.test.ts`. Existing Miniflare/D1 fixtures cover source roundtrip, grants, auth, concurrency, report state, shared recovery. | Run all applicable tests on the actual final union SHA before merge and retain command/runtime/exit/log. Prior individual slice passes do not establish union pass. The 34-context/425-tuple integration case at60s is near limit and has failed even isolated: runner serialization is NOT demonstrated repair. Retain as robustness debt; never weaken assertions or declare a timeout performance acceptance. |
| Frontend unit/DOM | `node --test ui/*.test.mjs`; 103 tests passed on participant mount. `test/shared-link-browser.test.ts` has47 fault cases but uses fake DOM, not Chromium. | npm test excludes UI .mjs tests. Explicitly include node UI command in candidate receipt; candidate changes may add tests so do not hard-code103 as permanent count. |
| Real browser + security | scripts/consent-browser-check.mjs executes real local Worker with Miniflare D1/KV, synthetic Access JWT/JWKS, actual Chromium; verifies Connect/Cancel, callback, CSP, redirect refusal. Existing stage/report/participant browser cargo in /tmp has screenshots/assertions. | Consent browser requires externally supplied Playwright and Chromium (not package dependency). Current participant browser is actual UI with API stubs, not real authenticated Worker proof. C owns final authenticated union proof. Proposed future consolidation preserves existing scripts rather than substitutes screenshots or adds a platform. |
| B2 success journeys | scripts/journeys.mjs, scenarios/journeys.json, runner-selfcheck tests. Fail-closed exit codes, required floor AUTH/ME/J8/J14a/J7/J6/MCP/OUT, explicit extra REQUIRE; local-only shared J11 with ALLOW_WRITES. | COVERAGE currently marks only J7/J8 EXECUTED;7 PARTIAL,5 NOT-EXECUTED. A green runner floor is not all14 journeys. Use exact coverage row/capabilities and real role fixtures; no SKIP/RESERVED credited as required success. J11 fixture still Q1:4, not proof of all source templates. |
| B3 HTTP/MCP parity | scripts/parity.mjs distinguishes RESULT vs REFUSAL, reserved and exclusions; runner tests challenge exit behavior. | Placeholder IDs currently establish refusal parity; source itself says FX real-fixture mode deferred. No whole-product success parity from same/compared. Need same synthetic principal/scoped resource and expected result on both faces, with exclusions individually covered. |
| Persona outcomes | Original G0 cast: owner, facilitator/member, code participant, link participant, viewer, support, seeded support+owner; B2/B3 define mechanics; Grok owns execution/observations. | Require task completion with postcondition and negative authority assertion for each applicable role/route, on exact candidate. Repeated owner/viewer sentiment waves do not cover missing participant/support paths. Sat/confidence/frustration are qualitative observations, not correctness or statistical repeatability. Preserve separate run/session/environment/persona metadata; no pooled acceptance score. Human-only print/phone/voice/acts-as cases remain explicit where still applicable; old source policy/capability claims must be reconciled to current rulings. |
| Performance | Existing durations/provider logs and fixed source corpus are measurement seeds; real local Worker harness exists. | No latency/throughput/browser performance acceptance threshold found in inspected scripts/package or original targeted B/G/PRD cargo. No established workload size/concurrency/cold-warm/device/network protocol. Do not invent a passing threshold. Smallest next order: capture local baseline for actual login/context/form/review/submit/receipt/report paths, source13-item form and34-context report corpus, stated data scale and repeat counts; agree practical budgets with product owner, then challenge/add bounded assertions. Test wall-clock timeout is not user latency. |
| Visual/accessibility | Actual desktop/mobile source-matched screenshots, keyboard focus tests and native-invalid focus evidence exist; source matrix distinguishes partial/full parity. | No repository-wide a11y scanner or accepted quantitative a11y/performance rule observed. Reuse meaningful keyboard/hidden-state/viewport assertions; preserve exact source comparison and genuine remaining header/composition gaps. Installing tools or declaring comprehensive WCAG compliance is outside this inventory. |

## Safe local capabilities and missing inputs (names only)

- Local isolated Worker/Miniflare D1/KV is available in existing test implementation; synthetic migrations/source fixtures already exist. Use ephemeral DB/container state, never replay seed against shared DEV.
- `scripts/local-vertical.mjs` defaults loopback8795; `scripts/pinned-vertical.mjs` defaults8796 and uses real pinned multi-item flow. Both mutate their chosen target and accept DEMO_BASE override: explicit loopback guard in orchestration is required, not assumed from a default.
- B2 accepts BASE, EMAIL, EXPECT_PROVISIONED, VIEWER_SESS, ALLOW_WRITES, REQUIRE. B3 needs BASE and SESS. Do not print values. Existing local seeded identities can supply sessions; key existence/access on any external runner has NOT been probed.
- Browser execution needs Playwright package + browser binary, presently host-local. A portable/container supply mechanism and version pin are absent from package.json; no install has been authorized or performed.
- Local OAuth browser oracle generates synthetic signing keys/JWKS itself; it does not require production Access secrets. Real provider/email authentication remains a distinct environment capability/evidence boundary.

## Existing homes and ownership proposal

Keep source/test commands in app `scripts/`, `test/`, `ui/*.test.mjs`; release requirement procedure in existing app `docs/release.md`. Put the concrete accepted premerge acceptance matrix and evidence pointers in existing cookbook `planning/2026-09-16-parity-build/` and the existing B2/B3/G3/S1 tickets. Return B mechanics to issue15, Grok cast outcomes to issue19, stitched exact-head disposition to issue17/release coordination. No new platform, crew or parallel persona implementation. Root/C complete current candidate first; reviewer remains on44.

Historical inspected cargo: `/tmp/3d-remaining-goal-plan/sources/{MEAL-B,MEAL-G,G0-CAST,04-ACCEPTANCE-EVIDENCE,11-FIELD-TEST-PLAN,18-PRD}.md`; these are retrieval copies with prior provenance in that folder, not current orders. G meal itself says ORDERED/not fired; subsequent receipts must supersede it explicitly. Existing acceptance evidence labels partial, not full B3/full-app. Current ruling takes precedence over obsolete source counts, reserved semantics and suppression assertions.

Recommendation: use the existing exact-head manual bundle immediately as the release gate, with unresolved persona/performance rows honestly open. A separately challenged bounded automation proposal may make those commands mandatory premerge later; this inventory neither installs that nor changes enforcement.

## Operator correction: shared exact-candidate preview

Chris identified duplicated per-reviewer build/start work on 2026-09-17. Default proposed architecture: one Git-triggered Cloudflare preview bound to immutable commit/build identity; independent reviewers and persona workers consume that same build and retain separate judgments/artifacts. Local builds are for necessary isolated tests or defect reproduction, not an automatic prerequisite for each reviewer. A moving branch alias alone is insufficient identity proof.

Root observed DEV previews_enabled=true with only main build trigger, and separately ordered Auth to enable a non-production versions-upload trigger. Implementation/readback is pending; do not claim it exists from this inventory. Existing Worker version previews share DEV resource bindings, so they are not disposable databases and do not authorize fixture writes. Preserve no seed/migration replay, current main-to-DEV/production routing, and current campaign limits. Journal k0296/k0299 records authority and custody. This bounded preview setup is now parallel current work; broader agentic CI remains after-demo queue.
