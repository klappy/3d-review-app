# First shipped increment reconstruction follow-through — proposed execution plan

Status: planning only, not FIRE. Existing home: kitchen `rail/1-ordered/2026-09-16-3d-reproducible-preplan/`; project knowledge stays in cookbook `planning/2026-09-16-reproducible-build/`. Preserve PR18/dac8a457 historical proposal and its supersession. Do not create a duplicate ticket or merge that historical PR wholesale.

## Scope, authority and owners

Reconstruct immutable app `a57ba930ced3c23616982c7dab291949b75a3b8a`, tree `f0d676873e597b9f74848b2e3ca86e618a5c154d`, from maintained cookbook plus named retrievable dependencies. The measured denominator is **140 tracked paths**, including dist, docs, tests, seeds and tooling. The release is accepted for the first DEV collection increment, not the full product. Next-sprint changes do not move this target. Production, remote DB actions, new features and fixing known product residuals are excluded.

Root coordinates existing queue and is journal pen. Candidate execution coordinator: existing Auggie `/root/queue_resolution`; candidate cookbook author: its available existing worker after actual custody/availability ACK. Independent planning reviewer/comparer: `/root/testing_plan_review` if available; it has already seen target source and cannot serve as the blind cold builder in that context. Cold construction requires a fresh agent context with no inherited app/oracle exposure, selected through existing Auggie custody after actual availability ACK, not a new user-owned task. Native Auditor's prior 117-file crosswalk is an input, not a request to repeat its source audit. Auth and Design supply missing provenance only when needed. No executor or reviewer ACK is claimed.

My planning estimate: 10–15 minutes from actual START 05:59:48Z. Proposed execution capacity to validate with the actual owner: 45–60 minutes for bounded evidence/gap audit, then 2–4 hours for cookbook recipe/source completion and own validation, then 45–90 minutes for independent cold reconstruction and comparison. These are planning estimates, not worker promises or a completion deadline; material unknowns include generated dist provenance and absent complete recipes. At first gap-audit checkpoint the owner returns an actual estimate and either the complete bounded continuation or an explicit remaining gap, never a reduced denominator.

## Inputs and custody

Use adjacent MANIFEST.md, COOKBOOK-PR-RECEIPTS.json and TRACKED-FILE-ORACLE.json. Manifest pins cookbook main/design-system and PR31/36/34/35/37 independently; none is silently treated as merged. Use existing kitchen CROSSWALK-2026-09-16-app-rebuildability.md, RECONCILIATION, PENDING, journal rows k0167 (postshipment deferral) onward through release acceptance, app26 commit/review/check history, and the exact included source PRs. Read only relevant claims and all pagination for selected discussions. Never make journal observations normative merely by copying them.

Audit author can inspect the target app to explain gaps. Cold builder receives only a sealed cookbook commit, dependency manifest and synthetic acceptance inputs. It must not clone/read the app repository or copy its archive, use existing app node_modules/output, or receive the byte oracle before sealing its output. Independent comparer holds the oracle and may read app evidence afterward. Maintained cookbook source modules/templates are permissible only as explicit reviewed source of truth with recipe and provenance, not an opaque full-app archive disguised as reconstruction.

## Ordered work and exact outputs

1. **Evidence and gap audit.** Extend existing crosswalk to all 140 oracle paths; retain old statuses and add this release column. For each path record cookbook locator+immutable revision, actual producing recipe/tool/input, selected normative source, app provenance, and status reproducible / partial / missing / external input. Keep observed defects separate from accepted intent. Trace five shared-link operations, Auth RATE_LIMITED and email-consume CAS correction, UI recovery/storage and R15 semantics. Distinguish tree equivalence/cherry-picks from literal ancestry. Identify dist versus deployed bundle provenance rather than assuming tracked dist is current.
2. **Maintained cookbook completion.** On one isolated cookbook branch based on current accepted cookbook integration base (exact pin recorded before authoring), incorporate only missing release-relevant accepted content from frozen paired PRs. Proposed release folder `planning/2026-09-16-reproducible-build/releases/a57ba930/` contains `MANIFEST.json`, `PROVENANCE.tsv`, `OUTPUTS.tsv`, `DEPENDENCIES.json`, `REBUILD.md`, `ACCEPTANCE.md`, `GAPS.md`, and `rebuild.py` plus explicitly owned source/templates as necessary. Amend existing REPROJECTION.md by dated supersession pointer: its older semantic-only equivalence language does not erase the newer 100%-tracked-path reconstruction obligation. Existing normative API/domain/design docs get substantive corrections by pointer, not another docs-only wrapper. API contract generator's historical path heuristics must not erase accepted five-row/shared-link/Auth deltas. Source-path and recipe changes receive independent semantic review.
3. **Pin executable recipe.** Author must make `python3 rebuild.py --manifest MANIFEST.json --output <empty-output>` the documented entry point, with no access to app origin. This is a specified output to implement later, not a claim an executable exists today. Pin OS/architecture, Node/npm/Python exact versions, package-lock and external source digests, locale/TZ and commands. Start from observed successful deployment Node24.18.0/npm10.9.2, lock-pinned Wrangler4.133.0/Vitest2.1.9; Pin Python3.11.2 (previously observed local runtime), with actual author/cold-builder availability verified before their FIRE; this is not a claim it was used by the deployment. Record registry dependencies and source f042cde retrieval exactly. No secrets/private transcripts/real responses. Installation uses npm ci, never npm install to revise lock. Fresh local database gets explicit 0001→0002→0003→0004→0006→0007 once; no0005. This local clean initialization is not the remote additive rollout procedure. Seeds only locally, in reviewed dependency order. Recipe must say which artifacts are generated and which maintained source documents are projected verbatim, with no app checkout fallback.
4. **Independent clean reconstruction.** Separate clean directory/process and isolated local DB; record provided inputs and network/source access. Run sealed recipe, save stdout/stderr and environment digests, seal output hashes before revealing oracle. Compare exact relative path set, file modes and bytes for all 140 files, plus explain any extras outside output. No silently ignored dist/map/docs files or normalized differences. Missing/nonmatching files produce a failed exact-reconstruction claim and specific recipe correction. Behavioral equivalence alone is separately reported and cannot silently waive the all-file obligation. If evidence shows an irreproducible historic artifact, return that precise exception to root for explicit disposition; do not rewrite the shipped target.
5. **Behavior and operations gates.** On reconstructed output run npm ci, npm run typecheck, npm test (shipped build observed 25 files/129 tests); run `node --test ui/language.test.mjs ui/participant-dom.test.mjs ui/participant-resume.test.mjs ui/present.test.mjs ui/visibility.test.mjs` and synthetic local browser collection journey. Acceptance: provisioned staff selects survey and shares link; two independent anonymous browser contexts submit distinct responses; isolated drafts/tokens/receipts survive reload; idempotent submit and conflict/error recovery reproduce accepted frozen outcomes; revoke/expiry/closed collection and cross-survey isolation enforce denial; counts increase exactly two; Auth replay/CAS and scope/rate limits remain enforced. Existing shipped acceptance receipts are baseline evidence, not proof rebuilt code passes. No live email, remote mutation or production deployment. Validate generated capabilities/OpenAPI/manifest content hashes and operation union, both migrations' exact hashes and config names/bindings without applying remote config.
6. **Return through existing queue.** Author hands exact cookbook commit, command transcript, full output ledger and remaining gaps to independent builder; reviewer returns exact package/output hashes and PASS/AMEND per gate. Root records accepted cookbook source and independent reconstruction receipt against a57ba930, then pulls the next shipped release only. In-progress next sprint is not a new target. Any recipe fix invalidates affected outputs/tests; repeat cold construction for final sealed package before whole-release claim.

## Seven observable completion criteria

1. All 140 paths have explicit provenance and producing inputs; zero unexplained exclusions.
2. Every selected claim links to authority/review and preserves supersession/known defects, including exact source lineage and paired cookbook disposition.
3. Maintained cookbook contains an executable, version-pinned recipe and sufficient owned inputs; no app archive/checkout fallback.
4. Independent sealed cold build matches every target path/mode/byte, or returns an explicit unmet gate rather than a success claim.
5. Contract/schema/security checks and local behavior oracles pass with exact receipts on reconstructed output.
6. Production untouched; no secrets/real participant data enter cargo; local versus remote migration semantics remain distinct.
7. Root accepts the result and existing queue/journal points to immutable package, evidence and next pull condition; full-app residuals remain visible.

## Failure responses and release boundary

Unavailable historical evidence: record locator, retrieval failure and affected claim; do not substitute latest head. Divergent cookbook proposals: root routes deciding owner, author continues unrelated mappings. Tool/runtime mismatch: freeze actual versions and retry reproducibly, never alter shipped lock invisibly. Byte mismatch: diagnose producer and correct recipe; no target edit. Security/behavior regression: AMEND recipe/spec, no deployment. Privacy concern: keep only disclosure-safe source IDs. Lost historical acceptance locator does not revoke already accepted DEV release; it is an audit provenance gap. Actual test-window cleanup remains root/Auditor custody and does not block offline planning.

## Driver-seat delta and learning

Applied current driver-seat lens as future cold builder: added sealed-input/output separation, all-path denominator, command entry point, exact local migration order, explicit generated-dist investigation, source-equivalence versus ancestry distinction, and one-release pull rule. Rejected app archive replay (proves copying), prose-only wrapper (no executable sufficiency), reopening every full-product requirement (changes release target), ten parallel rebuilders (excess inventory), and treating provider health source_sha as app SHA (wrong identity). Existing provider Git/build/version chain supplies release identity; no new runtime buildstamp feature required.

Triple-loop follow-through records: immediate errors (truncated supplier diff caused wrong file count; premature inventory count corrected to140), producing mechanism (summaries/titles substituted for complete measured data), learning change (save complete machine receipts then report measured values; semantic independent review remains required). Do not promote local lessons to shared canon without separate review.

## Pre-execution gates

Root lands this amendment in existing ticket; actual independent plan challenge/ACCEPT required. Actual author and independent validator ACK scope, custody and budgets before FIRE. Audit and recipe authoring may then start without product work; cold builder pulls only a sealed reviewed package. Budget/scope changes return to root. No provider permission, new environment or next-product decision is needed for this isolated local reconstruction. This plan currently has preflight FOUND at06:01:45Z from default canon, not project-specific approval; independent challenge is pending.

## Governing direction received during planning — cookbook defines the version

Root's recorded user ruling cookbook16c5709713415 / kitchen e39b1e3 k0206 supersedes any implication that app remains the ongoing source of product authority. This reverse audit is a transition: recover the shipped increment into reviewed cookbook source, prove reconstruction, then subsequent app releases build against the cookbook-defined product version and exact cookbook commit. The current incomplete cookbook is not yet proven authority for the whole app.

Add `VERSION.md` and `CHANGELOG.md` in the release folder, with canonical product release identifier, scope, cookbook commit, schema/contract revisions, app build receipt, known residuals and previous-release link. The author proposes the identifier using existing cookbook conventions; root accepts the exact value before publication, rather than this plan inventing a version. Store source version in cookbook first; final commit identity is recorded in release/build evidence without self-referential embedded commit hashing. Map a57ba930 as the audited legacy app target; do not falsely claim it was originally built from the new cookbook commit.

Exit from reverse reconciliation requires the independently reviewed maintained recipe, complete140-file comparison and behavior gates above, plus a reviewed canonical version/changelog and pinned cookbook source. Forward releases select that cookbook version/commit before app generation and record resulting app/build/version receipts. An explicitly staged release can expose a version badge before whole-reconstruction proof, but must label its actual authority scope and cannot use badge completion as proof of whole-app reconstruction. This planning work does not require completing reconstruction inside the next version-badge sprint or delay its separate accepted bounded scope.

## Independent challenge amendment — known defects and blind custody

Reviewer testing_plan_review identified two concrete scope corrections, now incorporated. Behavioral comparison preserves frozen known defects: raw CODE text, misleading direct-submit “not submitted” copy, missing staff revoke UI and separate respondent count. These are negative observations explicitly reproduced/documented, not desirable normative acceptance or fixes in this reconstruction. Security invariants and accepted recovery outcomes still pass their frozen tests; newer desired product behavior remains a separate sprint. Byte comparison and residual correction therefore never conflict.

The cold builder must start in a fresh context without inherited target app contents or oracle. Existing reviewer testing_plan_review remains a qualified independent comparer/plan reviewer but is ineligible as blind builder because it has inspected source. Auggie obtains the fresh worker's actual ACK and access declaration before execution; no currently named worker is falsely called blind. Final blind run receives only sealed cookbook/dependency/acceptance inputs. Any accidental target/oracle exposure invalidates that blind run and requires a fresh context before the claim.


## Superseded acceptance — September17,02:25ET

klappy rejects verbatimcode reconstruction/sourceadoption. The140path/mode/byteidentity and historicaldist obligations above are superseded. Do notexecute them. Governing correction is ../../COOKBOOK-SEMANTIC-AUTHORITY-2026-09-17.md and cookbook13c5709943136. Independentconformance must beagainst agreedrequirements/specifications/contracts/schemas/security/sourcebehavior/design, notoldrepositorybytes. Historicalaudit remains evidence, not normativecode.


Correction to the supersession pointer above: the governing file is ../COOKBOOK-SEMANTIC-AUTHORITY-2026-09-17.md (one directory up), not ../../. Useragreement/specification conformance supersedes file/byte replication.
