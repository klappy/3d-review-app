# September17 B2 amendment to existing report-entry ticket — proposed

What this is: Implement the local immutable synthetic report storage and disclosure boundary, with a deliberately disabled production renderer.
Why now: B1 complete capture is independently accepted; the next constraint is guarded materialization/read/list, not more capture planning.
Your move: Root independently reviews this bounded execution delta, lands/readbacks the exact packet and decides FIRE.

Class: entrée. Risk: STANDARD, local synthetic fixtures and reversible unwired code; no real data access, public route, provider or irreversible remote action. Station: subagent. Owner /root/queue_resolution/a8_inert_author; coordinator /root/release_reconciliation. Promise: actual revised remaining3–4active hours from B2 START, first measured checkpoint30; includes all own tests because no separate test author ACK exists. This does not erase original overall3–4h history: B1 consumed about6active minutes, then estimate was revised with sole test custody. No08:00 promise. Depends: accepted B1ceeba, A3523, Bdesign54e05455 and root acceptance of this local-only delta/seam.

## Exact inputs, custody and scope

Start isolated existing /tmp/3d-materialization-author at ceeba19635421a70e84903b17fd4e1494eb9b5f7, parent3523f20ae5807a0f1d4a047352b62c3aa6d2dd82. B1 independent ACCEPT /tmp/3d-b1-independent-review/INDEPENDENT-REVIEW.md SHA7dc72ad2fd07cdb408355b762d3202c1af4f057d2d8f4ae1004bf61079f5fdef. Root must reconcile later integrationbase separately; no rebase/shared push without disposition.

Accepted parent PLAN54e05455, CTE9f41f868, QUERY93273676 and DDL2ad9a489 are full hashes in parent PLAN-HASHES.json. B1 table-free capture SQL8e3f51e2 and its public internal adapter behavior must remain equivalent. B2 restores the reviewed report-ID target only in store queries once the local0008table exists; do not make B1 capture require the new table. Share invariant predicates/packing through one author/producer, preserve regression proof of table-free capture.

Exactly six editable app paths: src/report-capture.ts; src/synthetic-report-store.ts; test/synthetic-report-boundary.test.ts; test/synthetic-report-materialization.test.ts; docs/synthetic-report-boundary.md; migrations/0008_synthetic_report.sql. Auth actual5710224924 reserves0008 additive only; no0005 replay or0006/7 rewrites. Tests list applied migration by name. Local disposable D1 only. No A, Auth, UI, handler/registry/contract, package, seed, provider or production changes.

## Exact private renderer seam

Production store has one private compiled renderer descriptor constant, initially null. No exported setter/factory, mutable runtime registry, public caller-supplied rendered JSON or approved-product escape hatch. Production build/read/list fails closed while null. Actual source renderer installation is a later C reviewed code change, not a configuration switch authorized here.

Tests bundle the same store source using existing esbuild with exactly one asserted substitution of that private null constant for the descriptor defined in PRIVATE-MARKER-CONTRACT.json. Marker implementation and transform live only in test/synthetic-report-materialization.test.ts and temporary test output. Assert exactly one replacement, retain source/diff proof that no other code is changed, and independently test the unmodified production module refuses all three operations. No package changes; missing existing tool capability returns a concrete AMEND.

The substituted descriptor has pinned tuple, strict payload validator and deterministic renderer over only captured assessmentId/digest/sorted responseIDs. Store renders internally, then validates and canonicalizes; callers do not supply arbitrary payload. Descriptor validation binds every output identity to original attested capture. A separate test bundle with scorerVersion v2 proves identity changes; no dynamic version override on an operation. Tests may directly corrupt local fixture rows to validate refusal, without exposing a production mutation API.

Because only a test marker exists, this candidate is an internal storage ingredient. Its production fail-closed result is not a report feature, and marker IDs/content can never appear through public runtime routes. The marker contains no raw answers or mutable labels. Future C must independently prove real renderer/schema/source tuple and actual worst payload size, not borrow marker measurements as release evidence.

## Runtime gate clarification proposed for root review

Current account/script standard usage_model and no limits field are observed; paid CPU entitlement remains unverified. Extend only the authoring distinction already accepted for B1: local unreachable B2 code/tests may be authored with unresolved effective account CPU allowance. This is not a waiver of actual resource acceptance. Measure local query/row/bind/serialized sizes and walltime honestly; actual Worker CPU/memory limits, real renderer/payload/page worst cases and D1 limits remain mandatory before integrated release. No remote schema/provider action follows. Root's independent acceptance of this explicit delta is required before FIRE.

## Done means and measured checkpoint

1. Reviewer can run local D1 exact5/18/5/6/7bind variants and observe capture,guardedcommit,get,build-result,list work or named failure;0008table and immutabilitytriggers match reviewedDDL.
2. Reviewer can race same-key builds and observe one immutable row/exactpayload; changed capture/grant/archive before commit refuses, with meaningful negative controls and no broad OR IGNORE.
3. Reviewer can inspect stored/current attestation, source/index/version/key/payload checks and observe known-append permits frozen old output while mixed/unknown/malformed/corrupt input suppresses without ID/count leakage.
4. Reviewer can test authorizedempty list sentinel, bounded page+lookahead, wholepage corruption refusal, no total/filter leakage, and scope/support/grant/archive interleavings using single-query observations.
5. Reviewer can prove exact marker-transform isolation, all unmodified production operations refuse and no route/registry/provider changes exist; a forged raw JSON product has no callable admission path.
6. Reviewer can reproduce local byte/parameter/resource measurements and overlimit/constraint failures without truncation, then see actual platform/real-renderer release gates still open.
7. Coordinator can read first30-minute checkpoint: exact pins, localDDL/triggers, allbind preparation results, first guardedinsert+secure-read or exact failing phase, null-production proof, savedinterface/commit and revised remaining estimate. Partial work is reported as partial, not a completion percentage.

## Failure Modes — What Breaks When Storage Looks Like Public Readiness

Forged marker admission, differing test/production algorithm, changed authorization/current-input observation, incomplete capture, duplicate/corrupt identity, swallowedconstraint error, leakedheldmetadata, scope expansion, costbreach or false report shipment.

## Required Response When Detected

Stop the affected path and retain failing evidence; keep production descriptor null; require independent correction for seam/query changes; fail closed on unknown/invalid data; compare fullidentity/exactpayload on retries; propagate nonduplicate constraints; suppress full page and avoid diagnostic IDs; return custody/resource AMEND; label internal ingredient. Never relax gates, apply remoteDDL or activate routes to meet an estimate.

## Prior art and reference fidelity

CurrentB1/A actual sources and D1command evidence are reused. The accepted B SQL and independent34/425SQLite probes supplied the complete membership design. Historical d605 snapshot adapter depends on0005 and is excluded; source renderer remains separate C. Parent18-B/DELTA contains attributed house inventory/prior inspection. This amendment adds no new query architecture or cryptography and claims no fresh corpus search.
