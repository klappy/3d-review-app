# C2 driver-seat / challenge disposition

Actual Oddkit challenge 2026-09-17T07:34:46.236Z returned CHALLENGED, block_until_addressed:false, no tensions. Generic evidence/scope/reversibility prompts are not a PASS. The proposal is a scoped engineering hypothesis against exact observed B2/C1/Auth pins, not a universal principle.

Future-user simulation: a staff member loses the build response after commit, sees a changed collection, then chooses retry. The old promise of original-report recovery was wrong: convergence applies only to same capture and tuple. Revised plan explicitly shows uncertainty, no automatic retry, changed-capture new key and a test. A viewer pages after grant revocation: a valid encrypted cursor is not authority; current same-observation grant remains required. A held row must not leak through generic receipt IDs: the exact build-only empty object projection is required.

Rejected alternatives: readable HMAC token is authenticated and may carry already disclosed ID, but fails the accepted confidentiality requirement; AES-GCM keeps existing secret and dedicated domain. Hand-maintained schema validation is cheaper but risks accepting source-inconsistent values; exact canonical rerender uses accepted C1 and its cost is explicitly measured before release. No cross-request validation cache. Global dispatcher status redesign is unrelated and excluded.

Reversibility: local code can be discarded before merge; ingredient publication alone has no DB effect. Immutable report writes have no inverse, so confirmation and suppression/access remain compensating controls; remote0008 and deploy require their own reviewed gates. Disconfirmers: authority/suppression leak, unsupported raw capture, source-output mismatch, exceeded effective resource limits or custody conflict -> AMEND/HOLD, not deadline-driven relaxation.

Success criteria are the plan's exact local D1/HTTP/MCP/refusal/race/cursor/receipt tests, independent combined review, actual resource proof and separately observed DEV journey. No current plan claims those unexecuted results. Actual authors/reviewer are named; root remains expediter and sole Git journal writer.

| Six-B | Verdict | Evidence |
|---|---|---|
| Borrow | Applied | Accepted A/B1/B2/C1 exact commits and existing target/shared capture producer. |
| Bend | Applied | Private compiled descriptor and target-first projections; public API separate. |
| Break | Observed | Null descriptor and inner joins cannot implement real rendering or authorized held distinction. |
| Beget | Delegated | Actual a8 author ACK; coordinator independent candidate review. |
| Bide | Rejected for local work | Public Auth and platform gates need not serialize disjoint internal integration; release still held. |
| Build | Minimal | Four owned paths, no new dependencies, migration, routes or provider writes. |

Final scoped order challenge repeated07:37:35.160Z after the exact envelope/custody revision: CHALLENGED, blockfalse, no tensions, raw result adjacent. This does not substitute for independent review. The same evidence/scope/reversibility/success answers above govern the lexical prompts.
