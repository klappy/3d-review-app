# C2 integration — proposed, not fired

Coordinator actual START 2026-09-17 07:28:41 UTC. Existing report-entry scope; no new feature meal. This order integrates accepted synthetic-only ingredients. It does not authorize production, real-data reports, remote schema writes, provider changes, UI authorship or deployment.

## Immutable inputs and base

A 3523f20ae5807a0f1d4a047352b62c3aa6d2dd82; B1 ceeba19635421a70e84903b17fd4e1494eb9b5f7; B2 dc9e6dc4a61056048f86c0ae898a775ca64b471a; C1 3d8dce056154d7c860344c1e7a3d8175f6680381. Their independent review receipts remain mandatory attached inputs. Main observed a57ba930 at 07:31 UTC; other owners' later main changes must be rebased and reviewed at integration, not overwritten.

Proposed isolated assembly: start exact B2, cherry-pick only C1 commit (its nine additions are absent B2), verify resulting union retains every A/B1/B2/C1 path exactly, record the resulting full commit and tree. Assembly creates no remote branch until root authorizes publication. C2 author order must pin that actual assembled SHA before FIRE; an expected union is not a fabricated SHA.

Auth disposition: cookbook14c5710531174. Reservation/class disposition: 14c5710224924. Existing 0008 SQL remains exactly 2ad9a48901204c4f1a162b99bbe3ba998d9c317c8ded72a6c6e859e87419365a. No 0005 assumption, seed replay or remote DDL. Existing source-C plan 3fd619 and accepted B plan 54e05455 remain governing except explicit deltas below.

## Two local implementation dishes, one integration owner

C2a integrates the real renderer and shared observation boundary in report-capture/store, with exact payload schema validation and actual D1 tests. C2b adds the three public authenticated operation handlers, cursor and contract/receipt/policy hooks. They may be authored in disjoint files after the C2a interface is pinned; shared source/query/contract ownership remains single. Both need independent review and the final combined candidate needs HTTP/MCP acceptance. Neither partial ingredient is called a shipped reporting journey.

The normative C2a detail is STORE-RENDERER-DELTA.md SHA256 4795b21fd81f6b1e70dfd86d2f84ad442b64784f3d04125fdf7fdddcad256db1: private real descriptor; awaited exact canonical rerender validation; target-first SELECT projections; typed NOT_VISIBLE versus HELD(marker); INSERT unchanged. A8 actual availability ACK is 2–3 active hours, first measured checkpoint30, excluding review/public/platform gates. C2b author availability remains pending. Coordinator owns this plan, independent reviewer testing_plan_review challenges it; authors cannot accept their own candidate.

## Same-observation authority and suppression

Keep the exact grant, archived-assessment, complete response membership, global orphan refusal, raw duplicate-aware parsing, A attestation and per-stored-row identity verification. Server minimum is member for build/dry-run, viewer for get/list. Support requires an actual assessment grant meeting that minimum; support status alone grants nothing. Participant returns notVisible("report"); anonymous retains NOT_AUTHENTICATED before handler.

A query must distinguish no authorized target from authorized target with absent/invalid capture by selecting from the authorized target and LEFT JOINing the captured result. It returns an internal authorized marker only `{assessment_id, eligible, policy_version}` plus the separately private candidate data when available. Final eligibility includes A and stored identity validation over the bytes returned by that observation. Never fetch a second grant/eligibility observation to manufacture a held envelope. No target -> NOT_FOUND_OR_NOT_VISIBLE with message `report not found or not visible`. Authorized ineligible -> held success. Get's target itself joins the requested report to its assessment, so nonexistent report and ungranted report both have no marker.

Build retains a guarded INSERT comparing exact captured token and a fresh guarded disclosure query afterward. Current capture changes between render and commit never trigger an invisible retry/render of new inputs. Postwrite authority loss -> notVisible; authorized ineligibility -> held; eligible observation without attempted result -> generic STAGE_CONFLICT. D1 transport/error -> generic STAGE_CONFLICT with no IDs and fixed message `The report build outcome could not be confirmed. Recheck reports before choosing to retry.` This is an uncertain outcome, not proof that no write occurred. No automatic retry or rerender. Deterministic convergence preserves one row only for the SAME captured membership and approved tuple. Since public params are {aid} and evaluation is current-at-execute, changed membership after an uncertain commit can legitimately produce a new key/row on a user-chosen retry; do not promise recovery of the original across changed inputs. DETERMINISM also maps to the same non-disclosing STAGE_CONFLICT operational refusal, never success or an invented marker. Read/list transport or operational failure uses STAGE_CONFLICT with fixed message `The report request could not be completed.`; it must not claim a build or uncertain write occurred. Build CONFLICT/UNAVAILABLE/DETERMINISM uses the uncertain-outcome message above. This exact mapping needs Auth hunk review before public FIRE; no error enum member is added.

Held result (build/get): `{assessment_id, suppressed:true, status:"held", reason:"Report unavailable under the current synthetic reporting policy.", report:null, snapshot_version:null, algorithm_version:null, policy_version:"synthetic-current-assessment-asof-query-v1"}`. List uses `reports:[]` instead of `report`. No count, next cursor, report IDs, hashes, membership or cadence. Same fixed reason for all ineligibility causes. This is functional API text, not klappy's public voice.

Success build/get result: `{assessment_id,suppressed:false,report:{id,created_at,payload}}`. List: `{assessment_id,suppressed:false,reports:[{id,created_at}],next_cursor:string|null}`. Do not expose internal reportKey/captureDigest or raw capture. Payload is the exact accepted C1 schema; no new recommendations/narrative or mutable display names. All page plus lookahead rows pass current and original attestation and identity checks before ANY output; one invalid row suppresses the whole page, not filtering or a count.

## Confirmation, policy and receipts

Build params exactly `{aid}`; get `{id}`; list `{aid,cursor?}`. IDs are nonempty bounded UTF-8 strings <=256 bytes; reject unknown fields; no caller minimum/page size/renderer/version/payload/suppression switch. HTTP path values remain authoritative and MCP uses the same dispatcher.

Add targetScope mapping for report.build and report.list -> assessment:aid, so build confirm intent binds assessment as well as existing actor/params/revision/300s expiry. Do not make the dispatcher perform a separate report grant query: after anonymous handling, a narrow three-report branch checks principal kind, then delegates all grant/role/existence authority to the shared authoritative query. This branch must precede generic support/participant/role handling. Report.get scope is obtained from its authoritative observation; it is read-only and does not mint a confirmation.

Dry-run performs capture/attestation only, no materialization/receipt. Eligible dry-run result is exactly `{assessment_id,suppressed:false,status:"ready",report:null}`; ineligible uses the held result above. Neither includes report IDs or memberships; impact uses the existing typed fields `{affected:[],irreversible:true,effect:"disclosure",compensating_control:"cap.report.get suppression / access control; a built report is never unsent"}`. Confirm token authorizes current-at-execute evaluation, not the prior captured cohort. Existing token verification precedes handler execution. Execute may still become held after dry-run. Confirmation lifetime/secret/domain remain existing platform semantics.

Receipt persistence special-cases only cap.report.build: `result_ids={}`; prior/params already project to empty objects and remain so. No report identifier/digest/cursor/payload enters receipt, spans or trace data. No undo/recovery locator; inverse none. Platform receipt ID/actor/assessment scope remains the existing contract. Add an explicit regression through persistReceipt, not merely handler-output inspection.

## Cursor: confidential authenticated keyset token

Existing B2 order is immutable random report ID binary ascending. Use last disclosed ID as internal keyset key, fixed five rows plus one validated lookahead. Do not introduce time ordering, total count or snapshot rowcount.

Auth's HMAC-readable JSON would reveal the already disclosed last ID, not automatically a held ID; it nevertheless conflicts with the accepted confidential-cursor contract and Auth's literal never-report-ID wording. The explicit superseding engineering proposal is authenticated encryption with existing SESSION_SECRET, not a new secret or external service. WebCrypto HKDF-SHA256 derives AES-256-GCM key using fixed salt `3d-report-cursor-v1` and info `report-list.v1`; random 12-byte nonce, 128-bit tag, AAD `report-list.v1`. Token `cur1_<base64url(nonce || ciphertext-and-tag)>`, canonical exact plaintext `{v:1,actor,assessmentId,sourcePin,indexRoot,scorerVersion,narrativeVersion,policyVersion,outputSchemaVersion,afterId,exp}`. No raw data/labels/counts. Bind exact current actor/assessment/approved tuple; integer epoch expiry now+300 seconds; reject expired, excessive future, unknown/missing fields, malformed nonce/tag, wrong secret/domain/actor/assessment/tuple. Bound token to 4096 bytes before decoding. Never log it. Cursor validation grants no authority: same-observation query always follows. Failures return INVALID_PARAMS; valid cursor with lost authority returns notVisible. Secret rotation invalidates old cursors safely. New eligible reports may appear by key ordering; this is keyset traversal of the live immutable set, not a snapshot completeness promise.

No global report status gate redesign: change only the three rows' slice/status/handler registration together. Keep public:false. Auth owns review of policy/receipt/contracts, not competing authorship. Other reserved capabilities stay reserved.

## Exact proposed file custody

C2a exactly four paths: `src/synthetic-report-store.ts`, `test/synthetic-report-materialization.test.ts`, NEW `test/synthetic-report-integration.test.ts`, `docs/synthetic-report-boundary.md`. No shared CTE/B1/A/C1 helper or migration edits. No separate schema module: exact canonical comparison against a fresh trusted C1 render of the original attested capture is the full validator; async call sites must all await it. Both descriptor type and call sites are in store.ts. No cache initially; resource proof includes all six original renders plus current attestation. C2a should return a stable typed NOT_VISIBLE/HELD/UNAVAILABLE/CONFLICT/DETERMINISM distinction; no handler reaches into SQL rows.

C2b: NEW `src/handlers/report.ts`, NEW `src/report-cursor.ts`, NEW `test/report-public.test.ts`, NEW `test/report-cursor.test.ts`; narrow changes to `src/handlers/index.ts`, `src/policy.ts`, `src/receipt.ts`, `contract/capabilities.json`, `contract/openapi.yaml`; NEW `docs/synthetic-report-api.md`. No `src/dispatch.ts`, UI, docs-search or release identity edits intended. Any necessity outside these paths is a concrete AMEND before editing. Contract generator must not be run: it would overwrite accepted schema amendments. Manifest cannot make an unsupported whole-contract generator hash claim.

Contract rows: build becomes write.effect/danger/inverse:none with accepted compensating control; get/list remain read; all public:false. Update exact three operation schemas/security (session/bearer, no participant), success/held/error results and danger body; total83 and prior accepted operation schemas preserved. Counts must derive from actual final contract union, not assume intervening richer-feedback work added no row. Integration with concurrent Auth/Grok contract changes is root-owned and must preserve both reviewed deltas.

## Required candidate proof and resource gates

Local candidate: exact union baseline and owned diff; 34/425 real C1 render→store→get/list; independently fixed version/key/payload hash vectors; real schema unknown/missing/type/null/oversize refusals; current/original attestation and tampered metadata/payload refusal; same-key concurrent builds and transport-loss retry with unchanged capture; changed capture after uncertain commit explicitly yields a new key rather than a false original-recovery promise; no partial insert; role/grant/archive/current mixed append and before/after observation barriers; held/unknown/participant/support cases; empty page, six-row lookahead and corrupt lookahead; cursor tamper/domain/secret/actor/assessment/version/expiry/replay with revoked grant; HTTP/MCP identical envelopes and wrong tool/dry-run/confirm/execute, scope binding, no receipt IDs/traces; nonreport regression tests and src typecheck. Existing source comparison remains required after actual integration; prior isolated PASS is not substituted for combined proof.

Resources: record runtime/config/entitlement evidence supplied by root. Local authoring can proceed under existing explicit exception; release cannot. Measure actual effective Worker CPU and memory, response/transport sizes, D1 query/bind/row limits for all34 valid C payloads, worst admitted capture and five+lookahead pages, and overlimit/corrupt refusal. Preserve observed malformed row 1,111,298 raw field bytes / 2,172,407 JSON transport bytes; do not relabel it below a transport limit. Query projection should refuse oversize stored fields before returning them when that can preserve whole-page suppression; any bound change receives independent review. Valid worst-case fixture must be A-accepted, not merely fill a string with arbitrary bytes. No paid entitlement assumed from usage_model standard; actual provider allowance remains unresolved at this draft.

Before DEV release: independent combined review, exact-head checks, Auth hunk disposition, Design API/UI contract acceptance, actual resource limits/margins, separately reviewed DEV-only 0008 additive rollout with data-preservation/rollback and explicit FIRE, Git-only deployment and observed version, real authenticated synthetic journey. Local tests cannot prove remote migration execution. Root alone selects/fires rollout and merge; production excluded.

## Immediate dispositions / no human fork inferred

1. Independent review of confidential cursor correction and report-specific dispatcher authority path.
2. Root/Auth acknowledgement of exact held reason/error mapping and report-only shared-file custody; existing policy decisions are preserved.
3. Actual C2a/C2b author ACKs, measured first checkpoint, accepted assembled base, and own candidate reviewers before FIRE. No new implementation is ordered by this document.
4. Provider entitlement/runtime acceptance remains a release dependency under root, not a reason to leave safe local integration planning idle.
