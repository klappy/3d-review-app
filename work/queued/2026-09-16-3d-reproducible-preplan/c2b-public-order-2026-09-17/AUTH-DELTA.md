# C2b exact Auth condition incorporation — public order only

Actual Auth ACK-with-conditions: cookbook14c5710852889, 2026-09-17 07:44:58 UTC, read against main299f825. Full text AUTH-ACK-5710852889.md. This delta supersedes only the specified C2b public details of accepted plan9778158b; fired C2a four-file order/base/semantics remain unchanged. No implementation yet.

1. In authorize, after anonymous throw and before generic support/participant/role handling, match exactly the three report capability IDs. Reject non-user/non-support as notVisible("report"); **return** for user/support, without a query. Thus generic roleAt SELECT is unreachable for these rows. Do not bypass handler's real store authority. Keep build roles exactly `O, M`, get/list `Vw+`, all public:false; never V/any. targetScope maps build/list aid only, get remains null. Nonreport behavior unchanged.

2. Cursor HKDF IKM is UTF8 raw nonempty SESSION_SECRET, not code-escrow's base64-decoded keyBytes. Empty/absent/nonstring secret fails closed. Existing fixed salt/info remain `3d-report-cursor-v1` / `report-list.v1`; AES-GCM nonce12 bytes/tag128 bits. Replace fixed-domain-only AAD with UTF8 JSON.stringify(["report-list.v1", expectedActor, expectedAssessmentId]); verify plaintext actor/aid as well. Declare independent `REPORT_CURSOR_TTL_SECONDS=300`, not imported confirmation TTL. Every token/config/decryption/shape/binding/expiry refusal maps to INVALID_PARAMS with exactly `Invalid report cursor.` and no varying hint/docs/data. Enforce UTF8 token4096 bound before decode, canonical base64url/no invalid alphabet, nonce/tag minimum, exact plaintext fields, integer exp and now<exp<=now+300. No key/secret/token logging.

3. Add narrowly `cursor` and `afterId` coverage to receipt.ts existing REDACT_KEYS, plus actual tests that report paths never put cursor/afterId into span/log/error/receipt data. Do not change unrelated redaction keys or receipt schema. The raw encrypted cursor necessarily appears in an eligible list response's next_cursor; successfully disclosed report IDs necessarily appear in eligible get/build/list. Auth's blanket no-response-ID sentence is applied to **held output, raw cursor plaintext, diagnostic/error/log/receipt fields and non-disclosed lookahead**, consistent with accepted public functionality; it cannot mean deleting eligible report IDs. This interpretation is explicitly presented to root, not silently treated as literal compliance.

4. Both eligible AND held dry-run return the typed impact block. Handler result shape is then augmented by unchanged dispatcher fields impact/confirm_token/expires_in; OpenAPI/MCP schemas include those fields. This is not an extra store write or receipt. Dry-run transport failure uses neutral request message, never execute's uncertain-write message. Only execute build operational failures use the exact uncertain-outcome message. UNAVAILABLE/CONFLICT/DETERMINISM stay internal discriminants, never public code/hint/docs/contract errors. Persist actual build receipt state `prior:null, params:{}, result_ids:{}`; add no report entry to PARAM_FIELDS or PRIOR_FIELDS. Preserve unrelated omitted/null semantics. No report IDs/digest/payload/cursor in receipts/traces.

5. At explicit union base counts are total83, read29, write.effect7, write.reversible40, write.dangerous7, v2.0-bcs75, v2.1-oct8. Hand-edit only the three report rows: build effect/danger, all slice v2.0-bcs with status `implemented-synthetic-only`, unchanged roles/public:false, accepted inverse control `cap.report.get suppression / access control; a built report is never unsent`. Counts become total83/read29/effect8/reversible39/dangerous7/v2.0-bcs78/v2.1-oct5; unrelated metadata preserved. Actual later contract union may include another owner's row: recompute rather than overwrite their counts or changes. Keep five named recommendation/rollup/import operations reserved and test501. No whole generator or manifest claim.

6. Existing CAP_LIMITER has no report.build-specific entry. Record this in resource/security evidence: authenticated HTTP work currently lacks a report-specific per-principal limiter beyond existing applicable platform/MCP ceilings. No limiter addition or rate invented in this ten-path order. This is a known release-risk disposition for root against actual CPU/transport measurement; not a claim of safe throughput or an extra pre-authoring hold. If a limiter is needed, its exact policy/path requires a scoped reviewed amendment.

Mandatory tests incorporate all Auth requirements: branch returns/no grant SELECT outside store; exact support/participant404; actor+aid AAD and secret rotation; unique96-bit nonces observed in generated samples (not a mathematical uniqueness guarantee); invalid cursor fixed error including ownTTL; held preview impact and no report writes/receipt; dispatcher augmentation; actual receipt projections; redaction and no token logs; class/slice counts; five reserved neighbors; HTTP/MCP equivalent envelopes after normalizing trace/receipt identifiers; same-capture vs changed-capture uncertain retry. Raw byte-identical envelopes cannot be promised across independently generated trace IDs, so compare exact semantic fields with those expected nondeterministic platform fields explicitly normalized.

No new human-only policy fork. Root owns the narrow interpretation of Auth's overbroad no-ID sentence and final FIRE. Independent delta review is required before the order is marked ready; no local schema or provider action is implied.
