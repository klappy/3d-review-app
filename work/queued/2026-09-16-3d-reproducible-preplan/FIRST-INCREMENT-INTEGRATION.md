# First collection increment — minimum integration map

Read-only live PR/source inventory2026-09-17 about00:27–00:30ET. No branches changed, merges or test runs. Goal remains staff setup/select/open collection/share one link → independent anonymous responses → staff counts.01:00 is a target without a supported completion estimate.

## Required spine and candidates that can wait

| Candidate | First-increment disposition | Actual dependency / custody |
|---|---|---|
|App3,10f5f444|Required baseline ancestry, not an extra feature merge|Existing staff setup, survey selection/stage/count handlers, gold templates and UI. Main population still needs full exact delivery-candidate review.|
|App25,a3defc04|Use accepted Auth stitch as security base|Contains12 c4321f8 +15 1ff4838 +19 745bd85 +20 4b3d7cc. GitHub compares each→25 show ahead,0behind. Do not apply all those PRs again. Auth owns candidate and security conflicts; preview label currently says not-for-merge.|
|App24,fc0bbc36|Required shared-link backend|Five contract operations, handlers, additive0007, flow/concurrency proof; cookbook36 7cbeb26a. Await independent exact-head acceptance/checks; API-only proof isn't integrated proof.|
|App23,ed77f026|Required collection browser slice|Design owns actual UI worker and integration with24. Five files observed: four planned UI/test paths plus DEV fixture. Fixture must remain isolated and cannot establish real API behavior.|
|App22,78d00915 +cookbook35 ecab91a|Required safe delivery documentation/topology, separate from collection runtime|Order B changes settings only, no preliminary phase0merge; do not import older executable Wrangler settings when resolving its comments against Auth25.|
|App18,cf143f36|Recovery overlap must be reconciled; entire old candidate is not automatically a prerequisite|Design owns ui/app.js conflict with23. Preserve required failure/reload/privacy invariants; if taking18, retain its helper/tests/.assetsignore. Do not blindly overwrite23 or claim18 already included.|
|App17,cc88f369|Can defer as a package; cannot replace required collection evidence|Partial refusal/acceptance sweep, not full parity. Current fixtures call old issue_link semantics and need danger preview/execute if integrated. Defer whole package rather than silently weaken assertions. Auditor's actual slice oracle remains mandatory.|
|App14,436f9456|Defer extra synthetic coverage|Existing selected gold instrument templates suffice for two new source-valid responses. Adds package/index/seed overlap; no bulk seed insert needed for collection proof.|
|App16,6150483e|Defer mail adapter|Copying a shared link does not depend on send_links or outgoing email; existing staff authorization must work. Includes older rate-limit artifacts—would duplicate/conflict with25.|
|Report0005 / report branch|Defer for this collection-only slice unless existing integration base already includes it|Counts use existing response table/status, not report snapshots. Preserve0005 reservation; apply it only if present in chosen base. Never renumber0006/0007 to fill a gap.|

Auth12/15/19/20 remain meaningful source/review receipts; they are not four independent mandatory merge operations once accepted25 ancestry is chosen. Any later Auth25 correction replaces the integration pin and needs a delta review.

## One combined branch, existing owners

Recommend root name **Design's already dispatched C2 integration worker** as sole combined-branch writer under Design CoS, starting from the exact Auth-owner-accepted25 tree. This extends its existing23+24 integration assignment, not a new competing worker. Root confirms that custody change; it is proposed here, not an invented ACK. Auth retains authority over index/dispatch/auth/worker/oauth/ratelimit/types/wrangler conflicts. API author retains shared-link contract/handler/migration hunks. Auditor independently verifies behavior on the resulting exact SHA. Root owns integration/promotion disposition and journals.

Minimal assembly: accepted Auth25 tree → owned24 delta → owned23 delta → resolve only relevant18 recovery overlap under Design → comment/document-only22 delta for delivery documentation. Preserve original PR branches. This can be prepared entirely isolated while main/phase0 remain frozen. No need to wait for all open PRs or all design-kit PRs; per-sprint accepted contract/state coverage governs.

## Integration hazards requiring named-owner resolution

- **Contract union:**25 includes12's RATE_LIMITED contract/error/projection updates;24 was based on10f5. Preserve upstream limiter additions while applying only24's five operation changes. No wholesale baseline contract-file replacement or stale generator run. API author reviews final contract diff; Auth reviews preserved limiter semantics.
- **Auth/runtime:**25's main entry is worker.ts and owns index/dispatch/type/limiter policy. Keep token shape `pt_`+32 and current limiter namespace union (HTTP30105/30205, MCP ceiling30104/30204). UI public open omits staff credentials; later requests use participant bearer. Check actual HTTP and authorized MCP faces under this wrapper; don't infer MCP compatibility from API's bare-index tests.
- **Migration/test harness:** chosen25base has0001–0004+0006, not0005. Add0007 and ensure every integrated test database that calls changed handlers loads it. Preserve24's additional code-escrow test migration line. If reportbase later joins, include0005 by filename. No remote migration implied.
- **UI recovery:**23 remains onbaseline participant-resume.js, so18's recovery helpers are absent. Source observation:23 strips surveyfragment before entry but bootstrap chooses shared mode only from location.hash; reload continuation must be proven/corrected by current Design reviewer. Existing Recover path writes globalresponseKey; verify no shared-mode contamination. These are source concerns, not a new test run or independent completion claim.
- **Scope/assets:**23's extra DEV fixture is explicit in its PR; verify it is outside shipped assets and fixture endpoints cannot satisfy real integration checks.18's .assetsignore addresses old DOM test exposure and must not be accidentally discarded if those files enter candidate.
- **Topology:**22 only changes Wrangler comments; keep25's executable OAuth/KV/rate-limit config. Order B ends with mainempty; collection integration and normal protected DEV delivery are separately authorized.

## Paired essential evidence and final acceptance checklist

- Cookbook31 cbf43898: accepted Auth18-D/18-I and stitch semantic follow-up; cookbook36 7cbeb26a: current capability matrix/API contract/shared-link semantics. Preserve both, no full reconstruction projector before shipment.
- Cookbook34 eeba9392: Auditor-owned collection acceptance rows34–50/J11 correction work. Auditor is actual writer; do not duplicate its test/spec implementation.
- UI copy source branch cos/c2-shared-link-copy2fcf3538 changes only design-system/copy.md relative todesign-system. Design owns current pairing and accepted state/copy updates; broader28/30/32 library work is not an automatic all-PR merge prerequisite.
- On one final combined SHA, owners run the existing full relevant suite/typecheck plus their integrated security/migration cases; no acceptance by adding historical test counts together. Design/Auditor run real two-browser HTTP/D1 workflow with exact gold-source answers, private resume/draft/retry/receipt, revoked/expired/closed refusals, staff count+2 and accessible recovery. Fixture-only results remain attributed as fixtures.
- Independent reviewer inspects exact combined diff, conflict resolutions, essential docs and actual browser/API receipts. Refresh literal real BugbotSUCCESS/all attached terminal checks and finding dispositions on that SHA. Separate root prospective delivery authority; preserve state/config; normal protected main merge only after topology readbacks and applicable freeze release. Production is excluded.

Next coordination move: obtain Auth's exact accepted stitch-head release to Design, amend existing Design integration custody to use that base, and let Auditor target that one combined SHA. These handoffs—not unrelated PR closure—are the working dependency chain. No completion time is inferred from parallel author availability.

## Cookbook reconciliation one sprint behind

User requests reconciliation run in parallel with the next sprint against a frozen shipped release, avoiding a moving target. Root will record the ruling. Essential contracts, migration/configuration/release instructions remain pre-shipment; full100% reconstruction and clean-room validation follow shipment.

At shipment, attach one frozen handoff to existing release/journal custody: exact shipped appSHA and cookbook baselineSHA; included PR/head/merge IDs; check-run IDs/conclusions and independent review IDs; exact migration filenames/checksums plus execution receipts; configuration diff and provider deployment/build/version IDs; accepted behavior results with environment/runtime and residual findings. Record source provenance and omitted/deferred candidates. Subsequent sprint commits never silently move this target; a new release gets its own pin.

Proposed ownership, pending actual ACK: existing Auggie manages the reconstruction queue and routes a canonical-doc reconstruction worker for the frozen target; a separate non-author validator performs clean-room rebuild and behavior comparison. Root retains release acceptance and exception decisions. No executor ACK, budget or background run is claimed by this proposal. Reconciliation lag is intentional sequencing, not permission to omit the release handoff or call unvalidated reconstruction complete.
