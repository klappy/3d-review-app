# 149-P1 identifiable paper — bounded print-only implementation plan
2026-09-22. READY FOR INDEPENDENT PLAN REVIEW AND EXISTING PARTICIPANT FABLE READ/CLAIM. NOT FIRED.
Superseding sequencing addendum to this existing149 home, not a duplicate issue. P11/GAP028 full design scope remains. This slice can precede safe staff sign-in return because it prints no new URL/QR. Required149 QR/access mode and invitation work remain later in the same rollout, not removed.

## Owner, source and custody
Proposed cook: current external participant Fable after actual acceptance of this new four-path custody and promise. Internal agents plan/review only. Planning reference app98ab8e9f99bb71f557acb6b55f085399f25d0477; fresh accepted integration base/head/tree explicitly bound at FIRE. Proposed isolated branch design-batch/149-paper-identity-20260922; find existing branch first. Separate subfolder149-paper under already approved3d-review-fable root. No edits to participant/K5/staff/cloud branches.
Exactly:
- ui/stage-screens.js
- ui/stage-screens.css
- ui/stage-screens.test.mjs
- test/print-identity.integration.test.mjs (new; outside assets)
These are a proposed bounded ownership extension, requiring independent/coordinator binding; prior participant seven-path claim does not grant them. No assess.js, scope.js, app.js, share.js, kit global styles, auth/backend/storage, package/release/.assetsignore or participant changes.

## Observed contract and design target
At98ab loadBlankPrint already receives request/token/aid/sid/role/lang, checks role and credential fields, fetches existing authorized /v2/assessments/:aid/surveys/:sid/print, returns visible/blank/template_id/template_version/title/items. It currently discards aid/sid. renderBlankPrint owns preview and Letter/A4 selector; printBlankForm renders an isolated direct-body blank article and removes it afterward. Retain exports/caller contract and existing scope authorization/stale controller checks.
Reference frozen c653482 design-system/ui_kits/3d-review/views-print.js and print.css: paper header, branded hierarchy, readable questions/options/answer areas/footer. Preserve Letter default and explicit A4, rather than prototype default; no sampleQR, prototype credentials, inferred languages or new project/person names. Existing language query remains honored when caller supplies it.

## Exact implementation behavior
After successful authorized load, retain exact aid/sid plus template_id/version in model, with no token/request/session copied. Validate identifiers as nonempty strings and template version per actual returned contract; never derive IDs from title. The exact readable tuple (assessment id, survey id, template id/version) is sufficient identity. A compact hash reference is OPTIONAL, never a dependency: if used, derive SHA256 of UTF8 JSON.stringify([aid,sid,template_id,template_version]), first12 lowercase hex, and label it only a disambiguation aid, not lookup/auth/collision guarantee. Unavailable hashing omits this optional aid and still prints the complete readable tuple. Invalid/missing required identifiers disable identified Print with actionable message; no wrong identity.
Every printed page: template title/version and a readable exact form identity (optional compact reference alongside it) in header; complete exact assessment/survey/template/version in wrapped footer at least8pt. Two forms with same title visibly differ. No truncation/ellipsis of exact identity, no hidden metadata substitute. Test long IDs up to the actual contract-supported bounds, record those bounds and fixture lengths. Wrap complete values without dropping below the floor; if a bounded value cannot fit one footer, use an explicit readable identity continuation/repetition layout. Do not claim arbitrary-length strings always fit. Unexpected beyond-contract payload is an actionable invalid model, not silently truncated output. Implement explicitly tested repeating layout or explicit pagination inside owned rendering/styles; do not assume CSS table/header repetition works without PDF proof.
Preview disclosure uses factual operational text: “This printout includes assessment, survey and template identifiers. Anyone with the paper can read them. No answers or access credentials are included.” This replaces inaccurate “nothing personal on the page” claim. Reviewer may refine factual wording within scope; not Chris's personal/public voice. Keep default blank form credential-free and current no-invitation-link meaning. Do not add fake QR box presented as scannable; no new link target or mint API. Existing blank optional code slot remains empty.
Questions/required/held semantics and answer areas preserve actual loaded instrument; no paraphrasing questionnaire. Print isolated paper only, never clone authenticated shell/account/header. No printing real customer fixture or distributing generated paper.

## Seven acceptance outcomes
1. Authorized model carries exact aid/sid/template/version; any optional reference is deterministic; hostile text escaped; two same-title synthetic forms differ.
2. Missing/invalid required identity prevents identified Print; hash failure alone does not with actionable message; role/refusal/unsafe credential result still fails closed.
3. Actual browser Letter and A4 PDFs, one-question and30-question multipage variants, show correct identity on EVERY page and retain complete question sequence/answer areas.
4. Long title/IDs, narrow preview, keyboard paper selector/Print and enlarged text remain usable; no overlapping footer, clipped identity/question, blank overflow page or tiny text.
5. PDF extracted text and rendered page inspection confirm no token/email/account/answer/organization/assessment display name, no authenticated chrome, no new QR/URL or write request.
6. Existing print isolation/cleanup, Letter default/A4 switch/language selection input, loaded question options and error behavior regressions remain green.
7. Four-path diff only; independently reviewed exact source/PDF evidence and current gates; no claim QR149, physical paper scan/user outcome or full batch completed.

## Executable proof and30-minute checkpoint
Run node --test ui/stage-screens.test.mjs test/print-identity.integration.test.mjs ui/stage-composition.test.mjs and affected print caller tests discovered by exact filename. Use actual loadBlankPrint/renderBlankPrint/printBlankForm and module CSS, not a hand-drawn paper fixture. Empty isolated browser, synthetic authorized GET responses and blocked unknown/nonlocal writes. Generate PDFs through real browser print media with preferCSSPageSize and explicit Letter/A4; retain page-size/count/text output and rasterized EVERY-page images. Review at least1- and30-question fixtures, two same-title identities, adversarial long values. Use existing instrument language fixtures actually available; if only one available, report it rather than invent translation. Local mocked identity transport only.
PDF rendering requires preserving temporary print surface long enough for actual browser print capture through existing onPrint hook/harness; do not change production cleanup into persistent sensitive DOM. Verify cleanup after return/error. Tests should fail on parent missing identity.
Owner's first30-minute proposed usable checkpoint: exact identity model plus one-page Letter PDF and negative credential/invalidID tests, then complete multipage/A4/accessibility in subsequent bounded sprint if needed. This is cadence, not invented duration. Before slot end return exacthead/tests/demo/gates/blocker/next task. Independently usable production increment only after all required slice proof and normal canonical/check/DEV/PROD gates, no old-version design shipment.

## Driver-seat delta and gates
Future detached-page reader needs exact form identity without accidental answering authority. Revision: preserve already authorized IDs rather than new backend fields; compact human label with full recoverable footer; explicit disclosure; paper-only staging lets disjoint worker progress while staff safe-return binds. Rejected bearerQR/automaticmint, pretending generic homepage identifies form, guessing name-derived IDs, sourceSVG/PDFscreen equivalence, shrinking unreadable footers. Distributed paper cannot be recalled, so only synthetic output in this dish.
Actual Oddkit planning challenge for this plan returned CHALLENGED, knowledge_base, block_until_addressed=false. Confidence source contract observed at98ab, proposed repeated pagination/hash presentation is working design pending realPDF; counterexamples credential leak, missing page footer or excessive density retract acceptance. No tool PASS or implementation claimed.
Before FIRE: independent review of exact four paths/disclosure/paper acceptance, existing participant Fable actual new custody ACK/promise, fresh exactbase/current claims, worker preflight/checklist and coordinator FIRE. Staff safe-return is NOT prerequisite to this no-QR paper slice; it IS prerequisite to later labelled staffQR behavior. K5 code/export remains untouched. Return source/tree/PDF hashes/test results and untested physical/user outcomes to Auggie via actual session channel.


## Narrow review amendment — optional reference and bounded layout
Responds to independent326f62ac: exact readable identity is sufficient; hashing cannot block printing. Long-value evidence uses measured actual contract bounds and an explicit overflow/continuation response, not a promise to fit arbitrary text at8pt. Same four paths/no QR; all149 later obligations preserved. This supersedes mandatory-hash language wherever inherited in this slice. Fresh independent reacceptance pending.

Actual post-amendment Oddkit planning challenge: CHALLENGED, knowledge_base, block_until_addressed=false. Hash-unavailable and contract-bound overflow counterexamples are explicitly handled above; no tool PASS or source proof inferred.
