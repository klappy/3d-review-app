# 149 print-only plan independent review — AMEND
Reviewed plan07974b94cc10797663b85edb906694a4438c5b57 against actual stage-screens.js at98ab8e9f99bb71f557acb6b55f085399f25d0477 and existing149 ticket. Planning only; no product edits or new physical/browser proof.

## One binding correction
Remove the mandatory twelve-hex SHA256 mechanism and the rule disabling otherwise accurately identified printing solely because browser hashing is unavailable. The earlier coordinator ruling explicitly retained this hash as an OPTIONAL competing presentation proposal pending identity-label prior art/owner review, not a mandatory contract. Current source already labels Form template_id@version; retain a readable title/template/version plus exact assessment/survey/template/version identification. The proposed full repeated tuple already disambiguates same-title forms without making a second derived reference an availability dependency. If a compact derived reference is later desired, justify it separately; it must not substitute for exact identity or become a new access/lookup contract. Update acceptance1/2 and checkpoint accordingly. This is a narrow simplification, not a new design round or human reapproval.

## Accepted scope and practical clarification
The four paths are disjoint from K5 app.js/legacy/adapter work and staff/auth/participant controller custody. Existing loadBlankPrint receives aid/sid/role/token/lang, role-gates owner/member, uses authorized GET, rejects unsafe credential fields, then discards aid/sid. Retaining only exact aid/sid with returned template metadata is supported; no token, request or session should enter printable models. TextContent rendering and isolated direct-body paper with finally cleanup are the actual existing contracts. Keep these exports and cleanup semantics; browser print capture must not permanently retain the temporary surface.

Print-only staging is valid before safe staff sign-in return, because it emits no new QR/access URL. Existing149 remains incomplete until its separately required QR/access/fallback work and real outcome follow-up; physical paper/scanning is not replaced by PDF evidence.

The Letter/A4 single/multipage proof, every-page exact identity, actual questionnaire text, disclosure, no credentials/account chrome, accessibility and print cleanup are appropriately concrete. Clarify the long-value requirement with a tested supported bound or an explicit readable overflow refusal; arbitrary unbounded strings cannot be guaranteed to fit every page at a fixed minimum size. Do not silently truncate exact identifiers or shrink below8pt. This is implementation boundary clarification within the same four paths.

## Actual challenge and disposition
Oddkit planning challenge ran2026-09-22T16:37:28.765Z: CHALLENGED, knowledge_base, block_until_addressed=false; not PASS. Generic prompts addressed here: confidence is high in the observed source contract, while actual pagination remains unbuilt. Existing and proposed designs share title/template identity; the proposal adds exact form-instance context. Strongest rationale for a compact hash is scanability, but no demonstrated lookup need justifies blocking accurate paper when hashing fails. Counterexamples are excessive identifier length, missing repeated footer or accidental credential propagation; any invalidates implementation acceptance. Changes are reversible before generated paper distribution; only synthetic paper is authorized. No universal principle or new vocabulary is proposed.

Return the minimal amended plan for focused acceptance. Fresh accepted base, participant Fable explicit added-custody ACK/promise, worker gates and coordinator FIRE remain pending; no implementation authorization is implied.
