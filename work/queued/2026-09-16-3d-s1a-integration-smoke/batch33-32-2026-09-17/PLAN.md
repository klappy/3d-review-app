# Integration batching proposal — existing PR33 + PR32

Planning only. Actual START2026-09-17T08:29:13Z (04:29:13 ET), bounded10–15min. Read-only GitHub metadata/files/reviews/checks and local Git object/diff reads. No union, merge, cherry-pick, branch update, push, comment, test, build or provider action performed.

## Recommendation

Prepare one isolated, reviewable **PR33 + PR32** union on accepted main `bd2f6b27eb3ced85099c525793ac79cbdea2b7e5`, using **existing PR33** as the carrier selected and owned by root. Root has named exact33a5d8155 +32/65efa77 inputs; no desktop or additional native owner ACK is a prerequisite for this integration disposition. Keep its current branch history and add exact FAQ commits through a normal history-preserving integration. Publish/check that union once, independently verify the exact resulting head/tree, then use the normal protected merge path. Root must coordinate a short main integration window so sibling merges do not invalidate the tested base while its required check runs. Native authors may continue isolated work.

**Exclude34 now.** Root's separate exact068537ec falsifier has reproduced a P2 readback response-schema defect; AMEND is required before integration. Historically admitted text objects are returned as note objects, and deliberately injected malformed typed fields also survive the read projection. This is a support-only response-contract defect, not an observed authorization bypass. Old independent acceptance/green Bugbot does not supersede this new finding. **Keep29 conditional and outside the recommended batch:** root now reports owner-frozen d0695fe and Bugbot SUCCESS08:31:24, but final independent focused acceptance is not yet evidenced. Do not let it delay33+32; including it needs a freshly pinned union disposition after that review. **Keep report28 entirely separate.** No waiting for34/29 should prevent planning/executing an independently gated33+32 batch.

Root explicitly owns the33 carrier disposition and this33+32 batch scope. No new PR or desktop ACK is required. Publication must use expected-head protection and a normal non-force update, and STOP if Design/supplier concurrently changes33 or either input; root then reorients the exact candidate. Do not disable strict-base protection or dismiss checks.

## Exact observed inputs and check state

Observed read-only around08:29–08:31UTC; these are snapshots, not future check guarantees. GitHub main ref independently resolves bd2f6b27. Root16c5711363971 separately reports its actual DEV deployment triple; this task did not query providers.

| PR | Full head | Relation to main | Observed check/review boundary |
|---|---|---|---|
|33 badge |1eeacffbad85b0c2e3e41953fe777f14f11a1ec7|Parents3d6c4e70… + bd2f6b27…; current main is ancestor|Bugbot105130886895 in_progress. All five badge blob/mode pairs independently match3d6c4e7 through refresh; source proof saved. Latest root16c5711364191 still requests owed independent33 delta; do not claim this author's byte check is that acceptance |
|32 FAQ |65efa77ba3ca1382f08e72667ac1959157c6e2f9|Merge-base299f8255526e13b17215ea1af44e3600d4b35dc2; behind main|Bugbot105125640786 completed SUCCESS; earlier Otto independent local review14c5711076678. Exact two-file scope unchanged |
|34 feedback |068537ec0cd6c28c93923b99de9e67bcde18d248|Parent299f825; behind main|Bugbot105128638263 SUCCESS; Otto35/35/typecheck and disposition14c5711306518. **New root finding holds inclusion**, independently of old green checks |
|29 recovery |d0695febec7cf467fe0dd6348c0d003558ac48ed|Merge-base299f825; parent84f67ff2…|Bugbot105131284148 in_progress; independent delta receipt app29c5711361277 ACCEPT. Owner freeze/focused Auditor completion not observed here; excluded |

Raw metadata/review/check JSON and exact ingredient patches are under sources/. `mergeable:true`/GitHub's hypothetical merge_commit_sha are not local union proof and are not used as such. Old PR body hashes differ from fresh heads; use actual Git identities above.

## File interaction and custody

33 exact six paths: `ui/.assetsignore` plus `ui/app.js`, `ui/changelog.js`, `ui/changelog.test.mjs`, `ui/index.html`, `ui/style.css` — Design custody.32 exact two paths: `src/handlers/docs.ts`, `test/docs-topics.test.ts` — native FAQ/B1 author custody. The eight paths are disjoint, and current main's twelve release-identity paths do not overlap those changes.33 already contains current main; FAQ32 was based on299f825 after docs30. Thus there is **no observed textual overlap for33+32**, but no clean-union claim is made until the authorized local assembly is actually performed.

Main31 changed release identity in platform.ts (version import and opsHealth return).34 changes the adjacent opsFeedback region in that same file. These are different logical hunks, but the old feedback patch context contains the old health literal; never whole-file replace platform.ts from34 or erase the stamp. A future34 union must preserve main's full health hunk and34's accepted feedback hunk, with actual conflict/readback proof.34 also touches registry/OpenAPI/index/receipt and the mechanical84 capability count. Those are future overlap points with report28's contract/index/receipt changes; separate reporting custody and a later exact-source reconciliation remain necessary.

29 shares ui/app.js with33. Their current hunks appear in different functions (33 button-disable exclusion,29 submit/recover), but no future clean merge or semantic compatibility is asserted. Add29 only after its actual owner freeze and independent focused correction acceptance, under a newly pinned union plan; do not grow this batch while its checks are running.

Existing governance home: `rail/1-ordered/2026-09-16-3d-s1a-integration-smoke/` for bounded integration companion, with pointers to the existing release-identity and Design33/FAQ32 orders. Existing PR33 is the suitable current-base carrier; PR32's narrow FAQ title/pen is less suitable for absorbing UI. No new meal needed. Root retains durable order/journal pen; this worker creates only this local proposal.

## Minimal executable sequence for a separate root order

1. Root names the integration executor under its existing33 carrier disposition and exact33a5d8155 +32/65efa77 freeze. Read back main=bd2f6b27 and both heads; any drift gets explicit reorientation. Preserve native ownership of original eight paths and previous review receipts. No speculative29/34.
2. Executor creates an isolated local checkout from exact33heada5d815575afaf9b6a75f7eb2a4e29692d4d6d540 (which already containsbd2f6b27), integrates exact32head65efa77 by a normal history-preserving merge. No broad cherry-pick/rebase or force update. Stop on unexpected conflict or unrelated diff; ask the actual owning lane to resolve under the order rather than inventing a hunk. Record full resulting commit/tree/parents, all eight ingredient blobs/modes, and current-main preservation outside allowlist. This has not been executed here.
3. On that exact local candidate, run typecheck with the current stamp, FAQ4/search7 tests, badge changelog tests, and release-version tests. Suitable commands are `npm run typecheck`, `npx vitest run test/docs-topics.test.ts test/docs-search.test.ts test/version-stamp.test.ts`, and `node --test ui/changelog.test.mjs`. Counts come from actual results; do not copy old totals. If repository-required full suite applies, run it once on the union, not once per constituent. No provider/UI live writes are authorized by these local checks.
4. Independent reviewer checks actual union ancestry/allowlist, preserved docs search and release metadata, FAQ byte-exact approved strings, UI badge lazy shared-route behavior and button exclusions. Review existing browser evidence and perform only a focused local interaction if the union changes those semantics or exposes unresolved evidence. A byte-identical source inheritance receipt can avoid repeating the entire participant matrix; it cannot waive a new conflicting hunk.
5. Root updates existing33 PR title/body to describe the actual badge+FAQ batch and links both ingredient reviews, under root's already stated carrier authority. Publish the exact independently reviewed candidate through a normal non-force update; wait for actual fresh head Bugbot SUCCESS and all required checks. Prior individual checks remain provenance and do not substitute for the new union head. Auto-fix or main/head movement triggers narrow owner/reviewer disposition, not automatic adoption.
6. Root performs the normal protected merge once, then verifies Git-triggered build and exact deployed SHA/version plus bounded badge/FAQ acceptance. Main remains DEV, no production or migration. Do not presume sibling32 auto-closes: verify Git ancestry/PR disposition and record supersession/closure only under root authority. One merged batch reduces strict-base refresh cycles from two to one while preserving actual gate evidence.

Actual executor/reviewer availability and budgets are still unacknowledged. Proposed preparation/testing range20–40active minutes and independent delta15–30active minutes exclude check/build queue and live acceptance; these are sizing hypotheses, not delivery promises. Root may use existing owner capacity instead. The only committed clock here is this10–15minute planning task.

## Conditional34 alternative

If the separate falsifier proves the legacy concern nonblocking, or the named author lands a narrow correction with independent exact-head acceptance and fresh required checks **before this batch is frozen**, root may choose a revised33+32+34 union. Name that exact34head, add its nine paths to the allowlist (17total), preserve main's platform.ts health update, and include all three ops-feedback tests plus dispatch/contract count validation and support-only/legacy readback evidence. Independent review must inspect the actual platform composition and receipt/registry behavior. Do not infer typed legacy read safety from35 green existing tests. If34 is not ready by freeze, ship the eligible two-PR batch and integrate34 later. This avoids making an unresolved privacy/schema question the release train's silent dependency.

## Completion of this planning task

No exact union SHA exists because no assembly was authorized here. Read-only evidence supports the bounded33+32 option; its actual execution and acceptance remain gated. The34 concern is left with root's already-running tester,29 remains with Design/Auditor, and28 remains with the report coordinator. No duplicate testing/review assignment was dispatched.

## Final pin amendment, root steering after first plan handoff

PR33 current head is **a5d815575afaf9b6a75f7eb2a4e29692d4d6d540**, atop1eeacff. Read-only Git verified the only added path/hunk is **ui/.assetsignore**, one `changelog.test.mjs` exclusion, preserved in sources/pr33-packaging-delta.patch. Root explicitly authorizes this sixth Design packaging path. Earlier table/check observations remain historical at1eeacff; the actual proposed batch has eightpaths and requires fresh exacta5d8155/union check acceptance, not inheritance of an in-progress old check. Validate this packaging exclusion locally alongside the badge test; do not publish the test file as a static asset. PR29d0695fe is now root-reported owner-frozen with BugbotSUCCESS08:31:24; final independent acceptance remains pending. Report28 and held34 remain excluded.

Confirmed34 evidence: /tmp/3d-feedback-legacy-evidence/REVIEW.md (read08:33Z), independent actual HTTP/Miniflare falsifier. Minimum proposed fail-closed typed legacy projection remains with Grok/Auth/root; this planning worker did not retest or implement it.

Final root disposition:33+32 ONLY; root owns carrier choice/custody, so desktop/native ACK is not a prerequisite. Preserve original32 independent review and require final exact33-union independent review. No newUIedits. Any observed Design/supplier head movement stops publication rather than overwriting it. Local assembly still awaits named root FIRE; no assembly was performed by this planning task.
