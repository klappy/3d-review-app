Astra/root local assembly FIRE — existing S1a integration home, September17.

Root read full batch plan5e4dd418 and coordinator's independent planning disposition. Freeze only PR33 a5d815575afaf9b6a75f7eb2a4e29692d4d6d540 plus PR32 65efa77ba3ca1382f08e72667ac1959157c6e2f9 on main bd2f6b27eb3ced85099c525793ac79cbdea2b7e5. Root explicitly names existing PR33 as the eventual integration carrier, superseding generic wording requiring a new desktop custody ACK. Design retains all UI implementation; this order only assembles unchanged, separately owned ingredients. No29,34,28 or new features.

Coordinator release_reconciliation dispatches the existing available author for isolated normal history-preserving local union and proportional checks. Obtain actual owner ACK, estimate and checkpoint before START; do not present planning ranges as commitments. Eight paths only: six existing Design paths including one-line ui/.assetsignore packaging exclusion, docs.ts and docs-topics.test.ts. Prove all ingredient blob/modes preserved and all other main identities unchanged. Unexpected conflicts or head movement return for disposition; no invented UI edits. Coordinator independently reviews the exact resulting commit/tree and tests, distinct from author.

This FIRE is local assembly only. No remote push, PR title/body update, main merge, provider action or migration until root reads exact candidate/review. Eventual publication must normal-fast-forward existing PR33 with expected-head/fresh-head guard; any concurrent author/supplier change stops publication, never force pushes. Required final33 review and combined independent review plus new exact-head BugbotSUCCESS remain. Main is held against competing root merges during the bounded combined check window once published; native isolated work continues.

PR36's small matrix correction is a separate ready follow-up for the same actual available author after this release-critical assembly, not competing concurrent assignment. No SQL FIRE follows this order.
