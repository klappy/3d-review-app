# Proposed narrow 0008 execution-window amendment — not FIRE

The current order requires global quiet and unchanged counts in all 20 existing tables. This proposal supersedes those two requirements only for the exact empty additive 0008 action; all other accepted plan, custody, source, drift, partial-failure and root FIRE gates remain. No provider action is authorized by this document.

## Source-backed safety argument

Exact SQL 2ad9a48901204c4f1a162b99bbe3ba998d9c317c8ded72a6c6e859e87419365a has only CREATE TABLE synthetic_report, its CREATE INDEX and two CREATE TRIGGER statements. It contains no existing-table ALTER, UPDATE, INSERT, DELETE, DROP, seed, ledger or copy. The table starts empty; foreign keys point to existing assessment/principal primary keys. No row backfill or existing-row scan is required by these statements. The current deployed bd2f6b27 runtime has no synthetic_report references; its exact reviewed parent/config compatibility is recorded in preflight SOURCE-COMPATIBILITY.json. A runtime source movement invalidates this argument until rechecked.

Concurrent ordinary requests may append trace/login/receipt records or perform already authorized existing-table operations. Count changes from those requests cannot establish migration data loss, and count equality cannot establish absence of it. Requiring every ordinary application request to stop is broader than this exact DDL needs. An empty referencing table does not introduce a parent-row preservation/backfill race. Provider DDL/write contention can still cause refusal or partial execution: the existing stop-and-inspect rule handles that; this proposal makes no new transaction or automatic-retry guarantee.

## Precise proposed replacement gate

1. Retain actual exclusive 0008 schema custody/no competing DDL writer, root's bounded schema window and exact SQL FIRE. Auth's actual no-competing-schema-writer ACK remains required and recorded; root must resolve any contradictory writer evidence. Root holds main merges/build-source changes for this short window. This is coordination, not a claimed technical lock.
2. Immediately re-observe exact active source/build/version, drained queues, DEV DB binding, parent key/schema definitions, absent target table/index/triggers, baseline foreign_key_check, all prior table counts and bookmark. Stop unexpected source/queue/binding/schema movement. Do not cancel builds or modify ingress.
3. Ordinary application reads and their incidental trace/session/login/receipt writes need not provide a global no-write ACK. Other existing-table DML likewise is not silently authorized by this order; if separately authorized it does not itself block this empty additive action. No worker may execute competing schema/provider/config changes or activate a report-writing runtime during the window.
4. Send exact SQL once only after actual gates. Require HTTP, top-level and every statement success. Transport uncertainty/refusal/partial state stops; no blind retry or destructive rollback.
5. Verify exact new schema/index/trigger/FK definitions and empty synthetic_report; compare all preexisting schema definitions with baseline, clear foreign_key_check, unchanged active source/binding/queue. Retain before/after existing-table counts with explicit deltas as observation, rather than requiring equality. Do not infer causation or explain every count delta as harmless. The no-existing-data-mutation conclusion is limited to the inspected exact DDL plus observed schema effects; it is not a database-wide integrity certification.
6. Any unexpected new-table row, existing-schema drift, FK failure, unaccounted SQL statement/result, source movement or suspected nonauthorized writer remains a hard stop for completion/promotion and requires read-only reconciliation. An existing-table count delta alone is not a migration failure or grounds for replay. Preserve all rows.

Thus Auditor's missing global quiet ACK need not be an execution prerequisite solely to prevent its metadata read/trace activity, provided root explicitly adopts this independently reviewed amendment and the actual no-competing-DDL/source gates close. It does not claim the Auditor read order was acknowledged, nor reopen actor authority. Design/Auth/Grok acknowledgments remain preserved factual receipts rather than discarded history.

Independent exact amendment review and root persistence/disposition are required before changing the existing HOLD. Reporting runtime activation, resource proof, OAuth/browser acceptance and production remain entirely separate.
