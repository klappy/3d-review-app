# A8 successor plan — main/DEV cutover, draft 1.1.0

Status: PROPOSED FOR INDEPENDENT REVIEW. Not fired, not executable authorization. Author: /root/release_reconciliation, bounded Otto planning coordinator. Actual ACK sent to Astra; clock observed2026-09-17T03:38:21.782Z; checkpoint estimate12minutes. Original A8 ninety-minute history is not reset. Root remains sole rail/journal pen. No active product/provider edits have been made.

This draft supersedes prior A8 main/staging execution steps while preserving their historical evidence. main -> existing DEV; production -> existing production. A staging branch/app is optional future scope, never a present prerequisite. This slice corrects topology, not full A8/product/API/design readiness or production delivery.

## Evidence and resolved uncertainty

- PR3 live draft/blocked 10f5f444d68475d7114ab8fe0bf269fe106476b1 -> empty main 895339fb5a289b357148e7050447125791e6a411. Empty tree 4b825dc642cb6eb9a060e54bf8d69288fbee4904 independently read.
- GitHub all rulesets including parents returns only 23571595; effective production rules returns []. Current ruleset covers main/phase0 with strict Cursor Bugbot 1210556, PR, no force pushes/deletion, no bypass, creation checks enforced.
- Fresh CF GETs: DEV existing tag b0b4617b8a3f49679e4d54ac876b6c73, trigger b82be56e-33f0-43d5-bf24-3749dd4dc168 watches phase0. Production tag 0a74a3aacb60454e90c0fb92fff2c3dd, trigger d067de78-0937-42cc-8f3e-cc001c4af8fc watches main. Both use npm ci && npm run typecheck && npm test; DEV deploy selector root wrangler; prod selector --env production. Both path include[*], path exclude[*.md], same existing connection 4988599d-fefa-4e58-a1c3-2e92746a38b3. No credential values fetched.
- Complete builds pages: DEV 19, all stopped; prod 0. DEV latest push_event 10f5f444/build2d7d63ba; active deployment 450369eb/version038befe0 at 100%. Production active 4a4d9869/version864f58cd at 100%, unchanged. These are planning observations, refresh before execution.
- CF configuration docs explicitly say updated settings apply to the next build; branch docs describe builds on pushes. PATCH schema updates branch/path/command settings; a separate POST starts a build. Historical account evidence: trigger modified 00:32:10/11, next DEV build 00:37:35 was push_event, production still0 builds. Together these support a branch-filter-only PATCH followed by an ordinary PR merge; they do not establish an atomicity/no-side-effect guarantee for every provider failure.
- Therefore choose RETARGET BEFORE PR3 MERGE, with immediate readbacks and an unexpected-build stop. No third environment, new trigger, or speculative no-op provisioning is needed.
- Extra defensive evidence: local empty directory npm ci --offline --ignore-scripts --no-audit --no-fund fails EUSAGE/1 because no lockfile. This is not a CF execution test or permission to intentionally run an empty-main build. Never rely on Wrangler auto-configuration being harmless: CF docs describe automatic config/PR creation when config is absent.

Sources: https://developers.cloudflare.com/workers/ci-cd/builds/configuration/#build-settings ; https://developers.cloudflare.com/workers/ci-cd/builds/build-branches/ ; https://developers.cloudflare.com/api/resources/workers_builds/subresources/triggers/methods/update/ ; https://docs.github.com/en/enterprise-cloud@latest/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/available-rules-for-rulesets . Raw compact provider observation: provider-observation.json adjacent. Statement classifications: current GET, published behavior, historical corroboration, local test, and pending execution remain distinct.

## Scope and pre-fire products

Coordinator routes a real author, obtains bounded ACK/checkpoint and keeps excluded owner paths. Allowlist: wrangler topology comments/config where actually necessary; docs/release.md, docs/deploy.md, README release wording, AGENTS release binding, .cursor/BUGBOT.md; narrowly specified version-stamp machinery only after actual runtime owner accepts any src/index.ts hunk. No auth/contract/handler/migration/seed/report/Design edits. Existing root wrangler already targets DEV and production selector targetsprod; avoid gratuitous activeconfig changes. Cookbook companion is local 18-H-A8-OPERATIONAL-RECIPE-DRAFT.md, targeted as a bounded appendix to existing stub planning/.../prd/18-H-infra.md, not a claim that all18-H is specified.

Preserve exact recovered five-file envelope unchanged. Successor debrief corrects its prospective local commit wording: old candidate never had a commit. New revision pins, manifest, independent review and cookbook/app pair must be real. The original inert five-file allowlist does not itself authorize this expanded candidate. Persist revised TICKET/DELTA/challenge/checklist and rerun exact-order FIRE-CHECK through actual owner before authoring/execution.

## Concrete transition

1. **Prepare and independently gate correction candidate.** Isolated branch from refreshed PR3 baseline; paired cookbook recipe; inspect pending owner changes. If merged into phase0 before cutover, this is an actual DEV Git-hook deployment and needs exact-head independent acceptance, literal Cursor Bugbot SUCCESS, terminal attached checks, all findings disposition, explicit prospective candidate-limited hold disposition and compatible rollback. No seed/migration commands. Read back deployed Git/build/version/bindings/health as post-correction DEV baseline. If candidate is docs-only and *.md path filter suppresses build, record that fact instead of inventing deployment; changes to AGENTS/.cursor metadata may still be excluded by pattern, so inspect actual build receipt. No broad product expansion.

2. **Pre-cutover freeze and receipts.** Obtain actual ACKs from owners of main/phase0 that no shared-head pushes/merges occur during the bounded control-plane cutover. Source branches/worktrees remain untouched. Snapshot both complete trigger objects, current heads, effective rules, build pages and active versions. Account for both old main/prod and old phase0/DEV builds. Waiting for identified legitimate builds to finish is preferred; cancel only individually identified obsolete builds under the accepted order, with terminal readback. No queued/running oldtarget build may remain before finalmerge. If owner freeze or queues cannot be established, stop cutover without pausing unrelated source work.

3. **Protect production before creating it.** Add a new production-only ruleset, exact refs/heads/production, active, no bypass. Copy current PR/deletion/non_fast_forward and CursorBugbot1210556 strict status policy; initial do_not_enforce_on_create=true applies solely to creation. Do NOT add a separate 'creation' restriction or 'update' restriction with nobypass, which would lock all creation/updates; PR policy is the intended updatecontrol. GitHub documents creation-only status exemption. Create production ref at exact verified empty895339fb ancestry; if it already exists, STOP/reconcile instead of replace. Immediately set creation exemption false; read back ref, ruleset, effective rules. No modification of existing main/phase0 rules. API refusal is a stop, not permission to weaken broadly. If protection tightening fails, do not attach trigger or populate main; frozen production ref remains untouched for repair.

4. **Detach wrong production path first.** PATCH existing prod trigger with only branch_includes=[production] and display name Production Deploy (production, via PR), preserving every other setting. No POST start build, deploy hook, upload or Wrangler seat invocation. Re-read trigger; compare all non-allowlisted fields exactly. Re-list builds and latest prod version; no new build is expected from settings change under published behavior. If a build appears, stop main writes, inspect exact source/branch/SHA/commands; cancel if queued/running and accepted cancellation order covers it. Do not continue based merely on cancellationrequest. Resolve/refetch terminal status and unchanged deployment. Never restore prod->main once main contains app code.

5. **Move existing DEV listener before population.** With main still verified empty and production detached/protected, PATCH existing DEV trigger only branch_includes=[main] and display name Dev Deploy(main). Keep existing Worker, bindings, secrets, command, path filters, repo connection and token association. Re-read exact fields; old phase0 must no longer match. Re-list both build queues and versions. Stop/reconcile any unexpected build, field drift or deployment change. A production deploy is never accepted by this slice. No main merge until both control-plane readbacks and queue checks pass. This removes the earlier sequencing ambiguity: the eventual ordinary main merge is the deployment event.

6. **Gate and merge PR3 whole baseline.** Update obsolete PR3 scope claims and check actual base/head unchanged since independent review. Literal current head Bugbot SUCCESS is mandatory; only Workers SUCCESS on old 10f5f444 is insufficient. Independent whole baseline review, terminal checks, source findings dispositions and explicit prospective per-candidate hold disposition apply. This order does not require every unrelated PR merged, but unresolved defects in the delivered baseline must be truthfully dispositioned for the bounded DEV delivery. Protected normal PR3 merge produces main SHA and push_event. No manual trigger retry. Read successful build source=push_event, branch main, exact merge SHA, DEV deployment/version and actual bindings/health/stamp back. Production version should equal pre-cutover. No source/datareset, migrations or seedreplay.

7. **Verify and return.** Independent reviewer reconciles main tree to acceptedbaseline/correction; confirms main->DEV and production->production filters, required rules, DEV state identity, production unchanged, no stale queues and paired cookbook recipe pins. Rebuild the declared topology/config from cookbook in an isolated clean workspace, compare expected files/selectors without touching provider; full app clean rebuild remains a separate unresolved denominator. Release freeze only after actual receipt. A8 full acceptance still requires original API/MCP/docs/observability/dependencies and actual stamp; production remains separately captain-authorized with own bump/changelog, review/checks and actual comment-only Bugbot policy proof.

## Rollback / exceptional evidence boundary

Before main contains code: DEV filter may restore phase0 only under reviewed rollback, no new deployment dispatch; retain production onproduction rather than restore known bad main mapping. After main population: keep correct filters, revert compatible code/config by checked PR to main so rollback rides Git. Never point production back to main, reset DB, replay seeds/migrations or force-reset shared branches.

If provider behavior contradicts published next-build semantics, stop this order and seek a separate bounded reversible proof order. Candidate proof: an additional temporary DEV-associated trigger with build and deploy commands both `exit 1`, exact dedicated probe branches, unchanged existing triggers, documented build-token association, approved build-usage cap, no manual build dispatch; controlled Git push probes, branch-filter PATCH, complete build/version readback, then delete probe trigger (API documents cancelling unfinished builds). Must inspect automatic dependency-install behavior and limit probe source to inert manifest/no install hooks before fire. It is not authorized or executed by this plan, not new staging infrastructure, and a negative quiet window alone is not a universal guarantee. Human-only boundaries are actual new financial commitment/secret channel/captain production; ordinary safe provider coordination still requires its reviewed order and exact execution fire, not automatic extra user confirmation.

## Driver-seat revision / rejected alternatives

Future operator opens default main and sees same accepted app at existing DEV URL; production branch owns separate production. Removed new stagingresources and mistaken unchanged-DEV-throughout claim. Rejected retarget after merge (misses intendedGit pushevent), preserving phase0 deploy listener (stale race), replacing existing trigger (unnecessary creation/build risk), manualdispatch/upload, broad protection bypass, and empty-tree failure as primary safety proof. Freshindependent review and prescribed challenge remain pending, so this document is review material, not accepted plan or FIRE PASS.

## Superseding root-review amendment 1.2.0 — exact candidate authoring order

This appendix resolves ROOT-AMEND.md and governs over every conflicting optional file/stamp phrase above. The earlier drafts remain planning provenance in kitchen49d0c2a. Status remains proposed for independent acceptance; no candidate author or provider execution is fired. Root's challenge result is CHALLENGED/nonblocking, not semantic approval. Planning coordinator ACK: /root/release_reconciliation; this revision checkpoint estimate8minutes, original A8 history unchanged.

### Chosen app correction manifest — seven paths exactly

Author from app baseline `10f5f444d68475d7114ab8fe0bf269fe106476b1` in an isolated branch; if the base or a selected file changes before start, stop and return the precise delta for planning review. Do not autonomously add paths.

| Path | Exact selected change |
|---|---|
| `wrangler.toml` | Replace only the two leading branch-map comment lines: DEV branch `main`, production branch `production`, production promoted through a separately reviewed captain-owned main-to-production PR. Add one comment saying this is the governing target and actual trigger cutover requires the release procedure. Every TOML setting/value and remaining line stays byte-identical. |
| `README.md` | Replace only the paragraph under `## Deploy (git hooks only)` after its release-procedure warning. State target main→existingDEV, production→production; correction pending until provider/Git readbacks, no manual deploy, no migration/seed replay, secret values stay out of Git. Link new docs/deploy.md. Preserve all other historical content, including known unrelated79-row/local-run claims; list those as retained debt rather than expanding this slice. |
| `docs/release.md` | Rename `## Existing Git hooks` to `## Historical Git hooks — superseded target`; keep the existing table, attributed observations and command history explicitly historical. Replace the paragraph beginning `These are commands for the Git-connected build` with historical-context wording that does not direct main-to-production. Insert a new `## Governing topology correction — main is DEV` section before `## Lawful corrective path`: settled target table, settings-first existing-trigger sequence, production bootstrap/readback/drain, exact-candidate gates and link to docs/deploy.md/control-plane-plan.json. Preserve mandatory hold, original review timelines, existing23571595 history, stricter literal-SUCCESS limitation and production stamp obligation. Change no historical receipt into a current execution claim. |
| `.cursor/BUGBOT.md` | Replace only its first body paragraph. Both current transitional phase0→main baseline PR and future main→production promotion PRs are comment-only; no head writes/autofix/rebase. Main is the settled DEV branch; production is production. Remaining exact-head SUCCESS, real service-setting verification and ownership rules remain byte-identical. |
| `AGENTS.md` | Insert one bullet after HYGIENE10a: settled main→existingDEV / production→production, provider cutover still separately gated, captain owns production, staging optional-future. Existing HYGIENE19/version text and ownership paragraphs remain unchanged. |
| `docs/deploy.md` (new) | A concise operator entry point: target map; pre-cutover versus post-cutover status; links to the release hold and exact control-plane request artifact; settings-first cutover checklist; appSHA→push_event/build→deployed-version evidence chain; data-preserving rollback; no manual dispatch; no production authority. Explicitly say `/v2/health source_sha` is a cookbook contract pin, never the app Git SHA. |
| `docs/release/a8-control-plane-plan.json` (new) | Review-only, nonexecuting manifest containing the five exact named request descriptions below, baseline pins and stop/readback conditions. `status` is `PROPOSED_NOT_APPLIED`; no script, credentials or service calls. Unknown newly created ruleset ID is a response-bound variable with mandatory readback, never a fake executable identifier. |

The other112 existing baseline files remain unchanged, including `docs/release/proposed-ruleset.json` (historical applied23571595 artifact), `package.json`, lockfile, every runtime/test/contract/handler/migration/seed/UI/report file. New runtime/build/client stamp machinery is EXCLUDED. No new package/runtime dependency is added. Original A8 stamp acceptance and production HYGIENE19 remain open; this slice proves source identity from provider/Git evidence.

### Exact review-only request manifest

The JSON describes, but never sends:

1. GitHub POST `/repos/klappy/3d-review-app/rulesets`: new name `3d-review-production-pr-and-bugbot`; targetbranch, active, empty bypass; exact include `refs/heads/production`, exclude[]. Rules copied from observed `docs/release/proposed-ruleset.json` with all PR/deletion/non-fast-forward/strictBugbot1210556 settings preserved, except initial `do_not_enforce_on_create:true`. No creation/update restriction is added. Existing23571595 is untouched.
2. GitHub POST `/repos/klappy/3d-review-app/git/refs`: `ref:refs/heads/production`, `sha:895339fb5a289b357148e7050447125791e6a411`. Ref must be absent; its creation cannot replace/update an existing ref. Read back exactSHA. Then GitHub PUT `/repos/klappy/3d-review-app/rulesets/{created_ruleset_id}` using the exact full just-read object reduced to supported request fields, changing only required-status creation exemption tofalse. Response ID is bound only after actualPOST/readback; failure blocks trigger attachment and main writes.
3. CF PATCH existing prodtrigger `d067de78-0937-42cc-8f3e-cc001c4af8fc`: `branch_includes:[production]`, `trigger_name:Production Deploy (production, via PR)`. Every other field retained and compared to its current snapshot.
4. CF PATCH existing DEVtrigger `b82be56e-33f0-43d5-bf24-3749dd4dc168`: `branch_includes:[main]`, `trigger_name:Dev Deploy (main)`. Every other field retained and compared. No new trigger or Worker.
5. Cancellation description only: CF PUT `/accounts/{account_id}/builds/builds/{observed_build_uuid}/cancel` permitted solely for individually observed queued/running old-main or old-phase0 builds whose cancellation is specifically included in the final cutover disposition. Response-bound UUID and terminal status required; no bulk cancellation, no manualstart/buildpost.

Account ID is the already configured provider context, not a credential or new resource. The artifact distinguishes request bodies from conditional readback-bound identifiers. All applicable current schemas must be re-read before actual mutation; unexpected schema change returns an AMEND, not implementer improvisation.

### Chosen cookbook manifest — four paths

The maintained cookbook change is isolated from current Auth/Design/Auditor branches, targeting the freshly revalidated plan branch; starting planning snapshot `bdb2b45f9c8afe0599f48094301ba509b36fbf1e` is evidence, not permission to overwrite newer accepted changes.

- `planning/2026-09-16-parity-build/prd/18-H-infra.md`: append only the bounded A8 recipe pointer and status. Leave all unrelated stub sections unassessed.
- `planning/2026-09-16-parity-build/operations/A8-DEV-PRODUCTION.md`: maintained operator recipe defined by the paired recipe draft, with exact source/receipt links, tools and reconstruction commands below.
- `planning/2026-09-16-parity-build/operations/a8-topology-source.json`: canonical full UTF-8/LF content of the seven selected app outputs, each with output-relative path and SHA256, plus baseline app SHA and selected source-file hashes. It owns the source text; app files are its projection. No final app/cookbook commit hashes embedded recursively: paired hashes belong in PR review receipts.
- `planning/2026-09-16-parity-build/operations/render_a8_topology.py`: standard-library-only deterministic projector/verification CLI. Validate exact seven-path allowlist, no absolute/traversal paths, unique entries, LF/finalnewline, content SHA256; `render` writes only to an empty output directory, `verify` compares bytes to a supplied app tree without writing it. Any missing/extra path/hash mismatch fails nonzero. No network, clock, Git mutation, subprocess or provider calls. Do not modify unrelated generators.

Using full canonical output text is the selected simple projection design; no patch fuzz, inferred prose generation or hidden local state. It reconstructs this bounded seven-file correction only, not the rest of the app.

### Pinned reconstruction and verification

Authoring/reconstruction runtime: CPython3.11.2, standard library only, UTF-8 and LF. Source acquisition pin: Git2.50.1; API observation client gh2.79.0 is not a projection dependency. No npm install, Wrangler dry-run or deployment command is needed for this comments/docs/JSON slice. Existing provider builds keep their current commands and pinned lockfile behavior; this amendment changes neither.

From clean checkouts of the accepted cookbook revision and paired app revision, run these exact commands, replacing only checkout-directory arguments (not scripts or source paths):

```
python3 --version
python3 COOKBOOK/planning/2026-09-16-parity-build/operations/render_a8_topology.py render --source COOKBOOK/planning/2026-09-16-parity-build/operations/a8-topology-source.json --output EMPTY_OUTPUT
python3 COOKBOOK/planning/2026-09-16-parity-build/operations/render_a8_topology.py verify --source COOKBOOK/planning/2026-09-16-parity-build/operations/a8-topology-source.json --app APP
```

Require version3.11.2 for the credited receipt. A fresh reviewer independently obtains the pinned cookbook/app trees, supplies a previously nonexistent/empty output directory, reruns both commands, compares all seven outputs and asserts the remaining baseline paths unchanged. Parse pre/post wrangler using Python3.11 `tomllib` and require equality; additionally prove only the declared comments changed. Verify exact diff allowlist and recipe source hashes. No runtime tests are credited or needed for immutable runtime/TOML semantics; any semantic delta fails this order rather than expanding tests to legitimize it.

### Two separate orders — no authoring-to-cutover shortcut

**Order A: local candidate authoring only.** Product: the exact seven app paths and four cookbook paths above in isolated local checkouts; reconstruction output and structural checks. No provider operation, branch/ref creation on shared remote, integration, deployment, source-owner edits or runtime changes. Root must first independently accept this revised plan and land exact scope, applicable checklist/fire receipts; then obtain a real author ACK, accepted budget and checkpoint. Proposed planning estimate for the author is25minutes with firstreturn10minutes after its ACK; these are not accepted promises yet. Author returns local candidate and hashes to Otto/root; root authorizes review-only PR publication separately within established coordination authority. No author merge.

**Order B: provider cutover and DEV integration, unfired.** Requires OrderA exact-candidate independent acceptance, paired revision receipts, literal current-head BugbotSUCCESS and all attached checks/finding dispositions; fresh provider snapshots/queues/owner freeze; accepted final cutover review and a distinct actual executor ACK/budget/fire. It executes the unchanged settings-first sequence in steps2–7 above. If OrderA correction must integrate into phase0 first, its potential DEV Git-hook deployment is a separately dispositioned checkpoint before control-plane cutover; document-only pathfilter suppression is observed, not assumed. PR3 wholebaseline acceptance still applies before main population. No production promotion.

Final success for this correction is accepted mainSHA tied to DEV push-event/build/deployedversion, correct branch filters and protected production, preserved state, unchanged production and reproducible paired documentation. Health contract-source is not appSHA proof. Any unexpected build, head/queue/rule drift or unaccepted file overlap stops the relevant later order. No additional blanket captain approval is inferred; actual captain-only production and real financial/secret forks remain unchanged.
