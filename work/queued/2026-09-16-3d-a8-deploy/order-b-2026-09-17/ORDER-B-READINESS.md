# A8 Order B — read-only readiness return

Status: REVIEWABLE / NOT FIRED. Existing A8 home only. Observation2026-09-17T04:15–04:17Z (00:15–00:17ET). Order A exact accepted app78d009157e19c9f3e5180db42ebb41b5d6e9174f / cookbookecab91ae12f7361f1b8d273b15c095c681a67445 is the source, not proof that remote checks or integration completed. Governing payload app docs/release/a8-control-plane-plan.json and cookbook operations/A8-DEV-PRODUCTION.md were read. This return makes no provider/Git mutations.

## Fresh facts and readiness consequence

| Surface | Observed | Consequence |
|---|---|---|
| Git main |895339fb5a289b357148e7050447125791e6a411; parentless commit points to canonical empty tree4b825dc642cb6eb9a060e54bf8d69288fbee4904|Safe planned empty bootstrap input remains unchanged. Git treeGET404 is recorded; Git commitGET and local empty-tree hash agree.|
| phase0 |10f5f444d68475d7114ab8fe0bf269fe106476b1|Active developer branches can continue; shared-head freeze still needs actual owner ACK.|
| production ref |GET404, absent|Create-only mechanism still applies; never update an existing ref.|
| Rules |Only23571595, active main/phase0, no bypass; PR/deletion/non-fast-forward plus strict Cursor Bugbot integration1210556|Existing policies remain untouched. Effective main/phase0 reads agree.|
| PR3 |open, head10f5, base895339fb, mergeabletrue but mergeable_stateblocked|Not a merge permission. Whole-baseline acceptance/check/hold disposition still required.|
| DEV trigger |b82be56e-33f0-43d5-bf24-3749dd4dc168 watches phase0|Retarget this existing trigger, no new Worker.|
| Production trigger |d067de78-0937-42cc-8f3e-cc001c4af8fc watches main|Detach from main before main population.|
| Complete build listings |DEV19, allstopped; production0; next_pagefalse,total_pages1/0|No present cancellation target. Re-observe under actual freeze; do not issue speculative cancellation.|
| Active DEV |deployment450369eb-d0ef-41c1-af82-b0f28ded5cac, version038befe0-cf15-4c3d-b48e-021a7704f13e100%|Unchanged; latest Git build2d7d63ba-a2a1-48e2-9587-5cc353dae70e succeeded from phase0/10f5.|
| Active production |deployment4a4d9869-62e8-46c8-acbd-3ef95db0fb42, version864f58cd-835c-4999-bc5d-0c9f987ebe77100%|Must remain exactly unchanged throughout topology settings work.|
| Git capability |repository permissions admin/push/maintain true|Capability indicator; actual rules/ref writes remain untested and unauthorized here.|

CF bindings read04:16:11Z: DEV DB5d4cc260-a7b1-47cc-b03d-ed4f60d324c3, ENVIRONMENTdev, names ACCESS_AUD/ACCESS_TEAM_DOMAIN/CODE_ESCROW_SECRET/SESSION_SECRET; production DB2641de99-670e-46a2-872e-a2498189e287, ENVIRONMENTproduction, SESSION_SECRET. Compatibility date2026-09-01/flags[]. No secret values collected, no additional binding values inferred. Preserve current state; this limited names/ID read is not a full Access configuration or secret-value comparison.

## Exact required action order, only after separate FIRE

1. Root confirms exact Order A remote candidate checks/review/findings and prospective hold disposition, final Order B independent review and actual executor ACK/budget. Obtain bounded no-push/no-merge ACKs for main and phase0 from their actual shared-head controllers (Astra/root and any native Claude release authority). Also prohibit retries/manual starts/cancellations on both existing triggers during the cutover. Auth/API/UI workers may continue isolated source branches. Explicitly record whether A8 candidate enters phase0 first; that can cause a DEV build and needs its own accepted disposition before freeze.
2. Under freeze, repeat current refs/effective rules, full trigger objects, complete paginated build lists and active deployments; identify every active build by UUID/source/ref/SHA. Drain legitimate work. Today there are no active builds, so no cancellation request is justified. Any later cancellation needs an individually identified obsolete build and accepted disposition, then terminal readback. Missing owner ACK or incomplete queue listing stops cutover.
3. POST the accepted production-only ruleset payload. It targets only refs/heads/production, active, bypass_actors[], with existing policy and temporary do_not_enforce_on_create:true. Do not add creation/update restrictions. Bind the new ruleset ID from actual response+GET; check effective rules for production (GitHub supports this even before branch exists). Do not copy response-only metadata into PUT payloads.
4. Recheck production absence and main895339fb. POST git/refs {ref:refs/heads/production,sha:895339fb5a289b357148e7050447125791e6a411}. This creates empty ancestry, not an app promotion. Read exact ref. Immediately PUT the new ruleset using supported full request fields, changing only do_not_enforce_on_create:false. Read back ruleset and effective production protection. Failure leaves the created empty ref protected/frozen for repair; never weaken other rules or force-update a ref to recover.
5. PATCH existing production trigger with only {branch_includes:[production],trigger_name:Production Deploy (production, via PR)}. Compare all other fields against freeze snapshot using the classification below; reread queues and active production version. No POST build, retry, deploy hook, upload or migration. Unexpected build/version stops all main writes; inspect exact source before any separately authorized cancellation.
6. PATCH existing DEV trigger with only {branch_includes:[main],trigger_name:Dev Deploy (main)} while main remains895339fb. Read both complete triggers/queues/deployments; phase0 must no longer match. Existing Worker, D1, bindings, Access and commands remain intact.
7. Stop at a protected, correctly mapped topology until the whole exact baseline/integration candidate is accepted and allowed to merge. Ordinary protected PR merge into main is a separate intended DEV delivery boundary, never a bypass. Then prove accepted mainSHA→push_event build(branchmain, exactSHA)→deployed DEV version, preserved state, unchanged production and drained stale queues before releasing freeze. Health source_sha is cookbook identity, not appSHA proof. User's future staging option is not a present prerequisite.

## Comparison contract: expected metadata versus drift

CF allowed request changes are exactly branch_includes and trigger_name on the named trigger. Expected server metadata change is that trigger's modified_on. Every other request-owned value must equal the fresh snapshot: branch_excludes[], build_command `npm ci && npm run typecheck && npm test`, deploy_command DEV `npx wrangler deploy --config wrangler.toml` / production same plus `--env production`, root_directory/, path_includes[*], path_excludes[*.md], build_caching_enabledtrue, build-token associationadd641f1-6a28-4f31-8cd4-89848f9ffdab. Worker tags and repo connection4988599d-fefa-4e58-a1c3-2e92746a38b3 are invariant. Created/deleted timestamps and nested connection modification time are not blanket-ignored; any unexplained change stops for classification. New trigger IDs, extra triggers, altered commands/filters/token/repo/environment, builds or versions are substantive drift.

New Git ruleset receives server ID/node_id/links/created_at/updated_at; tightening may change updated_at. Fresh existing pull-request rule GET additionally returns required_reviewers[], allowed_merge_methods[merge,squash,rebase], require_extra_approval_for_unattributed_changes:true. The first two are supported request fields per current REST docs; include them when deriving the full tightening payload. The extra-approval flag is returned but absent from current REST request docs; omit unsupported fields from the request and require readback to preserve true. GitHub documents it as a default with no effect at zero required approvals, not a permission to ignore policy drift. Any unexpected policy default is reviewed explicitly before downstream mutation. Existing ruleset23571595 and its effective rules must remain byte/semantic unchanged apart from response ordering.

Cloudflare live schema confirms PATCHsupports the two selected fields and exposes a separate POST endpoint for starting builds. Published settings documentation says changes apply to the next build; this supports settings-first, not an atomic no-side-effect guarantee. GitHub documents creation-only status exemption and separately warns creation/update restrictions require bypass. No write test was performed.

## Rollback without restoring production→main

Before main population, retain production→production and its protected empty ref. If the DEV retarget cannot be validated, stop; a separately reviewed reverse PATCH may restore only DEV→phase0 after checking exact heads/queues. Do not restore production→main at any stage. If production retarget succeeded and DEV failed, this is a safe partial state: existing deployments remain serving, main remains empty, and source work can continue under explicit custody.

After main population, keep correct filters and recover via a compatible checked PR into main. Never force-reset shared refs, replay seeds/migrations, delete D1 rows, manually deploy or promote a prior production version under this order. An unexpected production version change requires immediate escalation and specific recovery authority; returning a trigger setting is not a deployment rollback.

## Remaining readiness and return

Technical observations still match the accepted sequence. Not execution-ready until fresh owner freeze/drain ACKs, exact remote checks/hold disposition, final Order B acceptance and real executor FIRE exist. A capability/estimate request was sent to the existing A8 author; return pending, no budget invented. Root remains sole shared journal writer. Raw current provider and Git observations sit adjacent; root can land this bounded readiness return in existing A8 cargo without new framework/backlog.

Sources refreshed: [GitHub available rules](https://docs.github.com/en/enterprise-cloud@latest/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/available-rules-for-rulesets), [GitHub rules REST](https://docs.github.com/en/rest/repos/rules), [Cloudflare build settings](https://developers.cloudflare.com/workers/ci-cd/builds/configuration/). Actual current CF/Git API reads above take precedence over historical planning snapshots.

Actual executor return: existing `/root/queue_resolution/a8_inert_author` ACKs availability after separate root gates/freeze/FIRE. Estimate **20minutes** for protect/create/tighten, production detach, DEV retarget and sequential readbacks; first checkpoint within7minutes. Excludes queue drain, PR3 review, build/deploy duration. Worker independently verified reported GitHub admin/push and callable CF PATCH schema; these are not proof of successful mutation.403/refusal/drift stops. This supersedes the pending-estimate sentence above without implying fire.
