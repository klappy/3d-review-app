# Independent DEV rollout re-review

**ACCEPT — plan only**, exact proposal SHA256 `1c780e3777bf298bd7a6d0e623dfc28d4451e36114ac54ee3abebcc32974d5c6` at `ROLLOUT-PROPOSED.md`. This supersedes the prior AMEND for the proposal; it does not fire execution or mark pending operational gates complete.

The P1 rollback finding is resolved. The revision distinguishes old-runtime schema compatibility before shared traffic from security compatibility afterward. It prohibits a reachable legacy rollback once shared contexts exist and requires, before permitting shared traffic, either a reviewed shared-aware rollback candidate preserving revocation/expiry, scope and atomic claims, or a concrete verified block of all participant reads/writes until reviewed recovery. Staff agreement alone is explicitly insufficient. Data and additive schema remain intact; restoration/downmigration remain unauthorized.

The chosen provider path is now explicit and matches the independently discovered callable connector: fixed account and DEV UUID, bookmark GET, exact hash-checked0006 SQL POST, exact readback, exact hash-checked0007 SQL POST, then complete schema/count/FK postconditions. HTTP, top-level and every statement result must succeed. Read access is not mislabeled DDL permission; the first approved migration is the actual capability attempt, and refusal stops without tool/account substitution. No extra write probe is introduced. Unknown atomicity is conservatively handled through stop/readback/reviewed forward completion, never blind retry.

The remaining conditions are accurately pending: final integrated SHA and SQL hashes, Auth custody/no concurrent writer, named executor ACK, fresh binding/schema/count/bookmark/write-quiescence evidence, deployed-source compatibility, applicable exact-head checks and root FIRE.0005 remains excluded; adding it requires amendment. Runtime promotion remains normal Git→DEV after topology checks, followed by real integrated acceptance. Production, seeds, original migration replay, fabricated ledger, manual deploy and build-command changes remain excluded.

No additional human confirmation is required merely because this is a separately reviewed DEV operational order. Existing user authority and root's recorded execution disposition govern. This re-review read the entire revised artifact and verified its hash; it performed no provider query or mutation, implementation, push, merge or shared-journal write.
