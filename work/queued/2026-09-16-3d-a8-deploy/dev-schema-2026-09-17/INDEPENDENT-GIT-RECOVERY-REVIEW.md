# Independent Git-only recovery clarification review

**ACCEPT**, exact `GIT-ONLY-RECOVERY-AMENDMENT.md` SHA256 `03f71907d2dd2467bae4dfb32e6517f59e35272dc13cdb7254c92b2aa1fd3ca2`.

Read the complete amendment, accepted rollout re-review and adjacent `ROLLBACK-AD95321-SOURCE-REVIEW.md` / `RECOVERY-INGRESS-READBACK.json`. The clarification satisfies the original safety intent: never put a shared-unsafe legacy runtime back into service after shared contexts exist. It does not require an unnecessary prebuilt second Worker or new isolation architecture before an otherwise accepted secure release.

Keeping the independently accepted, shared-aware current runtime serving while a usability correction follows protected-main review/check/build is compatible with that invariant. The ordinary Git recovery procedure is concrete, preserves configuration, migrations and later security fixes, and retains source→build→version verification. `ad95321e99923666b3dbc9eac163710e6e70ad41` is a published backend reference whose protections were independently inspected, not an unconditional full-tree revert or an immediate version-switch capability. Its legacy UI remains explicitly degraded recovery. Actual recovery diffs need their own exact-head acceptance.

The minimal before-traffic evidence is appropriately bounded: accepted integrated source/security/checks; verified DEV schema/config; published compatible reference and recorded owner/root Git-only recovery custody/procedure; ordinary deployment and actual acceptance receipts. No known security failure may be released. The rollout's earlier “rollback candidate or isolation” wording is clarified by this accepted amendment: safe current operation plus governed forward recovery is sufficient; isolation is not demanded during a harmless UI-fix build.

If an observed incident makes current traffic unsafe, merely waiting for a Git build is explicitly insufficient. Containment still needs actual scoped authority/executor, full relevant ingress assessment, in-flight handling, execution and observed refusal. The read-only receipt proves only that workers.dev/preview switches currently exist/enabled and no DEV custom domain appeared in the27-domain inventory. It does not prove all ingress is covered or that a kill switch works. The amendment makes no such claim and fires no provider action. This is honest incident planning, not a promise that future containment is instantaneous.

This ACCEPT closes the recovery-procedure design clarification only. It neither marks remaining source/check/schema/runtime gates complete nor authorizes provider mutation, deployment, merge or production. No implementation, provider call/mutation, Git write, shared-journal write or additional test was performed for this re-review.
