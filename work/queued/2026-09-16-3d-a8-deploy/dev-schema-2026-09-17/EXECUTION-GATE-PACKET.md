# Existing A8 DEV-schema execution gate packet — HOLD

2026-09-17 04:46 UTC. Parent: accepted ROLLOUT-PROPOSED.md SHA256 `1c780e3777bf298bd7a6d0e623dfc28d4451e36114ac54ee3abebcc32974d5c6`; exact independent ACCEPT in INDEPENDENT-ROLLOUT-REREVIEW.md. Root reports Git landing `5c78f291`, cargo `dev-schema-2026-09-17`. This packet changes no accepted SQL or scope and is not FIRE. Root remains sole shared-journal writer.

## TICKET amendment / executable return

Existing A8 only; station infra/ARS, coordinator Otto, remote-schema decision/custody Auth owner, disposition root. Class: additive DEV database operational write. ALLERGY: existing respondent data and shared-link authorization; production untouched. Product: exact DDL execution receipts, schema/count/FK readbacks, compatibility/recovery disposition and root handoff; not app acceptance. No seeds, 0001–0004 replay, 0005, fake migration history, command changes, manual Worker deployment, downmigration or production action.

On explicit named FIRE, executor refreshes the exact DEV binding/account/UUID, deployed version/source, schema/count/FK/bookmark and owner quiescence receipts. If still matching: hash-check final candidate's0006/0007; POST exact0006 SQL; require HTTP/top-level/every-statement success and exact readback; POST exact0007 once; require full accepted postconditions. Stop on drift, refusal, failed/missing result or partial schema; report before any forward repair. Normal Git deployment and native runtime acceptance remain separate owner actions.

## Source compatibility observed (not remote runtime test)

Read immutable app commit `10f5f444d68475d7114ab8fe0bf269fe106476b1` directly with git show/grep. `src/handlers/participant.ts:20` INSERT names six existing participant_session columns; added invitation_id remains NULL. `src/auth.ts:28` SELECT names existing columns and ignores the addition. `src/handlers/response.ts:77` INSERT explicitly names unchanged response columns.0006 creates independent table/index;0007 nullable addition and partial unique index leave old NULL-session writes outside the constraint, with no existing row deletion/update. This establishes source-level additive compatibility for10f5 before shared traffic, conditional on fresh provider proof that10f5 is still the deployed source. It does not prove remote schema application or safe post-shared-traffic rollback. No native test run duplicated.

Recovery candidate proposal: Design's planned Auth25+API24 backend intermediate, before UI changes, could preserve shared-aware authorization/claims while backing out UI. Actual assembly is not observed here. Require its real commit/version, independent review and shared security evidence; currently unpinned/unaccepted here. Merely naming branches or leaving source in Git is not a deployed recovery capability. Before shared traffic, root must pin a reviewed viable recovery deployment path or verified participant read/write isolation.10f5 is prohibited as a reachable post-shared-traffic rollback.

Actual executor availability ACK: `/root/queue_resolution/a8_inert_author` accepts separately gated connector execution after root FIRE, estimates15 minutes from actual START with first checkpoint within5 minutes, excluding source/owner/quiescence holds. This is availability, not plan verification/readiness. Executor reports no DB query or mutation performed for this ACK.

## CHECKLIST 1.4.1 run (13 gates)

| Gate | Status / evidence |
|---|---|
|1 Header|PASS: exact DEV additive purpose and return above.|
|2 Fields|PASS: coordinator/custody roles and actual executor15-minute/5-minute checkpoint ACK above.|
|3 Naming|PASS: existing A8, no new ticket or framework.|
|4 Ingredients|HOLD: accepted plan/SQL hashes/provider schema readbacks exist; final integrated source pin pending.|
|5 Product|PASS: four concrete operational receipt types above.|
|6 Observable done|PASS: exact schema, unchanged quiescent counts, FK state, no partial failure; runtime acceptance separate.|
|7 Dependencies|HOLD: Auth custody, native local-only/DB quiescence, final SHA, fresh binding/schema/bookmark and root FIRE.|
|8 Risk|PASS: existing data plus shared-security recovery explicit.|
|9 Concrete scope|PASS: only0006/0007 exact file SQL, sequential verification.|
|10 Reference fidelity|PASS: exact reviewed plan and readback; final deployment identity must refresh.|
|11 Prior art|PASS: house migrations, release.md:41, reusable provider API; no new service/dependency.|
|12 Failure response|PASS: stop/readback/reviewed forward completion; no blind retry/restore.|
|13 Lens|PASS scoped: existing rollback revision plus DELTA below; Git landing of this packet pending root.|

## FIRE-CHECK 1.3.0 run (8 gates)

| Gate | Status / evidence |
|---|---|
|1 Ticketed|PASS: existing A8 landed accepted DEV-schema cargo.|
|2 Well formed|HOLD operational dependencies as above; complete prospective sequence.|
|3 Spec current|HOLD final integrated SHA; accepted SQL hashes remain exact target, changes require review.|
|4 Reorient|HOLD owner ACKs/fresh execution preflight; historic readback is not under-quiescence proof.|
|5 Preflight|RUN, qualified:04:45:56Z FOUND from bundled baseline; cookbook overlay403, not project-canon success. Local actual project source/release instructions supplied separately. Raw adjacent.|
|6 Challenge|RUN:04:46:06Z CHALLENGED, block_until_addressed:false; not PASS. Evidence/alternatives/risk/retraction addressed below, independent ACCEPT separate.|
|7 Borrow|PASS scoped: existing D1 connector/migrations reused; alternatives evaluated below.|
|8 Lens|HOLD packet landing/readback; actual accepted rollback change already independently reviewed.|

## DELTA / driver-seat answers

Future operator needs a source pin and exact DB identity rather than a vague migration command. Existing plan therefore uses exact SQL bytes and sequential readbacks and removed dependence on unproven local CLI authentication. Both CLI and connector can execute SQL; connector read-only query/bookmark is actually demonstrated, while write capability is not. Any403 or ambiguity retracts execution readiness immediately. Blind migrations apply could replay historical files because the ledger is absent; automatic build-command migration would expand scope and retry risk. Both are rejected for this bounded rollout, not universally.

Confidence: observed10f5 source-level compatibility and absent remote objects are facts at their observation points; successful remote application, final integrated correctness and write permission remain unproven. Wrong identity or partial0007 risks outage; reachable legacy rollback after shared traffic risks authorization bypass. Therefore keep data/schema, stop on partial failure and require reviewed shared-aware recovery before traffic. Additive changes are not undone merely to restore a previous source version. Success means exact schema/readback and preserved quiescent data, followed separately by native integrated behavior proof. Scope is this database and these files only; changed SQL, source, deployed version, schema or owner custody invalidates readiness. No general safety claim is drawn from one instance.

Root closes real holds with linked receipts and an explicit named FIRE; until then no executor mutation. No additional human approval is manufactured by this packet.
