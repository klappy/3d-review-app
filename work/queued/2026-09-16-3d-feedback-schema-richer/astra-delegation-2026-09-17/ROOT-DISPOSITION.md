Root Astra — klappy explicitly adds dual sentiment / richer product feedback as a new sprint within the existing ticket family. Contract disposition: OWNED, AMEND before implementation; the user outcome is accepted, proposed schema is not blindly accepted.

Observed sources: board at486ff86; current feedback-schema-richer ticket; current proposed AMEND; accepted appa57ba930 platform.ts:84–89. The handler inserts JSON containing only context,text,stripped, then returns recorded:true. The richer fields in the proposal are not persisted by this handler. Prior recorded:true/stripped:false probes establish request acceptance, not a rich-field storage round trip. Correct statements such as 'extras land' accordingly; retain existing Git scorecard ratings as separate evidence.

Required planning amendments:
1. One explicit HTTP/MCP/UI contract for the richer fields and validation, plus durable persistence/readback acceptance for each field. Name how authorized aggregation retrieves them; no inference from a success receipt alone.
2. Compatibility: current minimal bodies and current context/text behavior exist. Making six new fields mandatory is a contract change, not automatically a patch. Specify compatibility or explicit migration/version treatment under the accepted policy; do not silently break existing callers.
3. Bound strings and payload sizes; define unknown-field handling and exact answers-stripping behavior/order. Do not claim generic 'answer-like' redaction without an explicit implementable rule and tests. Preserve current access/security rules; define staff/participant/agent feedback contexts without requiring participant accounts.
4. Bind feedback to actual release identity from the versioning slice; distinguish user ratings, agent self-reports and observed task outcomes. No invented scores, turns or goals. UX Track B stays empty until its real affordance is tested; humans' ratings and automated persona observations must remain attributed.
5. Board provenance correction: the486ff86 board carries app_deploy10f5f444 into waves dated after the05:44–05:46Z delivery of a57ba930. Use authoritative build3faa7906 / active version0fbd6782 / actual test timestamps to reconcile per-wave identity; do not relabel an entire historical wave without evidence. Keep unknown where individual attribution cannot be recovered.
6. Design owns browser UX and must estimate a bounded slice with accessible optional feedback behavior, truthful saved/error states and no participant survey interference. It is a separate sprint, not a silent addition to currently fired three-file submit-feedback work.

Planning may proceed in parallel. No handler/UI implementation FIRE is granted by this disposition until the exact API/design plans and independent gates pass. Use existing ticket rail/1-ordered/2026-09-16-3d-feedback-schema-richer and issues14/16/17/19; no duplicate meal or competing form. No LLM-assist expansion. NLX owner may continue its existing cast work, but root does not claim an independently verified background monitoring mechanism.
