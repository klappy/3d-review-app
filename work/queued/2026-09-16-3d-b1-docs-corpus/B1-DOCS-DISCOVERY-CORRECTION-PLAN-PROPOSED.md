# B1 docs discovery correction — proposed planning handoff

Status: **REVISION 2 — PROPOSED / NOT FIRED / INDEPENDENT RE-REVIEW PENDING**. Supersedes revision1 after independent AMEND in `B1-INDEPENDENT-PLAN-REVIEW.md`; original preserved as `B1-DOCS-DISCOVERY-CORRECTION-PLAN-PROPOSED-v1.md`. This is a bounded continuation of existing B1, not a new product lane or ticket. Root owns Git publication and integration. This local agent authored only this plan and the preceding independent evidence receipt; it will not implement or independently review its own plan.

## Outcome and boundary

A cold agent can discover how to configure an assessment and obtain the exact existing API parameters from the four-tool docs surface, without repository access or a human relaying a schema. Resolve independently confirmed NLX-MCP-001 and the parameter-discovery portion of003. Include governed examples for002/005/006/008/009. Do not claim whole B1, whole NLX, functional API, Design, shared-link implementation, or finished-app acceptance from this slice.

No aliases, auto-open-on-select, weakened source validation, changed capability classes, new permissions, duplicate Auth/Design workers, production work, or implicit retirement of PR21. Full MCP/API plus full Design coverage still precede broad UI. Shared links require no participant account/login/OAuth/mandatory code; staff and agent security remain required. main means existing DEV; production branch means production; optional staging remains future-only.

## Observation and custody

- Kitchen `rail/1-ordered/2026-09-16-3d-b1-docs-corpus/TICKET.md` names Fable, says held PR21 cannot count until fire, promises 90 minutes historically under R6. Do not reset or inherit that remaining budget without the coordinator's accounting.
- Issue15 c5702160101 is the historical Fable B1/B6 claim; c5701751176 separates Fable docs/definitions from Grok independent persona execution. Issue13 c5707281742 records the original Fable author session closed. Current Auth continuation does not by itself transfer B1 custody.
- PR21 at `429da09b30f0f9319472b7fb3c04ff5216a73e1b` remains open/held. Latest explicit disposition c5707627120 preserves its 90-file cargo, retires its live 79-row/app-home/clock claims, and asks Fable/NLX plus contract coordinator for an old-artifact-to-current-home map. No later B1-specific active author ACK was found in the reviewed records.
- Therefore: **named historical owner, no verified active B1 writer**. Auggie/root must secure a current successor ACK within existing custody before implementation. This plan is not that ACK. Do not commandeer the active Auth writer.
- Actual app baseline inspected: `10f5f444d68475d7114ab8fe0bf269fe106476b1`. Relevant cookbook plan snapshot: `bdb2b45f9c8afe0599f48094301ba509b36fbf1e`. Re-resolve both before execution; record the exact approved paired revisions.
- Live read-only query `docs {q:"how do I create an assessment"}` returned hits[]; trace `tr_OLwPDIqn5s6oPcFL`. Health trace `tr_Wz4TySFDWloATEbY` reports cookbook contract-source `0f44137`, not app deploy SHA or no-drift proof.

## Existing sources and homes

| Concern | Maintained cookbook home | Actual app home / observation |
|---|---|---|
| Capability authority, class/role/stage/HTTP paths | `planning/2026-09-16-parity-build/04-CAPABILITY-MATRIX.md`, applicable `04-ACCEPTANCE.md`, `prd/18-A*` and `prd/18-D*`; current rulings override historical claims | `contract/capabilities.json`, `contract/openapi.yaml`, `src/registry.ts`, `src/mcp.ts` |
| NLX behavior and journeys | `05-NLX-AGENT-USAGE.md`, `prd/18-G-nlx.md`; proposed/unreviewed status remains | `src/handlers/docs.ts`, `scenarios/journeys.json`; existing generated docs page lacks concrete params |
| Exact accepted behavior vs source observation | Source anchors and named tensions under current contract owner | `src/handlers/{assessment,language,grant,common,survey,response}.ts`; observed implementation does not automatically become approved policy |
| Source form types/options | Steve gold pin and current source manifest; applicable template/response contract | Returned `response.form` plus pinned instruments; never invent multi-select option codes |
| Repeatable projection | Existing `10-REPROJECTION-AUDIT.md` and B5 custody | `tools/gen_contract.py`; PR21 `lanes/b/generate.py` is only a comment stub, not a runnable generator |
| Independent persona evidence | `NLX-MCP-*`, issue19 casts / issue17 defects | Grok owns replay. Existing C004–018 pointer stubs do not constitute complete replay receipts |

## Completed PR21 cargo map (planning disposition, not adoption acceptance)

All90 changed paths enumerated from fully paginated PR21 files. All six topics, README, index, B6, generator, scenario shape and representative assessment/grant pages read at429da09. Page-group map is exhaustive by path membership; it is not semantic acceptance of all79 pages.

| Old cargo at429da09 | Current observed home | Disposition and accountable custody |
|---|---|---|
| 79 `lanes/b/docs/capabilities/*.md` | App `src/handlers/docs.ts` projects83 registry pages; no old markdown corpus present | Retain all79 at exact PR pin as historical unadopted cargo. Only class/tool/source-anchor page structure is a reuse candidate; no prose bulk-copy. Fable/NLX custody through Auggie. New supplement below is the maintained home for accepted bounded recipes/schema, not adoption of all79. |
| `docs/INDEX.md` | docs.ts `index()` generates registry section index | Superseded as live79 index; retain historical pinned index. No new index file necessary. Fable/B1 owns corpus reconciliation. |
| topic `the-four-tools.md` | `05-NLX-AGENT-USAGE.md`, app `src/mcp.ts` and docs.ts orientation | Retain source-linked tool semantics; current runtime independently matches four-tool structure. No new tool or change of effects class. Fable/B1. |
| topic `receipts-and-undo.md` | `05`, `prd/18-G-nlx.md`, docs.ts `reversibility`; app `src/receipt.ts` | Historical template retained, no verbatim reissue-code recovery as mandatory flow. B4/Fable owns full receipt-copy reconciliation; bounded B1 points to current declared inverses only. |
| topic `suppression.md` | `16-CONSTRAINTS.md`, `prd/18-B*`, docs.ts `privacy`, results handler | Unadopted historical wording retained; current D7/source decisions remain authoritative. Public results review belongs to existing Auditor/root, not B1. Never turn held into unsupported fewer-than-threshold claim. |
| topic `errors.md` | `05`, `prd/18-A*`, app `src/handlers/errors.ts`, docs.ts page.errors | Retained historical closed-code list, not accepted field-shape proof. B4/Fable owns general hint changes; B1 supplies only governed examples and docs text. |
| topic `survey-forms.md` | `14-STEVE-REPO-SYNC.md`, pinned source manifest, template/form handlers | Retain source vocabulary; no new source labels or invented examples. Fable/B1 with source-owner acceptance for dynamic form schema. |
| topic `what-can-i-do-here.md` | `05`, `18-G`, docs.ts role/scope branch | Historical `topic:start` has no actual current TOPICS entry; explicitly unadopted, not recommended. This plan introduces a real `assessment-setup` recipe/topic, not a fictional start topic. Fable/B1. |
| `lanes/b/B6-AUDIT.md` | Existing B6 ticket; new eighteen-cast NLX reports complement, do not replace it | Preserve seven-role/negative-case/33-uncovered heuristic leads at pin. J11–J14 now exist in app scenario shape but coverage is not proven. B6/Fable owner; Grok independently evaluates. No mass closure. |
| `lanes/b/scenarios/journeys.json` J1–J10 | App `scenarios/journeys.json` J1–J14 from cookbook05@59be64c | Structurally superseded live shape, not byte-identical adoption: old `scenarios` metadata becomes executable `journeys` steps. Retain exact old pin and negative-case leads; mandatory issuance/redeem journey superseded by participant ruling. Existing B2/app17 worker retains runner pen; B1 does not edit either scenario file. |
| `lanes/b/generate.py` | App `tools/gen_contract.py` generates contract projection | Old file is a comment stub; no executable generator inherited or discarded. Retain provenance. Existing generator is NOT suitable for this docs projection (generic params, current-time timestamp). New separate cookbook projector specified below, B5/Fable custody through Auggie. No gen_contract.py edits. |
| `lanes/b/README.md` | Actual app repo and current B1/PR21 disposition | App-home/OF-1 and79 completion language superseded. Retain pinned historical README; no current authority. Root alone may later retire PR21 after unique cargo ownership is verified. |

## Exact proposed projection design and allowlist

These are reviewable design decisions now, not choices deferred to implementation. Paths marked NEW are proposed and do not yet exist.

- Canonical maintained input (NEW): cookbook `planning/2026-09-16-parity-build/nlx/b1-discovery.json`. Source-of-truth for this bounded docs slice; not the entire API schema. Adjacent (NEW) `nlx/B1-DISCOVERY.md` records acceptance receipts, exact contract-owner dispositions and PR21 cargo-map pointer. Existing `prd/18-G-nlx.md` gets only the supplement pointer and explicit83/current-participant supersession. `05-NLX-AGENT-USAGE.md` gets only the bounded recipe pointer; no broad rewrite.
- Projector (NEW): cookbook `tools/project_b1_docs.mjs`, Node22.16.0 standard library only (`fs`, `path`, `crypto`, `child_process` only for read-only `git show`/`git rev-parse` of the local immutable cookbook checkout); no npm package, Python/yaml runtime or network dependency. Node22.16.0 is available here (observed `node --version`), and is the proposed reproducibility pin. Projector requires exact22.16.0 for acceptance replay; no silent runtime substitution.
- Output (NEW): app `src/generated/b1-docs.json`. Contains `schema_version:1`, source input SHA256, source descriptor, `recipes`, `capability_help`, and policy/acceptance states. No timestamps, checkout paths, usernames, random IDs, environment data or mutable branch names. Input JSON duplicate keys invalid; sort object keys recursively using JS code-unit order, preserve explicitly meaningful array order, UTF-8, two-space indentation, LF and exactly one trailing newline. Source pin is passed as a required40hex cookbook commit; source input must byte-match that commit before projection. Generated output excludes the projector's own output hash (no self-reference).
- Exact generation command from app root after immutable cookbook checkout at `$B1_COOKBOOK_DIR`: `node "$B1_COOKBOOK_DIR/tools/project_b1_docs.mjs" --input "$B1_COOKBOOK_DIR/planning/2026-09-16-parity-build/nlx/b1-discovery.json" --cookbook-commit "$B1_COOKBOOK_SHA" --output src/generated/b1-docs.json`. Both variables are task-specific paths/pins supplied by the accepted paired revision receipt, not inferred live branches. `--check` runs identical projection in memory and exits nonzero on byte mismatch without writing. No call to gen_contract.py; its generated_at and generic schemas remain unchanged. Running twice in separate clean directories must give identical bytes.
- Input structure: `schema_version`, `recipes[]` (id/title/keywords/required_role/steps/source_refs/status), `capability_help` keyed by exact capability ID (required/optional field descriptors, observed_app_pin, normative_disposition, acceptance_receipt, sanitized examples). `status` and `normative_disposition` distinguish accepted, proposed, observation-only, dependency-pending. Only accepted help can be rendered as operative instructions. Pending entries render a brief honest status/pointer with no executable workflow. Projector validates known keys/types/capability IDs/source references and requires a receipt for accepted entries; no full JSON-Schema package or permissive unknown schema expansion.
- App handler allowlist: only `src/handlers/docs.ts`, generated `src/generated/b1-docs.json`, NEW `test/b1-docs-discovery.test.ts`, NEW `test/b1-docs-examples.test.ts`. No package/lock/config/API-handler/mcp/contract/scenario/PhaseA-test changes. Existing test17 writer remains untouched. Observed app10f5 tsconfig has resolveJsonModule:true and Bundler resolution; JSON import needs no config change.
- Cookbook allowlist: the five source/projector/pointer paths above, plus NEW `test/project-b1-docs.test.mjs` and NEW `planning/2026-09-16-parity-build/nlx/B1-COLD-AGENT-RECEIPT.md` for independent sanitized receipt. No old PR21 writes, no shared journal writes by implementer. Root journal pen remains exclusive.
- Tests: Node22.16.0 `node --test "$B1_COOKBOOK_DIR/test/project-b1-docs.test.mjs"`; app lockfile at accepted base uses observed TypeScript5.9.3, Vitest2.1.9, Wrangler4.133.0, Hono4.13.8. `npm ci` preserves that lock, `npm run typecheck`, and `npm test -- test/b1-docs-discovery.test.ts test/b1-docs-examples.test.ts test/language.test.ts test/lane-b-grants.test.ts test/collection-stage-gate.test.ts test/pinned-instruments.test.ts`. No dependency upgrade. If accepted integration base changes versions, freeze/re-review the new explicit lock pin before execution.
- Runtime behavior: `docs {topic:"assessment-setup"}` returns the maintained accepted recipe. Existing query `hits` capability envelope remains; add bounded `recipes` matches. Normalize query lowercase/trim/punctuation-to-space, tokenize, ignore fixed stopwords (`a,an,the,how,do,i,my,for,to`), rank intersection against maintained recipe keywords, stable tie by recipe id, max5. Zero matches return existing hits plus named navigation fallback. Existing capability pages add the accepted `parameter_help` field and preserve class/tool/role metadata. No inferred mutation of request schemas. Role/scope filtering and existence hiding retain existing behavior; tests must reject any newly leaked inaccessible context.
- B5 custody: historical Fable/B5 through Auggie owns projector source; proposed B1 successor must explicitly ACK this bounded new projector and pointers as part of the handoff. App docs handler is Fable/NLX custody; Auth and app17 retain every overlapping contract/handler/runner file outside this allowlist. Root assigns an actual available author, different independent reviewer and budget; neither is invented by this document.

## Narrow parameter authority and acceptance table

Observation is not acceptance. `tools/gen_contract.py` at app10f5 explicitly says params_schemas OWED and emits generic additionalProperties. Neither that OpenAPI nor83 route registration authorizes the detailed shapes below. All observations below are pinned to app `10f5f444d68475d7114ab8fe0bf269fe106476b1`; class/tool/path source is cookbook matrix `0f4413768c4365054372d917619311d7c2a00e96`. Governing latest user rulings supersede both where conflicted. No actual contract-owner acceptance receipt for this new narrow supplement has been observed; each acceptance cell states the exact pending owner, not a fabricated approval.

| Capability/help | Observed exact handler fields | Proposed normative disposition / required owner receipt |
|---|---|---|
| `cap.language.list` | language.ts:list; required nonempty string `pid`; no specified optional field | Propose accept only docs description of current path param. Root API-contract owner must accept this row; no alias, no behavior change. |
| `cap.language.create` | language.ts:create; required `pid`, trimmed nonempty `name`; optional `code` string/null, existing BCP47-shaped validation and duplicate refusal | Proposed docs only; root API-contract owner accepts exact code/null behavior or leaves create example excluded. No invented language-code policy. List/select existing language remains independent of create. |
| `cap.assessment.create` | assessment.ts:create; required nonempty strings `pid`,`name`,`language_id`; optional nonempty strings `purpose`,`period`,`format` if provided; missing fields become null; language must belong to project | Proposed docs only, root API-contract owner receipt pending. Do not describe null as accepted optional input where reqStr rejects it. No scoring/placement policy adoption. |
| `cap.survey.select` | survey.ts:select; required strings `aid`,`template_id`; version optional existing optInt path/default latest; collection open iff assessment.collect | Proposed example pins an explicit valid source template version, avoiding adoption of coercion/default behavior. Root API-contract owner receipt pending. No auto-open. |
| `cap.assessment.set_stage` /005 | assessment.ts:set_stage; required strings `id`,`stage`; prepare/collect/understand/improve with adjacent transition validation | Documentation of explicit prepare→collect only proposed to root API-contract owner. Shared-link token/session/repeat semantics EXCLUDED pending Auth exact contract; no mandatory-code recipe allowed. |
| `cap.auth.request_link` /002 | class write.effect/danger is source-pinned; current and pending MCP auth entry differ | EXCLUDED executable example in this slice. Render dependency-pending identity-path status only. Root asks existing Auth owner for accepted exact head+entry transport+intent-bound confirmation receipt; later additive independently reviewed docs delta, no moving-branch dependency. |
| `cap.response.form/submit` multi /006 | response.ts:validateAnswers requires answers object; source item.type=multi array strings, bounds/dedup/exclusion and source options; form supplies item details | Observation-only factual explanation, no accepted full submit schema claimed. Root/source+response contract owner must accept particular pinned form exemplar before executable submit example. Pending acceptance excludes this case from slice closure, not from whole-product denominator. No weakening. |
| `cap.grant.invite` /008 | grant.ts+common.reqScope; required `scope` enum workspace/project/assessment, `id` nonempty string, `role` owner/member/viewer, `email` nonempty string lowercased; caller/member ceiling and owner protections | Flat shape docs proposal to existing Fable grant-contract custody via root. No acceptance receipt yet. Synthetic dry-run/execute example only after accepted exact grant contract pin; mail side effects remain stubbed. No nested scope or role-permission changes. |
| `cap.grant.revoke` /009 | grant.ts:revoke; required flat `scope`,`id`,`gid` nonempty strings; membership/owner guard and no inverse enforced | Same narrow Fable grant-contract acceptance request. Valid-param protected-owner negative test mandatory; missing-param failure insufficient. |

API examples list required/optional accepted fields for agent instruction; this is NOT a change to runtime unknown-key behavior or a claimed complete validation schema. Accepted-disposition flags are written only after root records the real owner receipt. The planning review can approve this concrete design while execution remains blocked specifically on required001/003 setup-row acceptance. 002/006/008/009 are additive dependency-pending examples unless their precise existing owner receipts arrive; they must not block independent001/003 plan preparation or be called resolved.

Exact questions for root to route, not an open-ended permission wait: (A) root API-contract custody accept or amend the five setup rows above at10f5 as the narrow docs source; (B) existing Auth custody return exact accepted request_link entry/transport and shared-link contract pin, or acknowledge excluded pending status; (C) existing grant custody accept flat scope/id/role/email/gid shape at accepted grant/mail pin for synthetic examples; (D) source/response owner choose the already-pinned multi form exemplar and exact submit contract. No new worker dispatch is implied by these questions.

## Examples that must be source-accurate

| Finding | Required example / invariant |
|---|---|
| 001/003 | `cap.language.list` receives `params:{pid:<project-id>}`. Assessment create uses `pid`, `name`, `language_id`, with optional `purpose`, `period`, `format`; identify where each ID comes from. Do not suggest `project_id` aliases. Validate names/types against the final accepted contract and current handler pin. |
| 002 | Email sign-in request is an external effect: `danger` flow or accepted intent-bound authentication entry as specified by current Auth owner. No real mail in docs tests; synthetic transport only. Do not claim anonymous MCP remains available after the authenticated wrapper lands. |
| 005 | Survey selection in prepare remains closed; `cap.assessment.set_stage` uses `{id:<assessment-id>,stage:"collect"}` and documented valid adjacent transitions. Never open collection merely to cure a discovery test. Shared-link participant workflow is the governing target, not code issuance; if current implementation cannot perform it, say pending and link the existing Auth dependency. |
| 006 | Multi answers are arrays of source option codes, constrained by required/min/max/exclusion rules actually returned for that item. Show a clearly synthetic example drawn from the pinned form and a scalar rejection; preserve the source validator. |
| 008/009 | **Handler scope is flat**: `params:{scope:"assessment"|"project"|"workspace",id:<scope-id>,...}` from `reqScope`; it is NOT `{scope:{type,id}}`. The nested scope object belongs to the docs role/scope query. Invite also needs `role` and `email`; revoke needs `gid`. Invite remains danger dry-run→confirm→execute; revoke protection must be tested with valid params, not stopped at missing-scope validation. |

No real email, bearer, code, private transcript, response data or sensitive fix URL belongs in examples or receipts. Use synthetic fixtures, opaque trace IDs and counts as appropriate.

## Independent acceptance oracle

Grok/Auggie or a separately appointed fresh reviewer, never the implementation author, owns this replay. The test starts with a cold agent given only the task, synthetic caller role/scope, endpoint and tools; no repository filenames, capability names or parameter coaching. No new worker claim is inferred from naming this oracle.

1. **Discovery:** docs orientation then the user's setup question obtains the executable recipe in no more than two docs calls; query spelling differs from one implementation example. Record calls, returned recipe/source anchors and why it matches. The ≤2 target is the existing proposed18-G acceptance target being tested, not assumed already accepted globally.
2. **Setup:** in isolated synthetic/local state, agent follows docs to select/create the appropriate project language, create an assessment, select a source-backed survey and identify explicit collect transition. Correct required params on first attempted capability call after docs; preserve returned IDs and receipts. No participant code prerequisite is introduced.
3. **Examples:** perform dry-run and negative/positive examples only for rows whose specific owner acceptance receipts are present. Dependency-pending002/006/008/009 remain excluded from this bounded slice acceptance and unresolved in the whole-product denominator; this is not a deferral permission or a full-B1 pass. Test005 explicit stage documentation after root narrow-row acceptance, without inventing shared-link behavior. For accepted effect examples use local synthetic mail and principals. Missing scope cannot stand in for owner-protection proof. Closed collection remains closed until deliberate authorized transition. No external send or production call.
4. **Boundary:** anonymous/unscoped/member/viewer test matrix proves docs add no scope/existence disclosure; wrong-tool errors continue to reject. Current Auth wrapper semantics are pinned and tested separately from docs content. Unknown query gives honest help, no fabricated success.
5. **Reconstruction:** clean independent regeneration from paired cookbook pin recreates exactly the changed docs artifact(s); examples validate against accepted schema/handler pin. This proves only this slice's reproducibility, not whole-app100%.
6. **Integration:** exact-head automated checks including literal Cursor Bugbot SUCCESS, independent semantic review and no unexplained artifact drift. After authorized DEV integration, Grok repeats read-only docs discovery against actual app commit/build/Worker version readbacks; health contract-source alone is insufficient. A docs-only query cannot prove full participant/report journeys.

Acceptance receipt records exact cookbook/app revisions, environment and transport, sanitized request/response expectations versus observations, trace IDs where available, negative cases, independent reviewer, retained gaps and source tensions. Scores/claims are not fabricated. No empty casts or pass-by-pointer receipts.

## Driver-seat revision and rejected alternatives

Future cold-agent failure: finding a capability name but receiving `params:{}` still forces source-code reading. Revision: return the setup recipe plus concrete schema and prerequisite ID discovery in the same docs path, not merely improve search ranking.

Future collaborator failure: nested `scope` copied from a docs query still fails invite/revoke. Revision: separate docs-query scope shape from handler-parameter scope shape with validated examples.

Future participant failure: “next_best collect = issue_codes” reproduces the rejected product flow. Revision: current docs must distinguish accepted shared-link target from unresolved implementation, and never call legacy issuance mandatory. Changes to actual shared-link mechanics remain with Auth/Design.

Rejected: copying all PR21 files wholesale (stale79/code journeys; no executable generator); adding aliases (new API behavior to mask missing docs); auto-opening surveys (changes stage policy); loosening multi validation (violates source); reducing danger classification (changes effects boundary); treating an LLM search service as necessary (unproven need); claiming B1 complete from two fixed queries (full83+topics acceptance still open).

## Pending gates and requested owner return

Pending before any implementation fire: current B1 successor/custody ACK; elapsed/remaining R6 budget from coordinator; narrow setup-row source acceptance receipts; optional Auth/grant/form examples remain explicitly excluded until their pinned receipts; independent planning challenge and revised-plan acceptance; applicable current ticket/fire checks. Reading this plan is not running those gates.

Minimal request to existing Fable/NLX custody through Auggie on issue15: acknowledge or identify an already-active B1 owner; confirm this bounded plan and PR21 cargo map or name exact amendments; provide actual implementation/checkpoint budget and shared-file custody; return the accepted paired source pins and independent reviewer assignment. No invented deadlines and no reset of the historical90-minute promise. Root publishes; this agent stops at the reviewable plan.
