# M05 return — actions and secondary journeys

Owner: config/actions leaf; Auggie return path. Fired after actual rail2 commit 0f1001c0dd60c43dbf7566d7b16b17e26667ba9d receipt. Observed start 2026-09-09T04:28:35Z / 00:28:35 America/New_York. Accepted 15-minute budget. M02 state and M04 report interface accepted dependencies.

Delivered modules-actions.js, test-actions.cjs and this return. Added two links to owned modules-config.js workspace view for portfolio/lifecycle. No other product files changed by this leaf.

All six done-means have concrete local journeys:

1. Finding-linked action form validates title, fixed synthetic owner and review date/condition, saves deduplicated action against original cycle, and toggles reviewed status.
2. Named follow-up copies configuration only, has blank period (no forced recurrence), zero responses and no report, links its original cycle and preserves original actions/responses/report snapshots.
3. Comparison checks T/C/H versions, target language and context and requires explicit reviewer confirmation. A C-v2 incompatible fixture reports reason; compatible invented counts never become a progress/impact claim.
4. Portfolio filters authorized cycles/reviewed outputs; workspace exact-scope grant alone reveals no descendant aggregates. Viewers see only reviewed outputs, not live counts. Archive/unlink/delete previews distinguish effects and preserve responses, grants and cycles. Owner/last-owner/retention holds remain explicit.
5. Help has a navigable three-step sample lesson, local reviewed marker and return/practice links. Readiness includes proposed 15-project/full-cycle/80%-group targets, zero real pilot projects, unknown eligible denominator, sample evidence links and human project-owner confirmation requirement.
6. Support fixed actor/reason produces deduplicated Not sent receipt and no authority change. Integration success/denied/unresolved-version cases show local sample request/response and explicitly unresolved production schema/version/read-write contract.

Checks: node --check modules-actions.js PASS. test-actions.cjs PASS: validated actions/dedupe, followup zero-state and historical snapshots preserved, compatibility confirmation/mismatch reasons, no-op lifecycle/owner holds, unsent support and no-network sample integration. test-config.cjs rerun after workspace links PASS. These are supporting runtime checks; independent browser coverage remains M06, including form navigation, training/readiness checkboxes, compatible/incompatible transitions and desktop/mobile.

Learning: comparison eligibility and viewer publication are different boundaries. A viewer's scope metadata grant must not expose live response aggregates through portfolio or lifecycle previews. Module uses report snapshots for viewer outputs and holds live comparisons. Project owner authority to create a follow-up does not establish recurring dates or a fresh report.


Auggie independently reran test-actions and all14 scenario checks PASS. Module candidate landed ff747cccc1d84184af523d5a42742c35abf1c6f2; late viewer guard awaits exact final compile commit. Full deployment and independent desktop/mobile M06 remain pending. This is artifact delivery, not final captain acceptance.
