# M02 return — synthetic setup/collaboration

Owner: config leaf, Auggie return path. Fired by Auggie after actual rail2 gate receipt commit 57ad5ad55c1d65174545ac8d275f6965946e7575. Observed implementation start 2026-09-09T04:22:43Z (2026-09-09 00:22:43 America/New_York). Accepted budget: 15 minutes from FIRE.

Changed only modules-config.js, test-config.cjs and this return. No Git writes, deployment, real invites or external effects.

Delivered visible routes: prepare, project, workspace, collaboration, collaborator. Owner name save updates shared metadata; Cancel rerenders persisted value. Separate project/workspace capabilities create deduplicated local objects or unsent request drafts. Assessment fields save into core cycles; response reinterpretation holds and creates a distinct empty cycle. Fixed collaborator picker previews exact resources and local pending/accepted/declined/expired/revoked states. Owner elevation/last-owner removal held. Explicit independent scope grants preserve a1 while ancestor grants are removed. Unauthorized p2 grouping denied. Workspace grouping requires separate project share authority; sample fixture can explicitly add that independent grant. Freeze and collection close remain separate and preserve responses. D4/member configuration/retention holds are explicit.

Validation: node --check modules-config.js and node test-config.cjs PASS. Transition tests exercise nine role/scope cases, exact workspace reach, create/request dedupe/validation, owner and member rename, saved config/new-cycle zero response preservation, invitation accept/retry/decline/expire/revoke, owner holds, grant survival, p2 grouping denial and separate freeze/close. These are supporting source/runtime checks, not independent browser acceptance. M06 browser validation remains required, including desktop/mobile name Cancel, scoped invitation recipient landing, form draft navigation and downstream collection settings.

Integration: shell fixed workspace grants to exact scope and workspace viewer landing after M02 boundary finding. Public module contains synthetic allowlisted labels only. Core model governs routes/grants; config uses configInvitations separately from participant survey invitation mappings. Recipient listings require current accepted recipient session; invitation lists filter to currently permitted resources.

Learning: explicit fixture scope grants must drive shell routes as well as collaboration cards. Initial core workspace descendant default contradicted conservative grouping assumptions; shell owner corrected before handback. Transition tests now cover no p2 or assessment reach from default workspace grant. No production permission policy inferred.


Auggie checkpoint 2026-09-09T04:31:40.580Z: module source landed 3a4aab4f67f3cec5534eb12ce65e9f1b88deef2a; targeted tests independently rerun PASS. Combined generated output/deployed real-browser acceptance remains pending; this is module delivery, not plated product.
