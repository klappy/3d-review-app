# Production prerequisite plan — proposed only

Issue70; app draft73 head7f92d1f9fdf0e4e87782110e22bb311c34805354; cookbook draft49 headf8beb7cb6e991547f6a90128bdfcf3decb73168a. No remote mutation performed.

Observed production is a first-deployment placeholder, not a previously functional app: workers.dev /v2/health returns RESERVED_NOT_BUILT awaiting first push-to-deploy; custom hostname has no Worker domain attachment and does not resolve. D1 contains only internal _cf_KV. The deployed version has no Git source annotation. Reuse accepted backend review evidence; there is no functioning old product path to preserve here.

1. Reconfirm production D1 application schema is empty immediately before initial schema creation; abort on any app table. Proposed-production-schema.sql composes unchanged DDL from migrations0001,0002,0003,0006,0007,0008. Local SQLite validation yields21 tables,16indexes,1view,zero app rows. SQL SHA256 abc09605e64ee6620b19041aa3c6084bbf487638beb3d4cc615104f58bdd9f4e. This is additive first initialization, not replay, reset or seed. Root must disposition exact SQL before any execution. Post-read must match schema and verify zero app rows.
2. Canonical instrument definitions: migration0004 imports9 source-pinned survey templates. This is required for creating usable surveys but is data insertion, not schema. Keep separate from step1 and obtain a precise root disposition under the user's no-seed boundary. Never run synthetic.sql, synthetic-responses.sql, dev seed endpoints, demo principals, or assessments. Migration0009 backfill is unnecessary for an empty principal table; new-account code already provisions own-creation access.
3. Configuration: existing production DB and SESSION_SECRET remain. Candidate wrangler.toml declaratively adds production rate limits, OAuth KV, PUBLIC_ORIGIN, ACCESS variables and custom domain. Verify referenced OAuth KV exists without reading keys. No manual Worker upload/deploy or arbitrary trigger change. Normal Git-connected production deployment applies candidate declarative config.
4. Mail prerequisites: production currently has no RESEND_API_KEY binding and no MAIL_FROM var. Verify approved sender and authorized secret source without exposing values. Use only supported secret/config channels; do not copy credentials into Git or chat. Without mail, do not claim working email login/invitations. Existing SESSION_SECRET value is never read or replaced.
5. Custom domain: candidate declares3d-review.klappy.dev, currently unattached. Verify ownership/zone and allow normal connected deployment to attach through declared custom_domain route; if it cannot, prepare a scoped domain attachment action for root review. Verify DNS/TLS and /v2/health afterward.
6. Promotion gates: DEV0.2.0 verified; cookbook0.2.1 authority accepted; independent exacthead review, Bugbot literalSUCCESS and terminal checks; root prospective disposition; ordinary PR73 merge into protected production. Observe production GitSHA→push-event build→active version→health stamp/pin→exact static assets. No manual deploy or seed/migration replay.

Authorization assessment: explicit shipment to production supports necessary additive first-service setup, but earlier no-seed/migration-surprise constraint means schema and canonical instruments must be made concrete and separately dispositioned now. Do not issue a vague permission request or silently execute. DEV release remains independent.

Owner: integrate_checkpoint. Station: isolated production release branch. Recipe: app docs/release.md, kitchen HYGIENE. Promise: no invented deadline. Risk: first production initialization; explicit root disposition of reviewed schema/config before mutation. Declared product: working accepted web/share increment on production, separately versioned0.2.1; no synthetic seed.
