# Release record — 0.5.0

Canonical candidate for app issue [71](https://github.com/klappy/3d-review-app/issues/71): interactive MCP Apps panel. Transported from the Design supplier MCP-only patch; excludes its separate share-lifecycle and G1 edits. This is not a deployment or real-host acceptance receipt.

## [0.5.0] — candidate

### Added

- One self-contained MCP Apps resource, attached to the existing four tools, with SDK host bridge.
- Workspace and project navigation, assessment views, survey inclusion, reflection notes, report reads and assessment invitation confirmation UI.
- Loading, refusal, error and host-capability states. Local AppBridge harness is excluded from deployed assets.

### Changed

### Fixed

### Security

- Resource access uses existing MCP authorization. Tool permission checks and dangerous-operation confirmation remain server-owned. No new tool, API endpoint or database migration.

## Compatibility and acceptance boundary

MINOR: compatible interactive surface on the existing tools. Follows participant 0.4.0; normal merge reconciliation and canonical index repinning remain required before release if preceding history changes. No production promotion is included.

Independent implementation and integration review accepted the bounded panel, including stale-preview and in-flight confirmation guards. Actual browser verification in the unmodified official MCP Apps basic-host 2.0.0 (upstream 6d9bdc7) exercised the real local Worker resource, AppBridge reads, navigation, loading, permission refusal and invitation dry-run/cancel; the six-check evidence received independent scoped acceptance at app 4b02c1e. Local isolated D1 and synthetic session injection do not prove production authentication or Claude account integration. No tool write or danger execute was performed. Host-initiated failed-result presentation remains a separate follow-up; no full tool, all-panel or mobile coverage is claimed. Exact reconciled-head checks and release disposition remain required.
