# Release record — 0.12.0

MINOR for [app issue 123](https://github.com/klappy/3d-review-app/issues/123) / [PR 127](https://github.com/klappy/3d-review-app/pull/127). Adds six capabilities and an additive durable roadmap schema after 0.11.4. Existing MCP connection corrections remain included. The canonical contract is [live-roadmap](../../2026-09-18-live-roadmap/AMENDMENT.md).

## [0.12.0] — candidate

### Added

- Live /roadmap reads durable progress and history through six shared HTTP/MCP capabilities, with SSE updates, cursor replay and explicit polling fallback.

### Changed

- Persona evidence: 3/3 local actual-browser HTTP/MCP write-to-visible cases and 1/1 mobile layout case passed; human satisfaction, confusion and frustration NOT MEASURED.

### Fixed



### Security

- Public events use an allowlisted projection; restricted provenance and authenticated publisher identity remain private. Claims do not self-verify completion; corrections support atomic public redaction.
- Publisher permission uses the existing authenticated identity and a narrow server-only build projection. Reviewed additive schema preparation runs only in the matching canonical Git-connected build.

## Persona outcome scorecard

| Persona/scenario | Evidence class/environment/source | Passed / attempted / failed / untested | Satisfaction / confusion / frustration | Comparable baseline delta |
| --- | --- | --- | --- | --- |
| Viewer sees HTTP claim, MCP reviewed summary and MCP redaction without page reload or server restart | Author actual Chrome, local Hono + D1 fixture at app 87aa8c50; three writes across the same running build | 3 / 3 / 0 / 0 | NOT MEASURED | No comparable earlier live implementation |
| Mobile viewer reaches Production and Remaining columns | Author actual Chrome at 390×844, same local source; separate layout check | 1 / 1 / 0 / 0 | NOT MEASURED | NOT MEASURED |
| Authorized connected agent publishes to the deployed DEV feed and an already-open browser receives it | Operational deployed acceptance pending; existing OAuth principal verified, narrow publisher configuration prepared | 0 / 0 / 0 / 1 | NOT MEASURED | NOT MEASURED |
| Production viewer receives the canonical feed and user confirms usefulness | Production delivery and human confirmation pending | 0 / 0 / 0 / 1 | NOT MEASURED | NOT MEASURED |

Independent source acceptance covered app 87aa8c50 and cookbook f5fb9a28: 37 server and 6 UI tests, typecheck and six-row canonical parity. Author local browser evidence belongs to that exact source (its inherited build label was 0.11.2+87aa8c5, not a deployed 0.12 release). The browser denominators above are separate from automated test counts. No human ratings, production writes, real ChatGPT browser replay or deployed publisher operation is claimed.

The final 0.12 integration adds seven focused deployment/schema tests, narrow server-only hashed publisher configuration and CI-only schema preparation; its review and final check receipts belong to PR 127. Migration applies only reviewed 0010_roadmap.sql after absent-schema preflight, or verifies an exact existing schema, and refuses partial/drifted schema. It never seeds or replays initial migrations. Empty verifier and narrative-reviewer permission lists grant no such authority.

DEV is explicitly provisional until the same product/version/pin is promoted and verified in production. The public feed distinguishes publisher claims, independently attributed review attestations and limited trusted serving-release observations; a live health observation is not full workflow acceptance. SSE reconnect/cursor replay, generation reset after redaction, last-event time and disconnect/fallback labels expose freshness honestly. Public history may remove sensitive content while restricted audit remains protected.

Before serving this increment, the normal review/check gates and canonical build must complete, migration readback must pass, and the installed authenticated MCP publisher must update the same deployed build's already-open browser without a rebuild. Deployment preparation is not that proof. Keep issue 123's operational acceptance open until it is recorded; do not infer completion from this candidate record.
