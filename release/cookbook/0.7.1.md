# Release record — 0.7.1

Canonical candidate for [app issue 86](https://github.com/klappy/3d-review-app/issues/86). PATCH: restores the existing panel refusal contract without changing APIs, permissions or transport.

## [0.7.1] — candidate

### Added


### Changed

- Release notes include a bounded persona outcome scorecard; human satisfaction remains not measured.

### Fixed

- Initial host-initiated MCP read refusals appear in the panel without leaking private error details; navigation clears the refusal.

### Security


## Compatibility and acceptance boundary

Host tool-result failures lacking a capability now render a safe refusal before normal workspace reads. Boot rerenders preserve it; navigation and successful host results clear it; stale reads cannot overwrite it. Code and trace are retained, private server messages/hints are not. Existing confirmation execution remains undisturbed. No tool, API, grant, transport, data or provider change.

## Persona outcome scorecard

| Persona/scenario | Evidence class/environment/source | Passed / attempted / failed / untested | Satisfaction / confusion / frustration | Comparable baseline delta |
| --- | --- | --- | --- | --- |
| Assistant user receives an initial refusal, safely navigates afterward, and avoids stale read overwrite | Automated actual panel event handler; isolated DOM, issue86 candidate | 3 / 3 / 0 / 0 named cases: structured envelope, text envelope, stale read plus successful reroute | NOT MEASURED; no human sample | All three new tests fail on 0.7.0 and pass with this fix |
| Assistant user sees refused host read and recovers to workspaces | Actual unmodified official basic-host2.0.0 UI in Chrome; candidate Worker with isolated synthetic identity/D1; no live account | 2 / 2 / 0 / 0: initial refusal visible; Open workspaces clears refusal and displays authorized fixtures | NOT MEASURED; agent-operated reference host, no human sample | Initial refusal gap documented in issue86; current refusal/recovery visibly verified |

These fixed denominators cover this bug only, not total persona or product coverage. Real Claude/Codex account use and human experience are NOT MEASURED. Candidate evidence is attached to the paired app PR; no live deployment or production acceptance is claimed. Independent exact-head review and required checks precede normal main/DEV release, then same-version production promotion.
