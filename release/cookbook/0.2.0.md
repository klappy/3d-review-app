# Release record — 0.2.0

Canonical release record for the accepted web and survey-sharing increment. App issue [66](https://github.com/klappy/3d-review-app/issues/66). Accepted source baseline: `4a27830c56c4ef05d2ea8656e07de68311eef942`; later G1 permissions and MCP work are excluded. Chris authorized DEV followed by a separate production promotion, subject to exact-head review and normal checks. The verified DEV deployment is recorded below; production remains a separate promotion.

## [0.2.0] — 2026-09-17 (DEV)

### Added
- Showcase-based public entry, workspace and project navigation, and assessment views.
- Survey Share controls create participant links and display locally generated QR codes.

### Changed
- The primary web entry uses the new assessment shell; existing participant, invitation and legacy routes remain available.
- Assessment views separate navigation from stage changes and preserve explicit permission scopes.

### Fixed
- Held or refused reports remain visible as honest states, with mobile layout and report-number readability improvements.

### Security
- Credential-bearing route changes retain the accepted forwarding behavior; no new permission inheritance is introduced.

## Compatibility and promotion

MINOR under the existing major-zero policy: changed accepted web behavior and additive sharing UI, without a new backend, migration, scorer, or framework. Separate DEV and production PRs retain all gates. The release tag is `v0.2.0` on accepted app merge `2c5127530f9ed79dc2ebcbcf3ff7f60a08b16909`. Prior 0.1.0 remains historical candidate state; this record does not retroactively assert its production release.

## DEV release receipt

- Status: released to DEV; tag `v0.2.0`; civil date `2026-09-17` (America/New_York). Production has not been promoted.
- Accepted app [PR #76](https://github.com/klappy/3d-review-app/pull/76), exact reviewed head `8dab901b4bf05ec7f687c10845ab43cfa964aca6`; normal main merge `2c5127530f9ed79dc2ebcbcf3ff7f60a08b16909`. The final bounded corrections isolate stale identity requests, restore workspace retries and responsive navigation, and clarify optional participant codes.
- Independent scoped acceptance, real Cursor Bugbot SUCCESS and Workers SUCCESS preceded the [prospective merge disposition](https://github.com/klappy/3d-review-app/pull/76#issuecomment-5723483191).
- Canonical Git push build `d53f7fe6-0a4a-4067-9565-84f196c7ab39`, from `main` at the accepted merge, supplies the DEV artifact. It completed successfully and activated deployment `0dc19708-dd45-4c65-8567-99f7283b8c9f`, Worker version `5350fa63-95c3-4c9d-9e2d-8bfcad6d2b2e` at 100% on `2026-09-18T01:16:27.881747Z`. Live health reports `0.2.0+2c51275`, the exact merge and build UUID, cookbook pin `1167180`, and healthy D1. Root, assessment, sharing, QR and legacy assets byte-match the reviewed source; generated changelog current is `0.2.0`.
- The app continues to pin candidate cookbook commit `11671805a5eb0b7b5b32f490eec4669ed7f096b2`; this later record does not rewrite that build-time fact or require an app status-only change. G1/MCP and production prerequisites remain separate.

The legacy version badge reads health; the new assessment shell does not yet mount that badge and is tracked separately. Anonymous live MCP initialization requires authentication (401), so this receipt does not claim authenticated live MCP parity; source and version tests bind MCP serverInfo to the same generated version. These display/coverage limits do not assert full-product acceptance.
