# Release record — 0.17.0

MINOR under the major-zero policy: the v3 facilitator-first experience (design-system-v3 prototype) replaces the kit presentation on DEV. Aggregated from lane change files and SPRINTS rows (klappy/kitchen rail/2-cooking/2026-09-24-3d-v3-build-today). Trains: #205, #217. Cookbook-side release record (planning/.../releases/0.17.0.md + releases.json + manifest record shas) owed after the training; this manifest carries the version only.

## [0.17.0] — candidate

### Added
- v3 shell and tokens: no context tree, breadcrumbs, one primary action per page, plain state words; global restyle to the v3 prototype; sign-in per prototype frame 1.
- Home dashboard as the prototype card grid (Start a new 3D Review, continue cards).
- Four-step setup wizard following prototype frames 3–6, with safe resume after a dropped connection.
- Assessment page with state-driven primary; Record my review → Choose a next step; Next step page saves notes.
- Results as bands with legend, per-perspective counts, evidence toggle and table.
- Collect: settled + not-yet-confirmed counts, open/closed rows, one-line totals.
- Participant welcome and pager per prototype frames 8/9; phone-width fixes.
- Project form: lead organisation, language name + ISO code, cycle date; access codes and workspaces reachable from project settings.

### Added (lane change files)
- Added: four-step setup wizard at Start a review (details, groups with optional "How many do you expect?", participant information, review & launch) over the unchanged API (#182, #191) (2-1202)

### Changed (lane change files)
- Changed: setup screens restyled to the design-system-v3 prototype — stepper, perspective colour dots, review sub-lines (#206, #215, #219) (2-1202)

### Fixed (lane change files)
- Fixed: an interrupted launch resumes without duplicate reviews (#194) (2-1202)
