# Release record — 0.12.1

PATCH for the roadmap reconnect defect found on [production PR134](https://github.com/klappy/3d-review-app/pull/134#discussion_r4050738091). The correction goes through main and DEV before production; no production-only fix or new contract.

## [0.12.1] — candidate

### Added



### Changed

- Persona evidence: automated redaction/reconnect regression improved from 0/1 to 1/1; human satisfaction, confusion and frustration NOT MEASURED.

### Fixed

- Roadmap reconnects use the accepted current cursor and redaction generation, preventing repeated table resets after public redaction.

### Security

- Immediate redaction clearing and stale in-flight response suppression remain; callbacks from closed streams and older generations cannot clear or restore the accepted view.

## Persona outcome scorecard

| Persona/scenario | Evidence class/environment/source | Passed / attempted / failed / untested | Satisfaction / confusion / frustration | Comparable baseline delta |
| --- | --- | --- | --- | --- |
| Open roadmap viewer survives redaction and subsequent EventSource reconnects | Automated local actual feed module at app be0e99d; stable accepted snapshot, fresh reconnect URL and closed-stream rejection | 1 / 1 / 0 / 0 | NOT MEASURED | Same regression fails at main66d6bb4: old stream remains open with stale generation |
| Local actual Chrome remains usable after redaction and natural reconnect | Author observation of actual corrected UI and local Hono/D1, two synthetic rows → redact one → remaining row visible after >30 seconds without reload | 1 / 1 / 0 / 0 | NOT MEASURED | No pre-fix browser baseline; automated regression supplies baseline |
| Deployed corrected browser behavior and user comprehension | Pending corrected DEV/production release and human follow-up | 0 / 0 / 0 / 1 | NOT MEASURED | NOT MEASURED |

Author8/8 roadmap UI tests and11/11 actual HTTP/MCP transport tests plus typecheck passed at be0e99d. The one scenario is included in the8 UI tests; do not add it as another test. Existing redaction privacy, stale-read suppression, public stream anonymity and fallback/pause tests still pass. Initial0.12 live publication3/3 belongs to66d6bb4 and does not prove this correction.

Native EventSource retries retain the URL supplied at construction. The client replaces that source only after accepting a consistent snapshot with a new generation, carrying both current cursor and generation. Redaction still clears public content immediately before refresh; a failed refresh never silently claims a current view. Closed-source and older-generation callbacks are ignored. The six canonical capabilities, permissions and database schema are unchanged.

Independent exact-head review, required checks, normal main→DEV validation and identical version/source/pin production promotion remain required.0.13 must incorporate the corrected source separately rather than bundle this fix.

Local Chrome checkpoint evidence at 21:20:45 and 21:21:31 UTC on 2026-09-18 retained only the non-redacted row with sequence3 and connected status. This is author browser evidence at product be0e99d; the harness inherited health label0.12.0+66d6bb4, so it is not deployed0.12.1 identity proof. Screenshots/checkpoints do not continuously measure flicker; the automated regression checks that invariant. No live user data was changed.
