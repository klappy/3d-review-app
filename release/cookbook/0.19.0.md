# Release record — 0.19.0

MINOR under the major-zero policy (highest bump across the train's change files: minor). Train 4 of 2026-09-24: v3/integration → main, aggregated from `release/changes/*.md` on v3/integration (klappy/3d-review-app), sorted by section then lane (captain rulings 11:58 and 12:00, klappy/kitchen rail/2-cooking/2026-09-24-3d-v3-build-today/LANES.md). Change files consumed: 1-1255, 1-1302, 1-1311, 1-1321, 1-1333, 1-1341, 2-1301, 2-1305, 2-1321, 2-1331, 2-1336, 2-1351, 3-1259, 3-1303, 3-1313, 3-1323, 3-1338, 3-1346, 9-1328.

## [0.19.0] — candidate

### Added
- public About page (#about) (#228, lane 1)
- left sidebar on assessment and scope pages is the shared Sidebar tree (workspace → project → assessment), in step with the breadcrumbs (component: Sidebar tree) (#243, lane 1)
- component: Sidebar tree — 0.16 left navigation Workspaces → Project → Assessment, current row from the same scope as Breadcrumbs (ui/v3/components/sidebar-tree.js; mounting via NEED 9→1) (#241, lane 9)

### Changed
- component: Breadcrumbs — assessment context panel crumb row is the shared Breadcrumbs (Home › Workspace › Project › Assessment); v3 components served locally (#232, lane 1)
- navigation: kit shell header crumb row is the shared Breadcrumbs component (Home › Workspace › Project › Assessment › Page), same as the assessment page (P0 12:32) (#239, lane 1)
- demo header Exit demo / Sign in is one shared component (component: Header with Exit demo / Sign in) (#246, lane 1)
- setup wizard: group rows use the glass look (--glass, --glass-edge, blur) instead of flat white cards (ruling 12:22) (#234, lane 2)
- setup wizard: group rows use the design-system glass blur token (--blur-panel) instead of a hard-coded 8px (ruling 12:22) (#238, lane 2)
- Stepper dots blur on the --blur-panel glass token (component: Stepper) (#244, lane 2)
- component: Stepper — assessment page stage tab bar (Prepare · Collect · Understand · Improve) replaced by the wizard's shared Stepper; server stage = active dot, earlier stages ticked, each step links to its view; Permissions stays a separate link (captain ruling 12:28) (#230, lane 3)
- results: band card and colour legend are one shared component (ui/v3/components/band-card.js) (#233, lane 3)
- results and collect count line comes from one shared Response count component (#240, lane 3)
- results "Show evidence and details" table comes from one shared Evidence table component (component: Evidence table) (#245, lane 3)
- results review gate (state badge + Record my review / Choose a next step) comes from one shared Review gate component (component: Review gate) (#247, lane 3)

### Fixed
- public links never land on sign-in (Read about it → #about, unknown hashes → home) (#228, lane 1)
- look: flat white/teal restyle (#200) removed; design-system-v3 glass tokens govern header, cards and buttons again (ruling 12:22) (#235, lane 1)
- setup review: participant note says plainly it is not sent or saved yet (review line + placeholder), replacing developer copy (#231, lane 2)
- setup wizard: alert note uses the design-system --warning-ink token instead of a hard-coded red, so it stays readable in dark mode (#242, lane 2)
- Stepper: completed steps are announced as "completed" to screen readers (component: Stepper) (#248, lane 2)
- band card component is served by the local server and its tests stay off DEV assets (#236, lane 3)
