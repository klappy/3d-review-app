# Release record — 0.9.3

Canonical PATCH for [app issue99](https://github.com/klappy/3d-review-app/issues/99), paired with [app PR101](https://github.com/klappy/3d-review-app/pull/101). Correct the existing MCP panel theme behavior without changing the four-tool contract or authorization.

## [0.9.3] — candidate

### Added



### Changed

- Theme scorecard: 6/6 automated local scenarios and 4/4 bounded official reference-host visual scenarios passed; human satisfaction, confusion and frustration are NOT MEASURED.

### Fixed

- The MCP panel follows the host light/dark theme with operating-system fallback and live updates; a local System/Light/Dark override takes precedence.

### Security

- Theme changes do not invoke tools or change authentication, permissions, confirmations or persisted product data.

## Compatibility and acceptance boundary

Default System uses host theme first and operating-system preference when the host omits it. Explicit Light/Dark overrides win and persist locally where storage is available. Host/OS changes update without reload or tool calls. Initial content waits for host initialization; connection failure reveals the fallback view. Existing shared design tokens supply both palettes. Teardown removes theme listeners. Compact action-specific cards remain separate issue98.

## Persona outcome scorecard

| Persona/scenario | Evidence class/environment/source | Passed / attempted / failed / untested | Satisfaction / confusion / frustration | Comparable baseline delta |
| --- | --- | --- | --- | --- |
| MCP operator: initial host preference, live host changes, OS fallback, explicit override/restore, unavailable storage/teardown, generated token parity | Automated local DOM/source tests, app e3aed856f3bc8892abce70c2ab06054619b0a14e, test/mcp-panel-theme.test.ts | 6 / 6 / 0 / 0 against six named scenarios | NOT MEASURED; no human sample | Prior source had no theme wiring; no comparable measured user score |
| MCP operator: supported-host light/dark rendering, dynamic host switch, override/restore, readable controls/dialog | Operator browser plan, candidate PR101; official basic-host2.0.0 localhost8086, actual local Worker3004; source b856557; outputs/mcp-theme-host-evidence.md | 4 / 4 / 0 / 0 against four visual scenarios | NOT MEASURED | NOT MEASURED |

The full targeted automated theme/panel/resource suite passed24/24 after normal0.9 main reconciliation; those overlapping tests are not added to the six-scenario denominator. Four supported-host visual scenarios passed with the limitations recorded in outputs/mcp-theme-host-evidence.md: pointer targeting required keyboard activation; pre-existing long affected JSON overflows horizontally in both palettes. Exact final integration and independent review remain gates. No real-human study, DEV or production deployment is claimed by this candidate record. MAIN/DEV validation precedes same-version/source/pin production promotion. Preserve0.9.1 harness correction and all previous release history.
