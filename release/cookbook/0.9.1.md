# Release record — 0.9.1

Canonical PATCH for [app issue102](https://github.com/klappy/3d-review-app/issues/102): repeated release-validation timeout and unsafe test-fixture overlap. Harness-only; no runtime, scoring, grant, transport or accepted product behavior change.

## [0.9.1] — candidate

### Added


### Changed


### Fixed

- Release validation runs all 34 synthetic report contexts as sequential bounded cases, preserves the 425-response aggregate and coexistence checks, and drains or quarantines unfinished fixture work before reuse.

### Security


## Compatibility and acceptance boundary

Production0.7.1 canonical buildf1e3a145 and DEV0.9.0 canonical builde97b30cf each failed the same120-second aggregate corpus test, with449/450 tests passing and no assertion mismatch. DEV0.9.0 did not deploy in that run. This candidate does not retroactively claim it was live.

The same34 contexts retain every capture/render/build/get/list/repeat-build/raw-row assertion. A final aggregate retains34 coexisting reports,425 responses and maximum23 responses per assessment. Cases run sequentially with the existing30-second default test timeout, not a raised blanket timeout. A tracked operation drains before fixture reuse; after10 seconds without settling, the fixture is permanently quarantined and future phases/cases refuse work. Hook budgets bound drain/disposal; no indefinite wait or automatic production retry. Existing refusal, forgery and race cases remain intact. Product source and all fixtures are unchanged.

## Persona outcome scorecard

| Persona/scenario | Evidence class/environment/source | Passed / attempted / failed / untested | Satisfaction / confusion / frustration | Comparable baseline delta |
| --- | --- | --- | --- | --- |
| Release operator: corpus validates all contexts and coexisting reports | Automated local Node/Miniflare at paired app issue102 candidate; final test receipt on PR | 35 / 35 / 0 / 0;34 contexts plus1 aggregate | NOT MEASURED; no human sample | Prior single aggregate timed out in two canonical builds; no claim of provider improvement before connected validation |
| Release operator: late operation cannot overlap fixture reset; drain cannot hang indefinitely | Automated deferred-operation tests; local test-only guard | 2 / 2 / 0 / 0; two lifecycle cases | NOT MEASURED | Previous timeout body continued under the next test's D1 lifecycle |

No new end-user journey is introduced; human usage, live deployment and product-performance scores are NOT MEASURED. Independent exact-head review and normal MAIN/DEV validation precede same-version production promotion. Preserve0.9.0 candidate and previous release history; no deployed immutable pin is rewritten.
