# Release record — 0.7.0

Canonical candidate for [app issue89](https://github.com/klappy/3d-review-app/issues/89): the user-designated DEV and production web domains. Separate from native mail0.6.0; no mail provider or recipient-policy change.

## [0.7.0] — candidate

### Added

- Canonical DEV domain dev.3dreview.app and production domain 3dreview.app on the existing Workers, with previous addresses retained as compatible aliases.

### Changed

- Invitation emails use the canonical origin for their environment; browser-created participant links remain on the current origin.

### Fixed

### Security

- Existing authentication audiences, host-only sessions, OAuth callback validation and participant fragment handling remain unchanged. New sign-in paths require matching Cloudflare Access destinations.

## Compatibility and acceptance boundary

MINOR under the pre-1.0 policy: additive public origins and changed canonical email-link origins. Existing DEV workers.dev and production3d-review.klappy.dev remain valid aliases. No cross-origin redirect, new API capability, permission inheritance, data migration or UI redesign. Browser cookies/storage remain per-origin: users sign in again on a new host; MCP clients may need to authorize the new endpoint.

Local verification reads effective Wrangler routes/origins and tests generated invitation fragments, real borrowed OAuth discovery/PKCE/consent/session/refusal flows on both new origins with synthetic identity/JWKS and isolated storage. This does not establish live DNS/TLS, Access edge coverage or human login. Infrastructure ownership separately adds only each new /v2/auth/access destination to its existing per-environment Access application, preserving policies and audiences; no inbox, inbound route or forwarding is introduced.

Retain the0.6.0 and earlier release histories, including subsequent C2 reconciliation before merge if needed. Independent exact-head review and all required checks precede normal main/DEV delivery. Production promotes the same validated semantic version, canonical release pin and source through its separate PR. This candidate is not a live-deployment receipt.
