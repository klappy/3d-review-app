# Release record — 0.9.2

Canonical PATCH for [app issue108](https://github.com/klappy/3d-review-app/issues/108): restore current-shell `#participant` compatibility for existing code-based participant sessions. The MCP theme candidate is separately sequenced as0.9.3.

## [0.9.2] — candidate

### Added

### Changed

- Persona outcome scorecard records five executed route cases and one independent local-browser participant-resume journey; human sentiment remains NOT MEASURED.

### Fixed

- Current-shell participant resume opens the preserved legacy survey instead of returning an existing participant session to the public access-code entry.

### Security

- Route forwarding does not read, modify or expose participant credentials. Existing public survey, shared-link, invitation and synthetic-demo routes retain their behavior.

## Compatibility and acceptance boundary

Verified0.9.1 source redirected `#participant` to `/#survey`; the legacy surface still owns `participantToken` resume and server receipt/form recovery. This patch changes only that redirect to `/legacy/#participant`. Normal current-shell `#survey` stays the public code-entry page; `#survey=...` stays the standalone shared-link page. No backend/schema, auth policy, data mutation or production-only patch.

## Persona outcome scorecard

| Persona/scenario | Evidence class/environment/source | Passed / attempted / failed / untested | Satisfaction / confusion / frustration | Comparable baseline delta |
|---|---|---|---|---|
| Returning code-based participant reaches legacy resume route; public entry, shared-link, invitation and sample routes preserved | Executed actual shell scrubber with local fixture; paired issue108 source and route regression | 5 / 5 / 0 / 0 named route cases | NOT MEASURED; no human sample | Baseline0.9.1 reproducibly redirected participant resume to public code entry |
| Returning participant redeems local code then reopens current-shell participant URL with existing token | Independent root-operated Chrome against real candidate UI at local51319, product source `e456e108b00a97011929e44db702481a8845adb6` | 1 / 1 / 0 / 0 | NOT MEASURED; no human sample | Home → Take survey → LOCAL-FIXTURE code → legacy form; direct current-shell `#participant` then resumed the same one-question form without code entry or browser-storage injection |

The initial Chrome extension-UI blocker was superseded by the independent local-browser proof above. No live participant or response write was performed. Current-head independent review, checks and canonical DEV validation remain pending. Production must promote the same verified version/source/pin; no production-only fix, live response submission, seed replay or manual deployment is authorized here.
