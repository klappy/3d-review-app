{
  "name": "3d-review",
  "version": "0.0.1-phase0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "migrate:local": "wrangler d1 execute 3d-review --local --file=migrations/0001_init.sql",
    "seed:local": "wrangler d1 execute 3d-review --local --file=seed/synthetic.sql",
    "parity": "node scripts/parity.mjs"
  },
  "dependencies": { "hono": "^4.6.0" },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.30.0"
  }
}
